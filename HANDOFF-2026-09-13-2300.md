# Handoff: sparse-adversary wiring theorem (node N1) landed

**Date**: 2026-09-13 23:00 UTC · **Branch**: `wip/adder-tower-c9` · **HEAD**: `2cffd31`

## 🎯 What we're doing
`KICKOFF-2026-09-13-sparse-adversary-wiring.md` asked for exactly one node of the
hitting-set graph (`docs/hitting-set-invariant-2026-09-13.md`): prove that a
block-sparse adversary defeats any per-block hitting set, plus two concrete
non-hitting-set corollaries.  That node is done and committed.  The session was
scoped to this node only ("touch no other file"), so nothing else was started.
`box done --green` was signalled and accepted; the treadmill stops after this lap.

## 🧠 Context to carry forward
- **Adversary.** `α = B · LiouvilleNumber.remainder g k₀` with `k₀ = L + k + 1`, block
  boundaries `e i = (i + k₀ + 1)!`, `L = S.sup (m ↦ m·B) + 1` so `m·B < g^L`.
  Gaps `e (i+1) − e i ≥ L + k + 2`, `e 0 ≥ L + k`.  Irrationality: Liouville number
  minus the rational partial sum (`Irrational.sub_ratCast`), then `Irrational.natCast_mul`.
- **Digits of `m·α`.** `blockSeq g L c e` (with `c = m·B`) is defined by `Nat.find` over
  "which block contains `n`"; `block_unique` makes that well-defined.  The analytic crux
  `realOfDigits_blockSeq` was proved WITHOUT tsum rearrangement: partial sums at block
  boundaries (`sum_range_blockSeq`, induction, `Finset.sum_range_reflect` +
  `Nat.mod_pow_succ` for the base-`g` expansion), then `tendsto_nhds_unique` along the
  strictly monotone subsequence `e`.  The repo's bridge `digitOf_realOfDigits` then
  recovers `blockSeq` as `digitOf g (Int.fract (m·α))`.
- **Window argument.** `paddedDigits_getElem` and `blockSeq_near` both read as
  `(Nat.digits g c).getD (…) 0` (via `Nat.getD_digits`), which makes the leading-zero
  positions of a block and the padding zeros the same case.  The infix is
  `take k (drop t l)` with `List.ext_getElem`.
- Both corollaries closed by kernel `decide` (no `native_decide`, no `Lean.ofReduceBool`):
  `Nat.digits` and `List.IsInfix` on these sizes reduce fine.
- N2 / N4 remain frozen Props (kickoff said do not attempt).  `DIRECTION.md` (read, not
  edited) still mandates the Mahler run+jump chain in `MahlerRunJump.lean`; that is the
  repo's headline thread for a future unscoped lap.

## ✅ State
- `src/NormalNumbers/SparseAdversary.lean` new; import added to `src/NormalNumbers.lean`.
- `lake build` green, 8889 jobs (observed; pre-commit hook re-ran it green).
- `#print axioms` on `not_isHittingSet_of_avoider`, `pair_one_eleven_not_hitting`,
  `triple_one_three_five_not_hitting`: exactly `[propext, Classical.choice, Quot.sound]`.
- Committed `2cffd31`, not pushed.  Working tree clean.

## 🎬 Next actions
1. Host: push `wip/adder-tower-c9`; update the hitting-set docs table to cite
   `SparseAdversary.lean` for N1 and the two corollaries (docs edit was outside this
   session's scope).
2. Next unscoped lap: obey `DIRECTION.md` (run+jump chain, `MahlerRunJump.lean`), or, if
   the operator re-scopes to the hitting-set graph, N2/N4 need a new idea — the sparse
   adversary is now a reusable tool (`not_isHittingSet_of_avoider`) for any further
   "S is not hitting" claims: just supply `B`, `w`, and a `decide`.

## ⚠️ Gotchas
- `IsHittingSet` lives in namespace `NormalNumbers.Literature`; refer to it as
  `Literature.IsHittingSet` from `namespace NormalNumbers`.
- Inside `rw [lemma … (by omega) …]`, implicit args of `lemma` are still metavariables
  when `omega` runs → give them explicitly (`(i := i) (n := …)`).  Likewise `Monotone e`
  applied to `Nat.succ_le_of_lt h` yields an `e i.succ` atom omega won't identify with
  `e (i+1)`: use `hmono (show i + 1 ≤ j by omega)`.
- `push_neg` is deprecated in this mathlib; use `push Not`.  `le_or_lt` is gone; `le_or_gt`.

## 📁 Key files
- `src/NormalNumbers/SparseAdversary.lean` - the whole node.
- `docs/hitting-set-invariant-2026-09-13.md` - the graph N1–N4 this feeds.
- `KICKOFF-2026-09-13-sparse-adversary-wiring.md` - the spec this session executed.

---
**→ Next session: this is your starting point. Don't summarize this doc back to
Trevor or wait for instructions, and don't offer other projects from the KB - this
doc IS the chosen thread. Absorb the context above, then pick up at "Next actions"
#1 and start working.**
