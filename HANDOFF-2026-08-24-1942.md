# HANDOFF 2026-08-24 19:42 — B–Y Lemmas 7/8/9 + Prop 12 done; W5 Lemma-13 inputs staged

**Branch/HEAD**: master @ `584ad94`, clean tree, `lake build` green (8735 jobs).
Supersedes HANDOFF-2026-08-23-2330.md.

## State

DIRECTION.md CURRENT DIRECTIVE still points at W4 (all its items (1)–(5) are
now PROVED); work has continued along its critical path into the W5 inputs.
`src/` is **sorry-free**; every lemma below verified axiom-clean
(`#print axioms` = trust triple) this session.

## Done this session (8 green commits)

All lap-authored W4/W5 groundwork, judge ratification pending (proposals,
no JUDGE-frozen statement touched):

- `BaryBlockCount.lean` — **B–Y Lemma 8**: `card_baryDiscrepancy_ge_le`
  ≤ 2b^{k+1}e^{−bε²k/6} for 0 ≤ ε ≤ 1/b (no 6/k ≤ ε needed).  Pure
  combinatorial Chernoff: `sum_exp_digitCount` tilt identity
  (`Finset.sum_prod_piFinset`), λ = ±bε/2, `Real.exp_bound` order 2.
- `BaryConcat.lean` — **B–Y Lemma 9** parts 1/2a/2b (`HasDiscLt`,
  deviation form) + `digitCount_eq_count_ofFn` bridge.
- `CFConcat.lean` — **B–Y Lemma 7**: `countOccurrences` calculus (cons
  recursion, superadditivity, seam bound ≤ k−1 straddle) + `CFDiscLt`
  parts 1/2a/2b.  2a/2b hypothesis is `|u|+(k−1) < ε|x|` (absorbs
  straddle; trivial for the schedule).
- `TBrickDefs.lean` — W5 d-ary machinery: `daryCell`, `volume_daryCell`,
  **Prop 12** (`interval_subset_daryCell_two`); `badBlocks` +
  `card_badBlocks_le`; `daryBadZone` (+`volume_daryBadZone_le('`) and
  `daryBadZoneWide` (+ 6d bound); generic `volume_iUnion_geom_le`
  geometric-tail; summed zones (`volume_iUnion_daryBadZone(Wide)_le`);
  digit semantics: `exists_block_of_lt`, `floor_subCell_bounds`,
  `exists_goodBlock_of_notMem_badZone`, `badBlock_cell_far`.
- `CFWordBridge.lean` — `iterate_mem_cfCylinder_iff`,
  `blockCount_eq_card_matches`,
  `blockCount_sub_countOccurrences_bounds` (orbit count vs digit-word
  window count differ ≤ |v|) — connects `chebyshev_blockCount_brick`
  to `CFDiscLt` of the appended word.

## KEY ROUTE DECISION (recorded in PENDING_WORK)

B–Y's uniform-m_d bookkeeping (tight J_n window, 16e^{4c}) does not fit
the repo's Lemma-5 substitute (`half_mass_long_extensions` bounds cfK
above only).  Instead: pick m_d PER CHOSEN cylinder J (maximal with
|J| ≤ d^{−m_d}; Prop 12 ⇒ ratio > 1/(2d)) and have J avoid the wide bad
zones at ALL orders ≥ m_min(n) (summed zone lemma).  Brick ratio
constant: 1/(2d).

## Next attack (Lemma 13 assembly)

1. Define the t-brick structure: CF word w + per-base (m_d, j_d, r_d≤2),
   `cfCylinder w ⊆ daryCell …`, ratio field ≥ 1/(2d) (in ℝ via
   `volume_cfCylinder`).
2. Selection lemma inside I_w: half-mass good-length collection
   (`half_mass_long_extensions`, C from that lemma) minus CF bad zone
   (`chebyshev_blockCount_brick`, converted γ→Leb via
   `gaussMeasure_le_volume`/`volume_le_gaussMeasure`) minus the ≤ 2(t−1)
   summed wide d-ary zones (two cells per base d ≤ t) is still positive
   for n ≥ n₀(t,ε) ⇒ a good J exists.  Note all bads are o(|I_w|):
   CF bad = O(1/n)·|I_w|, d-ary = O(ρ^{m_min})·|σ_d| with the σ_d-to-I_w
   comparison from the brick ratio.
3. m_min(n) computation: good J have |J| ≥ (1/2)e^{−2Cn}|I_w| (distortion
   ×2 + cfK ≤ e^{Cn} ⇒ |I_u| ≥ 1/(2e^{2Cn})), so orders m with sub-cells
   meeting J are ≥ log_d(1/(2d|σ_d|... )) — grind carefully.
4. Then Defs 10–11 refinement predicate + Lemma 13 statement, t′ = t+1
   case via Prop 12 (ratio 1/(2(t+1))).

## Gotchas (this session)

- `omega` fails silently when ℤ-pow atoms have mixed coercion shapes
  (`(↑d)^k` vs `↑(d^k)`) or across ℕ/ℤ: add a bridge equation
  `((d:ℤ))^k = ((d^k : ℕ) : ℤ) := by push_cast; ring` and it closes.
- `set x := e with hx` + omega: floor/let-values get unfolded; `clear_value`
  before omega.
- `rw [hipre]` where `hipre : w = take …` rewrites w.length too → min-mess.
  Derive a separate `htake` equation and `rw [← htake]` instead.
- Current mathlib: `abs_add` → `abs_add_le`; `List.drop_append`/
  `take_append` are the general (truncated-sub) forms;
  `Finset.sum_boole` for Σ ite = card; `push_neg` deprecated → `push Not`.
- `Finset.sum_prod_piFinset` + `Fintype.piFinset_univ` is the
  (Σ over Fin k → Fin b) ∏ ↔ (∑…)^k identity; pass `g` explicitly
  (higher-order unification fails otherwise).
