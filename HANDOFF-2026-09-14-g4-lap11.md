# HANDOFF 2026-09-14 — G4 disjunctivity, lap 11 (the explicit grid; in progress)

Branch `wip/g4-disjunctivity`.  Build 🟢 `lake build` 8924 jobs.  Trust triple everywhere.

## Proved this lap — `src/NormalNumbers/G4ScheduleGrid.lean` (new)

Step 1 of the lap-10 resume order (`gridParams_of_KN`) is done, as **`gridOf K N hK : GridParams`**
with `s = K²`, `B = K²(K+N)+1`, `U = K²∑_{i<K}B^{i+1}`, `D₀ = K·U`, **`Q = (U+K+N+2)!`**
(`hQdvd` is `Nat.dvd_factorial`; no `lcm`).  Explicit ℕ sizes, all proved:

* `gridOf.d_le`        — `d_α ≤ gridDm K N = 1 + Q(D₀+U)`
* `gridOf.shiftAL_le`  — `ρ_i ≤ (K+N)·Dm`;  `gridOf.add_shiftAL_le` — `n + ρ_i ≤ X + (K+N)·Dm` (the `Mx`)
* `gridOf.P₀_le`       — `P₀ ≤ gridP₀Bound K N = Dm^{2H}(2T+1)^{2T+1}((K+N)Dm)^{T²}`,
                          `H = (K²+1)^K`, `T = H·N` (`gridOf.card_Idx`)
* `card_apSample_ge/le` — `X/P₀ − 1 ≤ |P| ≤ X/P₀ + 1`;  `apSample_nonempty_of_le` from `2P₀ ≤ X`.

Step 2 (`card_apSample`) is therefore also done.

## Also proved this lap — `G4ScheduleParams.lean`, `G4ScheduleFar.lean`

The schedule is now **explicit in `K`** (design record: `PENDING_WORK.md` §lap 11): `N = 100K²`,
`Q = W!`, `m₁ = 100·8^K·K^{2K+1}`, `R = 2^{2^{m₁}}`, `Y = 2^{2^m}` with `m = m₁ + 8K²`,
`X = Y^{100}`, `Mc = 3·10⁴·T·m₁`.  `K` is chosen from `ℓ`, then `X` from `K`; this is not the
forbidden "freeze `K`, `X → ∞`" (there is no limit in `X` at all).

* `Sched.log_gridP₀Bound_le` — `log P₀ ≤ logP₀Nat K = (2H+T²)(3W²+J)+(2T+1)²`, and the whole
  ladder `… ≤ K^{cK+d}` (`logP₀Nat ≤ K^{20K+17}`, `Mc ≤ K^{6K+9}`, …).
* `Sched.farC_le` — `farC ≤ logP₀Nat K + m + 10`; `Sched.card_apSample_ge_half`,
  `Sched.sample_nonempty`, `Sched.two_mul_P₀_le_X`, `Sched.gridDm_le_X`.
* **`Sched.hfar_holds`** — the `hfar` field, PROVED against `η = 2^{−K}` (weaker than the
  witness's `2^{−K/4}`, so it transports), from the ℕ inequality
  `4K(logP₀Nat + m + 2J + 13) ≤ 4^N` (`four_mul_le_four_pow_N`).

## Resume here

3. ~~`hfar`~~ DONE (`Sched.hfar_holds`).
4. **`hbig`** (`G4ScheduleBig.lean`): `Nat.log 2 Y = 2^m`, `Nat.log 2 R = 2^{m₁}`, so the
   dyadic ratio is `2^{m₂}` and `log(2^{m₂}) ≤ 8K²`; term 2 needs `Y²/|P| ≤ 2Y²P₀/X ≤ 2^{−K}`;
   term 3 is `log Mx/log Y ≤ 101`.  Close on `16K²2^{−5k₄} + 272K·2^{−3k₄} ≤ 1`.
   Then **`hbudget`** (`G4ScheduleBudget.lean`): `Λ ≤ exp(rK)`; main term via lower Mertens
   `log_log_le_sum_inv_primesBelow` and `2^{ω(P₀)} ≤ P₀`; terms (a),(d) via `R^{Mc} ≤ X^{1/20}`;
   (b),(c) via the dyadic upper bound `sum_inv_primes_Ioc_le` (constant 4) — see the refuted
   crude bounds in `PENDING_WORK.md`.
5. Assemble `Nonempty (ScheduleWitness ℓ w)` for `X` large (depending on `ℓ`), then
   `isDisjunctive_four_of_witness`.

Refuted parameter values (lap 10) still stand: never `lam = 1`, never `N ≈ 10K log K`.
