# HANDOFF 2026-09-14 — G4 disjunctivity, lap 11 (ALL FIVE §5 INEQUALITIES PROVED)

Branch `wip/g4-disjunctivity`, HEAD `3668b7c` (tree clean, not pushed).  Build 🟢 `lake build`
**8929 jobs**.  Every new theorem prints `[propext, Classical.choice, Quot.sound]`; no `sorry`,
no axiom anywhere in the G4 wing.

## State of the campaign

Brief §4 was closed in laps 6–10.  Brief §5 — the `ScheduleWitness` inequalities — is now
**closed as mathematics**: `hB` (lap 10, `gridParams_hB`) and, this lap, `hfar`, `hbig`,
`hbudget` (which contains the Jackson term).  Nothing analytic remains.  What remains is the
**assembly**: constructing one `ScheduleWitness ℓ w` per omitted cylinder from these theorems
and feeding it to `isDisjunctive_four_of_witness`.

## The explicit schedule (design record in `PENDING_WORK.md` §lap 11)

`K` is chosen from `ℓ` first, then everything from `K`, then `X` last.  Not a limit in `X`.

    k₄ = 8464·ℓ²·16^ℓ,  K = 4k₄,  M = 4232·ℓ·16^ℓ   (8ℓM = K, 2ℓM = k₄, K ≥ 541696)
    s = K², r = (K²)^K, H = (K²+1)^K, N = 100K², J = K+N, T = H·N
    G = gridOf K N   (B = K²J+1, U = K²∑B^{i+1}, D₀ = K·U, Q = (U+K+N+2)!)
    η = 2^{−k₄}, ε = 1/K, D = Dj K k₄ = (16K·2^{k₄})², δ₁ = δbig = δfar = 1/8
    m₁ = 1000·8^K·K^{2K+1}, m₂ = 8K², m = m₁+m₂
    R = 2^{2^{m₁}}, Y = 2^{2^m}, X = 2^{100·2^m} = Y^{100}
    Mc = 10⁵·T·m₁, lam' = e, lam = 13/2, Lg = r(log2 + 23√K), Dm = gridDm K N, Mx = X + J·Dm

## Proved this lap (modules, all trust triple)

* `G4ScheduleGrid.lean` — `gridOf K N : GridParams`; `d_le`, `shiftAL_le`, `add_shiftAL_le`,
  `P₀_le : P₀ ≤ gridP₀Bound K N`; `card_apSample_ge/le`, `apSample_nonempty_of_le`.
* `G4ScheduleParams.lean` — the parameters above (namespace `Sched`), the `K^{cK+d}` ladder,
  `log_gridP₀Bound_le : log P₀ ≤ logP₀Nat K`, `P₀_le_exp`.
* `G4ScheduleFar.lean` — `X, Y, R`; `two_mul_P₀_le_X`, `gridDm_le_X`, `sample_nonempty`,
  `card_apSample_ge_half`, `farC_le`, **`hfar_holds`** (against `η = 2^{−K}`, weaker than
  `2^{−k₄}`; transport by monotonicity at assembly).
* `G4ScheduleBig.lean` — `J_mul_gridDm_le_X`, `P₀_le_two_pow`, `dyadic_factor_le`,
  `sample_term_le`, `log_Mx_div_le`, **`hbig_holds`** (statement has `K = 4k₄`, `η = 2^{−k₄}`,
  `Mx = X + J·Dm` as a cast ℕ).
* `G4ScheduleHarmonic.lean` — `sum_inv_smallPrimes_ge` (`≥ m₁log2 − 21K² − 4`),
  `sum_inv_smallPrimes_le` (`≤ 3m₁ + 5`), `card_small_subsets_le`, `card_smallPrimes_le`.
* `G4ScheduleBudget.lean` — `Dj`, `jackson_term_le`, `Lambda_le`, `main_term_le`, `term_a_le`,
  `term_b_le`, `term_c_le`, `term_d_le`, `budget_assembly`, **`hbudget_holds`** (statement uses
  `T K` for `Fintype.card G.Idx` — `gridOf.card_Idx` is the bridge — and `(K²)^K` for `G.rDim`).

## Refuted on paper this lap (do not retry) — `PENDING_WORK.md` §lap 11

`∑_{sm}1/p ≤ log R + 1`; `ω(P₀) ≤ P₀`; subset count `2^{|sm|}`; `m₁ = 100·…` (69 < 128);
`Mc = 3·10⁴·…` (term (c) exponent positive).

## Resume here — the ASSEMBLY (`G4ScheduleAssembly.lean`, new)

    theorem witness (ℓ w : ℕ) (hℓ : 1 ≤ ℓ) : ScheduleWitness ℓ w

with `k₄ := 8464*ℓ^2*16^ℓ`, `K := 4*k₄`, `G := gridOf K (Sched.N K) _`, and fields:
* `hK, hr` — positivity;  `X := Sched.X K`, `hne := Sched.sample_nonempty`.
* `η := (1/2)^k₄`, `ε := 1/K`, `hε1 : 1/K < 1`.
* `M := 4232*ℓ*16^ℓ`, `hM : 1/(4^ℓ)^M ≤ η` — equality: `(4^ℓ)^M = 2^{2ℓM} = 2^{k₄}`.
* `Lg := (G.rDim:ℝ)*(log 2 + 23√K)`, `hlog` from `log_det_one_add_tensorGram_le'` (needs
  `G.s = K^2`, `((K:ℝ)^2)^K = (G.rDim:ℝ)` by cast).
* `δ₁ := 1/8`, `hB := gridParams_hB rfl hℓ (K ≥ 33856ℓ²16^ℓ: `4·8464 = 33856`) (8ℓM = K)
  (8ℓM ≤ K+8ℓ) hη0 (η⁴ = (1/2)^K : `pow_mul`) hLg`.
* `R := Sched.R K`, `hR := R_ge_two`, `Y := Sched.Y K`, `hRY` (`m₁ ≤ m`).
* `D := Dj K k₄`, `hN : 1 + clog 4 (2^K·D) ≤ N` — `2^K·D ≤ 4^{N−1}`; use
  `Nat.clog_le_of_le_pow`; `2^K·D ≤ 2^{K + 4k₄+14} ≤ 2^{2(100K²−1)}` (cf. `two_Dj_add_one_le`).
* `Mc := Sched.Mc K`, `hMc := Mc_pos`; `lam' := exp 1`, `hlam' : 1 ≤ exp 1`; `lam := 13/2`.
* `Mx := ((X + J·Dm : ℕ) : ℝ)`, `hMx1`, `hMx := gridOf.add_shiftAL_le` (cast).
* `Dm := gridDm K N`, `hDm := gridOf.d_le`.  `δbig := 1/8`, `δfar := 1/8`.
* `hbig := hbig_holds rfl hK`; `hfar := (hfar_holds hK).trans (… (1/2)^K ≤ (1/2)^k₄)`;
  `hbudget`: `have := hbudget_holds rfl hK; rwa [← gridOf.card_Idx] at this` (and `rDim` is rfl).
* Expect friction only from `G.K`/`G.N`/`G.rDim` being `(gridOf …).K` syntactically (all `rfl`)
  and from whnf on the huge numerals — never `decide`, never `norm_num` on `X`; use `set`-free
  `exact`/`rfl` and `show`.

Then `isDisjunctive_four_of_witness (fun ℓ w hw _ => ⟨witness ℓ w (ℓ ≥ 1 from w < 4^ℓ? — NO:
ℓ = 0 is the empty word, `w = 0`; handle `ℓ = 0` separately: every cylinder `[0,1)` is hit,
so the omission hypothesis is contradictory)⟩)` ⟹ **`IsDisjunctive 4 primeLambertFour`**
unconditional, then `isDisjunctive_two_of_four`.  `#print axioms` both.  Then `box done`.
