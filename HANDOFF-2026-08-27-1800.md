# HANDOFF 2026-08-27 18:00 — Routes A′+B′ proved, C′ unblocked (layering fix)

**Branch/HEAD**: master @ `34e2482`, `lake build` green (8746 jobs).
Supersedes `HANDOFF-2026-08-27-1200.md`.

## What landed this run (all axiom-clean, `[propext, Classical.choice, Quot.sound]`)

Four laps, each ending in a green commit:

1. **`integral_logTailFn_eq` / `integral_logTailFn_tendsto`** (route A′,
   `Khinchin.lean`): the single-digit tail indicator `logTailFn K x = if K <
   cfDigit x 0 then log(cfDigit x 0) else 0` integrates to EXACTLY the
   Gauss–Kuzmin log-tail `log K₀ − Σ_{k<K} γ([k+1])·log(k+1)`, proved via a
   tsum/integral swap over per-digit-value cylinder indicators
   (`MeasureTheory.integral_tsum_of_summable_integral_norm` +
   `hasSum_nat_add_iff'` against `gaussKuzmin_logsum_hasSum`). This is an
   EXACT identity, not an estimate — stronger than the original handoff's
   MCT sketch anticipated.
2. **`integrable_logTailFn`**: `logTailFn K` is integrable, via the `ℝ≥0∞`
   analogue of the same tsum-swap (`lintegral_tsum`, which needs no
   summability side-condition).
3. **`logBirkhoffSum` / `integral_logBirkhoffSum`** (route B′): the `n`-step
   Birkhoff sum `Σ_{i<n} logTailFn K (Tⁱx)` and its first moment `n · ∫
   logTailFn K dγ`, mirroring `integral_blockCount` (`CFBlockFreq.lean`) via
   `MeasurePreserving.integrable_comp_of_integrable` + `integral_map`.
4. **`logBadZone` / `markov_logBadZone_brick`** (route B′ complete): the
   Khinchin log-tail bad zone (points of brick `I_w` whose `n`-step log-tail
   mass past cutoff `K` exceeds `η·n`), with a Markov (first-moment, NOT
   Chebyshev) measure bound `γ(logBadZone) ≤ 7·(∫ logTailFn K dγ)/η·γ(I_w)` —
   crucially UNIFORM IN `n` (the `n` in the Markov threshold and the `n` in
   the first moment cancel exactly). Uses `gaussMeasure_brick_inter_le`
   (`CFBlockFreq.lean`), the same distortion step `chebyshev_blockCount_brick`
   uses for the CF bad zone.
5. **Layering refactor — new file `CFLogTail.lean`** (route C′ unblock, the
   most important structural finding this run): all of the above (except the
   two khinchinK₀-VALUE-specific theorems) was discovered to need ZERO
   dependency on `khinchinK₀` — only on `gaussMeasure_digit_cylinder`. But it
   had been built in `Khinchin.lean`, which is DOWNSTREAM of `Headline.lean`
   → ... → `TBrick.lean` (the file that owns the union-bound plumbing route
   C′ needs to touch). `TBrick.lean` could never have seen it there. Moved
   `logTailFn`/`logBirkhoffSum`/`logBadZone`/`markov_logBadZone_brick`/
   `integrable_logTailFn` to a NEW file `CFLogTail.lean` that imports ONLY
   `CFBlockFreq` (upstream of `Headline`), including a from-scratch
   `summable_gaussKuzmin_log` (reproving `khinchinK₀_summable_log`'s
   comparison argument without referencing khinchinK₀). `Khinchin.lean` now
   imports `CFLogTail` and keeps only the khinchinK₀-specific glue:
   `integral_logTailFn_eq` instantiates the new GENERIC
   `integral_logTailFn_eq_of_hasSum` (upstream) at `L = log khinchinK₀` via
   `gaussKuzmin_logsum_hasSum`. **Verified**: a throwaway file importing both
   `Khinchin.lean` and `TBrick.lean` type-checks `logBadZone`/
   `markov_logBadZone_brick` alongside `TBrick`'s
   `exists_mem_notMem_union_of_bounds` — route C′ is unblocked with ZERO
   `TBrick.lean` edits needed.

## NEXT: route C′ — thread the third zone into the union bound

Per `DIRECTION.md`'s standing plan ("union it into B₂ the d-ary group"): a
NEW downstream construction (in `Khinchin.lean` or a new file downstream of
both `TBrick.lean` and the khinchinK₀ theory — do NOT edit `TBrick.lean`
itself) that:

1. Picks `K` large enough (via `integral_logTailFn_tendsto`) that
   `7·(∫ logTailFn K dγ)/η` drops below a target coefficient (e.g. `1/6`,
   to fit the union budget alongside the two existing Tier-1 zones —
   `exists_mem_notMem_union_of_bounds` in `TBrick.lean:244` takes only TWO
   zones `B₁, B₂` with `p+q<1/2`; fold `daryBadZoneWide` ∪ `logBadZone` into
   ONE combined `B₂'` via `measure_union_le` subadditivity, tightening the
   two existing `<1/4` coefficients to `<1/6` each so `p + (q+r) < 1/2`).
2. Builds `exists_good_avoiding_bad_khinchin` (mirrors
   `exists_good_avoiding_bad`, `TBrick.lean:470`) with the extra conclusion
   conjunct `x ∉ logBadZone B.w n K η` (i.e. the witness's `n`-step log-tail
   mass past `K` is `≤ η·n`).
3. Threads this through `exists_refinement_uniform`
   (`TBrickRefine.lean`/`CFSchedule.lean`) and the `xstar` schedule
   rederivation to discharge `xstar_log_tail_uniform` (`Khinchin.lean`,
   the sole remaining `sorry`) — target its clean nonneg-tail form via
   `xstar_logTail_eq` (already proved, `Khinchin.lean`).
4. Route D′ (layering, still open): move frozen `KhinchinTypical`/
   `khinchinK₀` defs (currently `Headline.lean`) to an upstream module
   VERBATIM so `Headline.lean:134` can close with `xstar_khinchinTypical`
   once the crux is discharged.

This is real plumbing work (matching `xstar`'s specific schedule
construction to the abstract `logBadZone` machinery) — expect it to take
multiple laps. The analytic content (routes A′+B′) is now COMPLETE and
axiom-clean; what remains is genuinely mechanical threading, not open math.

## No uncommitted edits. `src/` open sorries: `Headline.lean:134` +
`xstar_log_tail_uniform` (`Khinchin.lean`) — unchanged, the active
decomposition, not regressions.
