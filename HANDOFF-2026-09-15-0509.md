# Handoff: treadmill stop lap — objective R + S item 1 complete, run halted green

**Date**: 2026-09-15 05:09 UTC · **Branch**: `wip/g4-entropy` · **HEAD** `3da7828` ·
`lake build` 🟢 **9042 jobs** (observed this session, from a clean tree)

## 🎯 What we're doing

The `AFTER THE EXTRACTION` campaign in `DIRECTION.md` is finished. Its last attended addendum
(2026-09-15 01:04 EDT) closed objective **R** (row-balanced cancellation on `D_s^{⊗K}`) with a
kernel verdict and authorized exactly one successor — review lap 166's **objective S item 1**,
the seam `Sched.budget_confines` — then: stop, write the handoff, take no further successor.
Both are done. This lap verified that and signalled the treadmill stop. **No new proof work.**

## 🧠 Context to carry forward

**The session brief was stale, and a successor will hit the same trap.** My kickoff instruction
said "execute R in order: R-a, R-b, R-c/R-d". That text quotes DIRECTION's **23:59** addendum.
The **01:04** addendum, further down the same ACTIVE override, already records `R ✅` and moves
on. R was closed at lap 136 (counterexample `ex_not_ignoring`: row-balanced does **not** force
ignoring a coordinate) with its residue cleaned up in laps 146–164. *Read to the bottom of the
ACTIVE override before acting on any brief that quotes it* — the addenda accumulate, and the
newest attended one wins over both the brief and any HANDOFF pointer.

**`box done` vs `box done --green`.** Plain `box done` is refused here by the repo-wide
sorry-free gate — a known tooling defect, since the two survivors are DIRECTION forbidden-drift
items that this campaign was never scoped to touch. The addendum also forbids `box stuck` (it is
not operator-blocked, it is out-of-scope). `box done --green` is the matching exit and was
accepted: the assigned objective was a verified-green stop, not a sorry-free repo.

**How to state R/S's results, precisely.** Astra's review point (KB, 2026-09-15 00:05) was right
and is easy to re-break. The design prose "`K′ ≥ 3K/8` is forced" invites reading an upper bound
as a lower one. What the kernel actually proves are two *lower* bounds on the number of
**cancelled** layers, both derived *from* the capture inequality:
`Budget.budget_forces_two_layers` / `Capture.two_le_of_insideCapture` give `2 ≤ K′`, and
`RowVariance.three_eighths_of_budget` gives `K′ > (3K − 8)/8`. Nothing bounds `K′` above; nothing
shows `K′` is achievable. Claim those two and no more.

**The one live mathematical residue, for whenever a successor is authorized** (already in
`PENDING_WORK.md`, ACTIVE): the gap is **counting-side only**. A sampler whose blocks carry fewer
than `8E + 5` atoms is excluded by nothing proved — not by the counting bound (vacuous below that
threshold, and lap 164's probe shows `skel` is loose only by a constant factor *in the exponent*,
so it will not be rescued by sharpening), not by the error budget (`InsideCapture` constrains the
row second moment, not how atoms are grouped), and not by the entropy certificate (lap 169's
`G4EntropyBlocks`: blocking is subadditive, so the certificate survives down to blocks of size
one and supplies no minimum block dimension). The transport identity is atom-local, so nothing
visible forbids singleton blocks. That is where a future escape lives — and it is the *only*
place left, the other two of DESIGN §0's three deformations being closed
(`Sched.balanced_union_le`, `RowVariance.union_le_of_determining`, plus lap 166's
`G4GroupedVerdict` for the third at the schedule).

**Assumption I operated under**: nothing in laps 166–170 is, or should be presented as, a claim
about the normality of `G₄`. The campaign's results are about the tensor matrix and the sampling
schedule.

## ✅ State

- Full `lake build` re-run green this session: **9042 jobs**, from a clean tree. Only linter
  warnings (`G4EntropyBlocks.lean`: unused section var `[DecidableEq A]` on `mem_atomsIn` /
  `sum_card_atomsIn`; one deprecated `push_neg`). Cosmetic, deliberately left under one-writer.
- Tree clean; **no** untracked `G4BalancedRigidity.lean` skeleton (the brief warned of one from
  an aborted lap — that module is committed and compiling).
- Objective S item 1 landed pre-session: `e148a7e` (`Capture.InsideCapture`,
  `two_le_of_insideCapture`, `Sched.budget_confines`, `Sched.grouped_budget_confines`), hardened
  by `b14c49d` (re-routed `InsideCapture` through the `|S|`-free Mertens bound so the packaged
  hypothesis is *satisfiable*, not merely true — `Capture.structural_satisfiable`).
- No `axiom` declaration anywhere in `src/`. Headlines print `[propext, Classical.choice,
  Quot.sound]`.
- Stop signalled via `box done --green`; host re-verifies green before halting.

## 🎬 Next actions

1. **Do not start proof work on this branch without a new attended DIRECTION entry.** The ACTIVE
   override's stop condition is met and its 01:04 addendum forbids picking a successor.
2. If Trevor authorizes a continuation, the single on-path target is the counting-side residue in
   §"Context" above: decide whether a sampler with sub-`8E+5`-atom blocks can escape. Frozen
   `Prop` first; a named obstruction is a valid outcome.
3. Do **not** attack the two remaining `src/` `sorry`s — they are on the forbidden-drift list.

## ⚠️ Gotchas

- `src/` `sorry` count is **2** and that is correct, not a regression:
  `MahlerDriftOne.exists_prime_nonresidue` (a prime in `(p/3, p/2)` with prescribed Legendre
  symbol — character sums over primes, Linnik strength) and
  `PrimeLambertOscillation.phaseOscillation` (the open analytic obligation of an *open*
  irrationality problem; `irrational_primeLambert` is explicitly labelled sorry-gated). Both
  pre-expedition, off-path, forbidden-drift. Per the addendum they are **not** stuck strikes.
- The repo-wide sorry-free gate therefore refuses plain `box done` forever on this branch.
- Handoff filenames in this repo previously drifted ~4 days ahead of reality by each lap
  incrementing the newest filename. This file's stamp came from `date`, not from the listing.

## 📁 Key files

- `DIRECTION.md` — ACTIVE override + its 23:59 and **01:04** addenda; read to the bottom.
- `PENDING_WORK.md` — ACTIVE section holds the counting-side residue.
- `DESIGN-2026-09-15-deformation.md` §0 — the three deformations; all three now closed.
- `src/NormalNumbers/G4EntropyCapture.lean`, `G4GroupedVerdict.lean`, `G4EntropyBlocks.lean` —
  laps 166–169's output.
- `HANDOFF-2026-09-15-S-lap169.md` — the substantive predecessor; this doc is the stop lap.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor and don't
offer other projects from the KB — this doc IS the chosen thread. The campaign is COMPLETE and
stopped green; absorb the context above and, unless Trevor has added a new DIRECTION entry, say
so plainly rather than restarting the grind.**
