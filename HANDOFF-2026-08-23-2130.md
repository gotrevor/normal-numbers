# HANDOFF 2026-08-23 21:30 — W3 COMPLETE + W4 γ-mixing engine done

**Branch/HEAD**: master @ `b49b24c`, clean tree, `lake build` green,
`src/` SORRY-FREE, all headline axioms = propext/Classical.choice/Quot.sound.
Supersedes HANDOFF-2026-08-23-2040.md (which has the W3 module map).

## This session (laps 3–9)
1. **W3 ✅ COMPLETE**: all four frozen `CFMixing.lean` statements proved —
   `measurePreserving_gaussMap` (B1 flag), `volume_inter_preimage_eq_integral`,
   `cylinder_mixing` (C = 8 log 2, ρ = 9/10 — geometric; the Kuzmin escape
   valve was never needed), `gauss_kuzmin` (B4 flag).  New modules:
   CFDensity (density identity via reverse word-induction), CFRecursion
   (G_{k+1} = stepOp G_k + Lipschitz decay), CFInvariance (γ(T⁻¹S)=γ(S)
   branch telescope), CFPin (mixture Fubini + mean pin + envelope).
2. **W4 groundwork**: `CFGammaMixing.lean` —
   `setIntegral_inter_preimage` (s-started conditional density identity,
   `tChain` parameter) and `gaussMeasure_cylinder_mixing`:
   `|γ(I_v ∩ T^{-(|v|+g)}A) − γ(I_v)γ(A)| ≤ (9/10)^g·4|A|·γ(I_v)` —
   the KPW-Lemma-6 substitute, stronger than the planned summable-decay.

## Next (see PENDING_WORK.md for detail)
1. Block-frequency Chebyshev from γ-mixing (variance via pair terms;
   overlapping pairs crude-bounded), then the brick-conditioned version.
2. b-ary side: B–Y Lemmas 8/9 (check Counting.lean/Visits.lean overlap).
3. W4 statement FREEZING is judge-owned — draft proposals in drafts/,
   don't self-ratify.  Judge attention flagged in PENDING_WORK.md.

## Gotchas (this session; see also the 2040 handoff list)
- Pin renames: `mul_le_mul_right'`→use `gcongr`; `le_or_lt`→`le_or_gt`;
  `zero_le _`→`bot_le`; `inv_le_inv_of_le`→`inv_le_inv₀`;
  `List.dropLast_single` gone (use `show [b].dropLast = [] from rfl`);
  `Ioo_ae_eq_Ioc` is MeasureTheory-namespace root.
- `rw [defName]` under `convert`/binders fails ("equation theorems") —
  use `simp only [defName]` or explicit `have heq := by field_simp`.
- FTC-on-Ioo and Fubini-on-restrict recipes: worked examples in
  CFPin.lean (integral_mix_kernel, integral_gaussDensityReal_eq_mix).
- ∫₀¹ h_t dy = 1 antiderivative without t=0 singularity: (1+t)y/(1+ty).
- `foldl`-defined tChain: concat lemma via `List.foldl_append`; nil-case
  continuity needs `continuous_id'.continuousOn` (eta).
