# Handoff: lane-2 batch-2 target 2 done — π² signed-kick machine, run stopping

**Date**: 2026-08-29 20:45 UTC · **Branch**: master · **HEAD**: c7c5b48

## 🎯 What we're doing

Lane-2 operator laps per `HANDOFF-2026-08-29-lane2-brief-v2.md` (batch 2). This
run was SCOPED to target 2 (`sorry-free:src/NormalNumbers/PiSqBBP.lean`); target
met, `box done --green` issued and accepted, host halting. `DIRECTION.md`
CURRENT DIRECTIVE (novel-proofs doctrine) outranks the brief; these are lane-2.

## ✅ State

- **Target 2 DONE** (commit abd91b0): `PiSqBBP.lean` complete, sorry-free, full
  `lake build` green (8786 jobs, observed twice — direct + pre-commit hook).
  `#print axioms` observed on both headlines `piSq_boundary_of_zeroRun` /
  `piSq_boundary_of_maxRun` = `[propext, Classical.choice, Quot.sound]`.
- Contents: signed abstract layer (`kicked_tail_abs_le`: `|r m| ≤ A` for
  `m > n` ⟹ `|bⁿ(x−sₙ)| ≤ A/(b−1)` via `tsum_of_norm_bounded` vs geometric;
  boundary cores `boundary_of_fract_lt/_ge`; packaged
  `boundary_of_zeroRun_kickedAbs` / `boundary_of_maxRun_kickedAbs`), kick bound
  `abs_piSqKick_le` (`|piSqKick j| ≤ 48/(8j+1)²`, j ≥ 1), headlines with window
  `16⁻ᵏ + 52/(8n+1)²` conditional on frozen node `PiSqBBP`.
- Brief v2 `## Progress` updated (abd91b0); detailed session handoff at
  `HANDOFF-2026-08-29-lane2-target2b-done.md` (c7c5b48). Tree clean.

## 🧠 Context to carry forward

- **Key restatement** (the one real insight of the lap): the draft surrogate
  `kickedPartial 16 piSqKick n` was structurally unusable — `kickedPartial`
  indexes kicks from 1, so Formula 29's j=0 block (≈9.88, the only positive
  one) was missing and the "tail" would have been ~16ⁿ·9.88. Fix: shifted kick
  `piSqShiftKick m = 16·piSqKick (m−1)`, whose kicked series is exactly π² and
  whose partial is the honest `Σ_{j<n} piSqKick j/16ʲ`. Same trick will apply
  to any BBP formula whose k=0 block is included in the constant.
- The signed layer is deliberately abstract (any base/kick) and reusable for
  other signed BBP constants; it lives in `PiSqBBP.lean`, not `KickedOrbit.lean`
  (ADDITIVE-ONLY on landed modules).
- Node `PiSqBBP` (Formula 29, probe `experiments/pi_sq_bbp.py`, 88 digits)
  stays frozen CITED; its discharge is lane-2 owed, like `PiBBP` was.

## 🎬 Next actions

1. **Target 3** of brief v2: sharpen β = 26 in `LnTwoExpSepProof.lean` via NEW
   theorem `lnTwoExpSep_sharp` (do NOT edit `lnTwoExpSep_holds`). Lossy spots
   per that file's docstring: coefficient height (crude `8^ℓ` → Legendre value
   `P_ℓ(3) ~ 5.83^ℓ`) and zero-case remainder (`(1/6)(1/12)^ℓ` → sharper kernel
   estimate); rebalance `ℓ = c·n`. Multi-lap; named sub-lemmas expected.
2. Then: discharge node `PiSqBBP` itself (real-analytic proof of Formula 29).

## ⚠️ Gotchas

- `linarith` does NOT relate `16/X` and `48/X` as multiples of one atom —
  pre-state comparisons as `c·(1/X)` (via `mul_one_div`) before `linarith`.
- `gcongr` auto-closes side goals from context hypotheses; a queued follow-up
  tactic then fails with "no goals to be solved".
- ADDITIVE ONLY on frozen decls; never touch `CFScheduleA.lean:4400`/`:5774`;
  commit via `git-safe` (bare git is hook-blocked). Batch-2 targets 1 and 2
  are DONE — do not reopen.

## 📁 Key files

- `src/NormalNumbers/PiSqBBP.lean` — this lap's product (signed machine + headlines)
- `src/NormalNumbers/LnTwoExpSepProof.lean` — target 3's file (docstring lists lossy spots)
- `HANDOFF-2026-08-29-lane2-brief-v2.md` — operator brief with Progress log
- `DIRECTION.md` — overriding directive

---
**→ Next session: this is your starting point. Don't summarize this doc back to
Trevor or wait for instructions, and don't offer other projects from the KB —
this doc IS the chosen thread. Absorb the context above, then pick up at "Next
actions" #1: read `LnTwoExpSepProof.lean`'s docstring and decompose the β
sharpening into named height/remainder lemmas.**
