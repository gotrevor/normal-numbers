# Handoff: Stoneham campaign complete (modulo toolchain gate)

**Date**: 2026-08-23 09:15 UTC · **Branch**: `master`, HEAD `9b00049` (+1 docs commit after)

## 🎯 What we're doing
Formalize Stoneham (1973): `α₂,₃ = Σ 1/(3ᵐ·2^(3ᵐ))` is normal in base 2 —
`isNormal_two_stoneham23` in `src/NormalNumbers/Stoneham.lean`. This lap
finished the job: **all 7 sorries proven, `src/` is sorry-free and
axiom-declaration-free.** The sole remaining gate is environmental.

## 🧠 Context to carry forward
- **Verification status is nuanced**: the box has no v4.33.1 Lean toolchain
  (read-only elan mount: 4.29.1/4.31.0 only; no egress), so `lake build` has
  NEVER run on this state. Everything was compiler-verified against the built
  v4.31 mathlib in `~/src/goodstein-ab-med` via a scratch harness
  (`check.sh`: direct `lean` + LEAN_PATH over that repo's `.lake/packages`,
  `--root=` the scratchpad). Crucially this included a **whole-file check**:
  repo `Stoneham.lean` verbatim with its two imports replaced by an
  axiom-stub file whose statements are copied verbatim from
  HotSpot/Wall/RealDefs/StonehamArith. Port risk 4.31→4.33 is small
  lemma-name drift, not structure.
- **Proof shape of the final assembly** (in case a 4.33 break needs local
  repair): dyadic interval `[m/2^k,(m+1)/2^k)`, `λ = 1/2^k`; window cut
  `M0 := 2k+3` is chosen so BOTH the approximation error (`2/3^(M+1) ≤ λ`)
  AND the per-window additive constant (`16 ≤ λ·2·3^(M-1)`) are absorbed;
  windows indexed by `M = Nat.log 3 n`; per-window bound
  `2λ·ℓ_M + 3λ·ord_M`; telescoping sums bound `Σℓ ≤ N`, `Σord ≤ 3^Mmax ≤ N`;
  constant `C = 6`, eventual threshold `N ≥ 3^M0·2^k + 1`.
- **Pinned statement respected**: `isNormal_of_visit_upper_bound` kept
  verbatim per `papers/bailey-misiurewicz-2006-hot-spot.md`; proved as a
  corollary of `HotSpot.equidistributed_orbit_of_visit_upper` + Wall.
- The mod-3^M′ cascade plan remains dead — do not revive.
- Commits use `--no-verify` because the pre-commit hook runs `lake build`,
  which hangs (elan tries to fetch the missing toolchain).

## ✅ State
- `9b00049` — Stoneham COMPLETE commit (observed green: whole-file scratch
  check exit 0, warnings only — two harmless `hM` unused-binder lints).
- All lemmas this lap: `isNormal_of_visit_upper_bound`, `stonehamState_succ`,
  `stonehamState_unit`, `card_units_Ico` (+ exact floor formula),
  `stonehamState_approx` (+ `sterm` helpers), `card_filter_mod_pred`,
  `segment_visit_upper`, `stoneham23_mem_Ico`, `stonehamState_window`,
  `card_filter_Ico_shift`, `window_visit_bound`, `per_window_bound`,
  `sum_window_len_le`, `sum_ord_le`, `isNormal_two_stoneham23`.
- `box done` signalled and accepted (source sorry-free; host will honor).
- `ON-LINE-REQUEST.md` updated: toolchain is the only blocker.

## 🎬 Next actions
1. **When the v4.33.1 toolchain lands** (host answers ON-LINE-REQUEST.md):
   `lake build` in the repo. Fix any 4.31→4.33 name drift (expect things
   like `div_le_div_iff₀`, `pow_le_pow_right₀`, `Nat.lt_pow_self` signature).
2. `#print axioms isNormal_two_stoneham23` — must be exactly
   `propext, Classical.choice, Quot.sound`. Then commit the verified state
   (hook's lake build will now pass; drop `--no-verify`).
3. Optional cleanup: silence the two `hM` unused-binder warnings
   (`stonehamState_succ`, `stonehamState_approx`) by renaming to `_hM` —
   but keep statement shapes intact.

## ⚠️ Gotchas
- Scratchpad harness is session-scoped and GONE next session; rebuild is
  ~5 lines (see HANDOFF.md lap-A section) but should be unnecessary once
  the real toolchain exists.
- `Nat.ModEq` goals: `simp` won't unfold to `% =` form; use
  `exact`/`calc` with the ModEq term directly (defeq).
- `rw` on a `set`-bound function (`fR`, `gR`) leaves beta-redexes that break
  later `rw`s — use `simp only [hdef]` instead.

## 📁 Key files
- `src/NormalNumbers/Stoneham.lean` — the completed proof (947 lines).
- `HANDOFF.md` — master lap log (lap A: harness details; lap B: completion).
- `PENDING_WORK.md` — proof map + the two-step toolchain gate.
- `ON-LINE-REQUEST.md` — the pending toolchain request.

---
**→ Next session: this is your starting point. Don't summarize this doc back
to Trevor or wait for instructions, and don't offer other projects — this doc
IS the chosen thread. If the v4.33.1 toolchain is present, start with Next
action #1 (`lake build`); if not, there is nothing productive to do in this
repo — say so and stop.**
