# Handoff: hitting-set upper bounds U1–U4 + L1 + mono landed

**Date**: 2026-09-14 00:41 UTC · **Branch**: `wip/adder-tower-c9` · **HEAD**: `15945c8`

## 🎯 What we're doing
Kickoff `KICKOFF-2026-09-13-hitting-set-bounds.md`: turn four automaton verdicts (Python-verified
certificates in `experiments/certs/adder_cert_h{41,51,22,23}_*.json`) into theorems about the
hitting-set invariant `S(g,k)` (`Literature.IsHittingSet`), plus wire the base-5 Mahler lower bound.
Context doc: `docs/hitting-set-invariant-2026-09-13.md`. This lap completed the whole kickoff scope.

## 🧠 Context to carry forward
- Transcription follows `MahlerBase5Exact.lean` exactly: family `hXXChans (digits…)`, per-cert
  `live/rho/omega/forced` as list lookups, `checkCertA … = true := by decide +kernel`, then
  `signed_engine_g_single` per block, `fin_cases` on the channel membership.
- **h23 (ambient 26880) is the only heavy family.** Unchunked kernel decide took ~9 min per word and
  ~10 GB; the cost is superlinear in the `List.range n` size. Chunked into sixteen 1680-state
  `checkEdgesOnA` sweeps per word (≈10 s each) + one `checkForcedA`. Omega is packed 64 states per
  natural (values ≤ 7) in the `AdderTowerC8KData` scheme. Whole module compiles in ~19 min.
- **Gluing gotcha (cost one 18-minute rebuild):** the naive nested `checkEdgesOnA_add c0 (… c15)`
  term dies with `maximum recursion depth` at elaboration, even with a single level. Cause: a
  metavariable inside `lo + n` / `n + m` makes `isDefEq` unfold `checkEdgesOnA` itself (the whole
  sweep) to make progress. Fix in the file: `checkEdgesOnA_glue` takes the arithmetic as explicit
  hypotheses (`hlo : lo' = lo + n`, `hn : n' = n + m`, closed by `ring`), and
  `checkEdgesOnA_of_chunks` takes chunks as `∀ j < k, ∀ lo, lo = j * c → …` so `interval_cases j`
  + `simp only [Nat.reduceMul] at hlo; subst hlo` hands each chunk theorem over syntactically.
  Test assembly changes with `sorry`-stubbed chunk theorems first (probe in ~10 s) before the real build.
- The generator used to write the certificate blocks lives only in this session's scratchpad; the
  committed Lean file is the artifact. The JSON `word` field was the ground truth for pairing
  (`h23_w011` ↔ `[0,1,1]`, i.e. `h23Chans 0 1 1`), checked in Python before transcription.
- `DIRECTION.md`'s CURRENT DIRECTIVE is about the B6 measure route (a different campaign); the
  operator scoped this session to the kickoff nodes only, so nothing else was touched.

## ✅ State
- `src/NormalNumbers/HittingSetBounds.lean` (new, imported from `src/NormalNumbers.lean`) proves:
  `IsHittingSet.mono`, `hitting_4_1_three` (`S(4,1) ≤ 3`, `{1,10,14}`), `hitting_5_1_five`
  (`S(5,1) ≤ 5`, `{1,2,3,4,6}`), `hitting_2_2_two` (`{1,3}`), `hitting_2_3_four` (`{1,3,5,7}`),
  `not_hitting_5_1_four`, `not_hitting_5_1_five`.
- Observed: `lake build` green (8890 jobs), pre-commit build green, `#print axioms` on all seven =
  `[propext, Classical.choice, Quot.sound]`. No `native_decide`, no sorry, no leaves in this file.
- Committed `15945c8`; `box done --green` issued (host verifies and halts the run).

## 🎬 Next actions
1. Nothing remains in this kickoff. The open mathematics is the matching lower bounds
   `S(g,k) ≥ s` (frozen nodes N2/N4′ in `docs/hitting-set-invariant-2026-09-13.md`); a new kickoff
   from Trevor decides whether to attack them.
2. If the h23 compile time (~19 min) becomes a build-latency problem, split its eight words into
   per-word files the way `AdderTowerC8Chunk*.lean` does; the proofs need no change.

## ⚠️ Gotchas
- Never glue `checkEdgesOnA` chunks with implicit arithmetic (see Context); use the two lemmas in the file.
- `lake build` of the module needs ~10 GB RAM at peak; don't run two such builds concurrently on the 19 GB box.

## 📁 Key files
- `src/NormalNumbers/HittingSetBounds.lean` — the new theorems, certificates, gluing lemmas.
- `src/NormalNumbers/MahlerBase5Exact.lean` — the transcription template.
- `src/NormalNumbers/AdderEngineCoreG.lean` — `checkCertA`, `checkEdgesOnA`, `checkEdgesOnA_spec`.
- `experiments/adder_baseg_emit.py`, `experiments/certs/adder_cert_h*.json` — certificate source of truth.

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB - this doc IS the chosen thread. The kickoff
scope is complete; pick up at "Next actions" #1 only when a new kickoff names the lower bounds.**
