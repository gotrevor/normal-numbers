# HANDOFF 2026-09-14 — G4 disjunctivity, lap 12: **HEADLINE PROVED**

Branch `wip/g4-disjunctivity`, HEAD `b937072` (not pushed).  Build 🟢 `lake build` **8930 jobs**.

## What is now a theorem (kernel-checked, `[propext, Classical.choice, Quot.sound]`)

In `src/NormalNumbers/G4ScheduleAssembly.lean`:

* `NormalNumbers.G4.isDisjunctive_four : IsDisjunctive 4 primeLambertFour`
* `NormalNumbers.G4.isDisjunctive_two  : IsDisjunctive 2 primeLambertFour`
* `G4DisjunctiveFour_holds`, `G4DisjunctiveTwo_holds` — the named endpoint Props of
  `PrimeLambertFour.lean`.
* `every_binary_word_occurs (w) (∀ d ∈ w, d < 2) : ∃ n, OccursAt 2 primeLambertFour w n`,
  and the base-four twin `every_quaternary_word_occurs`.

The constant: `primeLambertFour = primeLambertAtBase 4 = ∑' n, ω(n)/4ⁿ` with
`ω = ArithmeticFunction.cardDistinctFactors`, summable (`summable_omegaR_div_pow`), and equal
to `∑_{p prime} 1/(4ᵖ − 1)` by `primeSumAtBase_four`.  `IsDisjunctive b x` is the frozen
predicate `∀ a c, 0 ≤ a → a < c → c ≤ 1 → ∃ n, Int.fract (x·bⁿ) ∈ [a, c)`.  Nothing in the
dependency cone touches `PrimeLambertOscillation`, any `sorry`, `native_decide`, or an axiom
(a `sorryAx`/`Lean.ofReduceBool` would print).

## How the last step went

`scheduleWitness ℓ w (hℓ : 1 ≤ ℓ) : ScheduleWitness ℓ w` is the lap-11 design verbatim:
`k₄ = 8464ℓ²16^ℓ`, `K = 4k₄`, `M = 4232ℓ16^ℓ` (so `2ℓM = k₄`, `8ℓM = K`), `G = gridOf K (N K)`,
`X = Sched.X K`, `η = (1/2)^{k₄}`, `ε = 1/K`, `D = Dj K k₄`, `lam' = e`, `lam = 13/2`,
`δ₁ = δbig = δfar = 1/8`, `Mx = ↑(X + J·Dm)`.  Two new side lemmas: `Sched.R_le_Y` and
`Sched.two_pow_mul_Dj_le : 2^K·Dj ≤ 4^{N−1}` (from `two_Dj_add_one_le`; feeds `hN` through
`Nat.clog_le_iff_le_pow`).  `hfar_holds` was proved against `η = 2^{−K}`; transported to
`2^{−k₄}` with `pow_le_pow_of_le_one` + `gcongr`.  `hbudget_holds` used `T K` for
`Fintype.card G.Idx`; `gridOf.card_Idx` is `rfl`-bridged.  `ℓ = 0` forces `w = 0`, and the
cylinder `[0,1)` is hit by `orbit_mem_Ico`.  Only two compile errors in the whole file, both
trivial (a `positivity` needing `show 0 < K^2`, and a `let`-hidden rewrite).

## Campaign summary (for the reviewer)

The candidate proof of brief §4–§5 is now a machine-checked theorem end to end:
A (`gridFrame_propA`), B (`gridFrame_propB_of_bound` + `gridParams_hB`), C
(`gridFrame_propC`), D (`gridFrame_propD_of_bounds` + `hbig_holds`, `hfar_holds`),
E (`Frame.propJackson`, `Frame.finite_contradiction`, `isDisjunctive_four_of_frames`),
§5 (`ScheduleWitness`, `scheduleWitness`).  Refuted along the way and recorded in
`PENDING_WORK.md`: the lap-9b `lam = 1` and `N ≈ 10K log K`, and the lap-11 crude bounds.

## What remains — nothing on this campaign

Follow-on queue (brief §7), NOT started: general base `b ≥ 3`; affine-entropy consequence;
ordinary-frequency transfer.  Reviewer task that would carry value: an independent
NL→formalization faithfulness cross-check of the headline *statement* (not the proof).

## Stop-gate note for the confirming lap (`box stuck`, strike 1 of 2)

`box done` was declined because the repo-wide gate counts **two** `sorry`s, neither on this
campaign and both forbidden by `DIRECTION.md` ("Forbidden drift" list):

* `exists_prime_nonresidue` — `src/NormalNumbers/MahlerDriftOne.lean:378` (`Mahler*` is
  off-limits; Linnik-strength; feeds only a conditional theorem).
* `phaseOscillation` — `src/NormalNumbers/PrimeLambertOscillation.lean:95` (explicitly
  excluded by both the kickoff and `DIRECTION.md`; the old irrationality endpoint).

Verify in under a minute: `grep -rn "^  sorry" src/NormalNumbers/*.lean` shows exactly those
two; `#print axioms NormalNumbers.G4.isDisjunctive_two` shows the trust triple.  The G4
objective is met.  The **operator ask**: either close the run as complete, or open a new
campaign from the brief §7 queue (base `b ≥ 3`; affine-entropy consequence;
ordinary-frequency transfer) with a fresh kickoff.  If you agree, `box stuck` again.
