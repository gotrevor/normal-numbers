# HANDOFF 2026-08-23 17:19 — W3 laps 1–2: analytic crux + substitution kernel DONE

**Branch/HEAD**: master @ `07fb78a` (clean tree, all committed, `lake build` green).
Read `HANDOFF.md` for campaign framing; **this doc supersedes its cylMap route plan**.

## Done this session (both axiom-clean, kernel-checked)

1. **`CFContraction.lean` (lap 1, sorry-free)** — the analytic crux of
   `cylinder_mixing` is SETTLED.  `stepOp_lipschitz`: the tail-parameter
   operator `(Pφ)(t) = Σ'_k stepWeight t k · φ(stepPt t k)`
   (`stepWeight t k = (1+t)/((k+1+t)(k+2+t))`, `stepPt t k = 1/(k+1+t)`,
   index k = digit−1) maps `L`-Lipschitz-on-`[0,1]` to `(9/10)L`-Lipschitz.
   So `Pᵏ` contracts at `(9/10)ᵏ` — geometric GKL rate, NO
   coupling/Wasserstein/Birkhoff.  Proof: center `ψ = φ − φ(0)`, split
   `ΣA + ΣB`, Abel-resum B via `gTail`, one parametric telescope
   `hasSum_inv_triple`; budget `61/72 ≤ 9/10`.  Numerics validated
   (true factor ≈ 0.41; spectral rate = Wirsing 0.3036).
2. **`CFDensity.lean` (lap 2, 2 disclosed sorries)** — step-1 route via
   reverse word-induction, ONE 1-D change of variables per appended digit
   (`MeasureTheory.integral_image_eq_integral_abs_deriv_smul`; no
   composite Jacobians, no cylMap — ⚠️ `cylMap` has a junk defect at
   length-1 words: `cfP [] = 1` should morally be 0; frozen statements
   don't mention it, just avoid it).  PROVED: `branchMap` conjugacy
   (`gaussMap_branchMap`, `branchMap_gaussMap`, irrationality transport),
   `ae_eq_of_irrational_iff` (sets agreeing off ℚ are =ᵐ),
   `branch_inter_ae` (`I_[b] ∩ T⁻¹A =ᵐ br_b '' A`), `concat_inter_ae`
   (`I_{w++[b]} ∩ T^{-(n+1)}A =ᵐ I_w ∩ T^{-n}(I_[b] ∩ T⁻¹A)`),
   `tParam_concat` (`t(w++[b]) = 1/(b + t w)`, `tParam [] = 0`), and the
   **kernel** `setIntegral_tailDensity_branch`:
   `∫_{br_b''A} h_t = (1+t)/((b+t)(b+1+t)) · ∫_A h_{1/(b+t)}` — weights
   exactly match `stepWeight`.
   W1/W2 privates lifted public (cfDigit_zero/succ/zero_eq_iff,
   gaussMap_eq_inv_sub, mem_cfCylinder_cons, cfK_concat, measurable_*,
   irrational_orbit, one_le_cfDigit, cfCylinder_append_subset).

## Open sorries (2 in `CFDensity.lean`, 4 frozen in `CFMixing.lean`)

Next, in order:
1. `volume_cfCylinder_concat` — `|I_{w++[b]}| = ofReal((1+t)/((b+t)(b+1+t)))·|I_w|`,
   `t = tParam w`.  Pure algebra from W1 `volume_cfCylinder` + `cfK_concat`
   (+ `cfCylinder_nil`/`volume` = 1 for `w = []`; `volume_digit_cylinder`
   covers `|I_[b]|`).  Cross-multiply with `q⁻ ≤ q`, `1 ≤ q`.
2. `volume_inter_preimage_aux` — reverse induction (`List.reverseRecOn`).
   Base `[]`: `cfCylinder_nil`, `T^[0]` preimage = A, `h_0 = 1`,
   `ofReal(∫_A 1) = volume A` (A ⊆ Ioo measurable ⇒ finite).
   Step: `measure_congr (concat_inter_ae ...)` → IH at
   `A' := I_[b] ∩ T⁻¹A` (measurable ✓, ⊆ Ioo ✓) →
   `setIntegral_congr_set (branch_inter_ae ...)` →
   `setIntegral_tailDensity_branch` → `tParam_concat` + lemma 1 +
   ENNReal.ofReal_mul (weight ≥ 0) to reassemble.
   Then frozen `volume_inter_preimage_eq_integral` = AUX + `if_neg`.
3. `G_k` recursion for `cylinder_mixing`: `G_k(t) := ∫_{B_k} h_t`,
   `B_k := Ioo ∩ T^{-k}A`.  `I_w ∩ T^{-(n+k)}A =ᵐ I_w ∩ T^{-n}B_k`
   (irrational orbit) ⇒ by AUX conditional vol = `ofReal(G_k(t w))·|I_w|`.
   And `G_{k+1} = stepOp G_k` on `[0,1]`: partition `B_{k+1}` over first
   digit (`Ioo =ᵐ ⋃_b I_[b]`, disjoint, `integral_iUnion` — h_t bounded
   ≤ 2 on Ioo so integrable), each piece = `branch_inter_ae` +
   `setIntegral_tailDensity_branch` at `A := B_k`.  Mind index shift
   digit b = k+1 ↔ `stepWeight t k`.
4. Anchor: `∫₀¹ h_s(y)/((1+s)log 2) ds = g(y)` (mixture identity,
   `∫₀¹ ds/(1+sy)² = 1/(1+y)`) + Fubini ⇒ `∫ G_k dν = γ(B_k)`; and
   `measurePreserving_gaussMap` (frozen #1; branch telescope — reuse
   `branch_inter_ae` with S ⊆ Ioo + lintegral version
   `lintegral_image_eq_lintegral_abs_deriv_mul`, sum over b, MCT) gives
   `γ(B_k) = γ(A)`.
5. Assemble `cylinder_mixing`: `Lip(G_0) ≤ 2·vol A ≤ (4 log 2)γ(A)`
   (from `|∂_t h_t(y)| ≤ 2`), `stepOp_lipschitz` iterated ⇒
   `|G_k(t) − γ(A)| ≤ (4 log 2)γ(A)(9/10)ᵏ`; ENNReal plumbing to the
   frozen two-sided form (C = 4 log 2, ρ = 9/10).  `gauss_kuzmin` =
   same with `G_k(0)` (`h_0 = 1`, `vol B_k = G_k(0)`).

## Gotchas (fresh this session)
- `set`-bound lambdas (Δ, ψ, A, B, u, v, T) caused whnf timeouts in the
  big proof → `set_option maxHeartbeats 1600000 in` on `stepOp_lipschitz`.
- `=ᵐ[volume]` between set-expressions containing `∩`: ascribe
  `( ... : Set ℝ)` on BOTH sides or `∩` elaborates at `ℝ → Prop`.
- `div_le_div_iff₀`, `div_le_div_of_nonneg_left (ha) (hc) (h : c ≤ b)`,
  `abs_add_le`, `Int.fract_add_natCast`, `Irrational.natCast_add`,
  `List.getD_append_right l l' d n h`, `tendsto_natCast_atTop_atTop`
  (root ns), `Finset.sum_range_sub'` — names in this pin.
- `field_simp` sometimes closes goals fully (then `ring` errors "no
  goals") or throws nested errors on `set`-aliased vars — prefer
  explicit `div_sub_div`/`congr 1 <;> ring` when it misbehaves.
- Telescoping tsums: partial sums via `Finset.sum_range_sub'` +
  `hasSum_iff_tendsto_nat_of_nonneg`; cast mismatches (`↑(0:ℕ)` vs `0`)
  need calc-with-`norm_num` endpoints, not bare `rw [← this]`.

**Gates**: statements in `CFMixing.lean` FROZEN (anchors must keep
passing); `#print axioms` target = `propext, Classical.choice, Quot.sound`.
