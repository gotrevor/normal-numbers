# Handoff: Mahler chapter two-sided (gᵏ−1 ≤ M ≤ g^(k+1)); Furstenberg wired — next: remaining cited-only ledger nodes

**Date**: 2026-09-01-2339 UTC · **Branch**: `wip/adder-tower-c9` · **HEAD**: `5f2e7c4` (working tree clean after this commit)

## 🎯 What we're doing

Autonomous overnight grind on `normal-numbers`.  Operator constraints: do NOT
touch `CFScheduleA.lean` (its two sorries are being converted to `def … : Prop`
nodes on `wip/cfschedulea-prop-nodes`), do NOT chase the repo-wide sorry-free
gate, do NOT touch Comparator statement holes.  Grind genuinely provable leaves;
new results in new files; report the crux advanced.

Three laps landed this session:
1. **Mahler's multiplier bound sharpened from `(g+3)·gᵏ` to `g^(k+1)`** — commit
   `136bef4`.  Closes the `g = 2` gap and halves the transcribed Berend–Boshernitzan
   constant for every base.
2. **Furstenberg 1967 dense-orbit theorem wired** — commit `ff266fa`.
3. **Mahler LOWER bound `gᵏ − 1` proved** (`MahlerLowerBound.lean`, commits
   `46fc958` + `5f2e7c4`): witnesses `liouvilleNumber g`, block `(g−1)ᵏ`; no
   `1 ≤ m ≤ gᵏ − 2` shows the block past position `(k+2)!`.  The chapter is now
   two-sided and machine-checked: `gᵏ − 1 ≤ M(g,k) ≤ g^(k+1)`.  Details + Lean
   gotchas in `PENDING_WORK.md` (top entry).

## 🧠 Context to carry forward

**Mahler (`src/NormalNumbers/MahlerMultiplier.lean`, docstring = full sketch).**
- `sweep_pos`/`sweep_neg` now take `M` directly: hypothesis
  `1 − q/gᵏ ≤ (M+1−2q)·η`, conclusion `∃ m ≤ M` in the cell.  New
  `start_in_cell`: if the progression through the grid point `j/q` below the
  cell needs `> M` multiples, the next grid point `(j+1)/q` is inside the cell
  and so is its progression's start.
- `defect_bound_of_bad` is the **universal** covering lemma (every reduced
  `p/q`, `q ≤ gᵏ`, `|qx−p| < g⁻ᵏ`), not just Dirichlet's.  This is what lets
  the escape lemma follow the **shadow rational** `ρ' = gρ − ⌊g x_n⌋` instead of
  proving two rationals coincide: `x_{n+1} − ρ' = g(x_n − ρ)` exactly, and
  `defect_contracts_of_bad` (at `M = g^(k+1)`) keeps the shadow quality-`g⁻ᵏ`.
- The method's floor is `M ≥ 2gᵏ`; `g^(k+1) − 1` also works verbatim.  B–B's lower
  bound `gᵏ − 1` bounds the remaining room by a factor `g`.
- ⚠️ **Claim hygiene**: B–B's `2g^(k+1)` is tier-S (secondary sources).  The paper is
  D. Berend, M. Boshernitzan, *On a result of Mahler on the decimal expansion of
  (nα)*, Acta Arith. 66 (1994) 315–322 (title confirmed by WebSearch; abstract not
  reachable).  `ON-LINE-REQUEST.md` (new file) asks the host for the PDF / exact
  statements.  Don't headline "half of B–B" until answered.
- Numeric probe of the refined sweep: 5149 samples, 0 violations, min slack 1
  (`scratchpad/probe_sweep.py`, session scratch only; 25 lines to re-derive).

**Furstenberg (`src/NormalNumbers/Furstenberg.lean` + `LiteratureFurstenberg.lean`).**
- The 1200-line rigidity proof is a verbatim re-homing of the author's
  `~/src/collatz-moonshot/CollatzMoonshot/Rigidity/Furstenberg.lean` (commit
  `4727694`, identical mathlib `0df444a3` / Lean `v4.33.1`); it imports `Mathlib`
  wholesale (6 other src files already do).  Route: Boshernitzan 1994 via Manners
  arXiv:1305.1514 §4.  The corpus note
  `reference/2026-08-26-lean-addcircle-rigidity-toolkit.md` is the API map — read
  it before touching `UnitAddCircle`.
- Bridge (`furstenberg_dense_orbit_holds`): irrational ⇒ non-torsion
  (`AddCircle.not_isOfFinAddOrder_iff_forall_rat_ne_div`, note the `x / 1`);
  image of `Ioo a c` is open (`QuotientAddGroup.isOpenMap_coe`); `Dense.exists_mem_open`;
  `AddCircle.coe_eq_coe_iff_of_mem_Ico (p := 1) (a := 0)` with an explicit
  `(0:ℝ) + 1 = 1` rewrite; the file's `coe_fract`.  Membership hypotheses from
  `Set.image` arrive beta-unreduced (`(fun t ↦ ↑t) t = …`): restate with `have`.

**Aristotle**: nothing in flight; nothing genuinely open to send.

## ✅ State

- Full `lake build` green: **8848 jobs** (pre-commit hook re-ran it at `5f2e7c4`).
- `#print axioms` (observed): `mahler_lower_bound`, `orbit_liouville_lt`,
  `mahler_multiplier`, `orbit_escapes`,
  `defect_bound_of_bad`, `defect_contracts_of_bad`, `sweep_pos`, `start_in_cell`,
  `mahler_theoremM_holds`, `berendBoshernitzan_bound_holds`,
  `furstenberg_dense_orbit_holds`, `Furstenberg.isClosed_invariant_finite_or_univ`
  — all `[propext, Classical.choice, Quot.sound]`.
- Ledger: `mahler_theoremM` WIRED (all `g`), `berendBoshernitzan_bound` WIRED (all
  `g ≥ 2`), `furstenberg_dense_orbit` WIRED.
- `src/` sorries: only the two OFF-LIMITS `CFScheduleA` residues.
- Docs current: `PENDING_WORK.md` (top two entries), `BRIEF-literature-statements.md`
  (rows 10, 11, 15), `Literature.lean` docstrings, `ON-LINE-REQUEST.md`.

## 🎬 Next actions

1. Inventory the remaining **cited-only** ledger nodes in
   `BRIEF-literature-statements.md` / `Literature.lean` (grep `unproved def`) and
   pick the next genuinely provable one (not `waldschmidt_conjecture_1_1`, which is
   open).  Candidates I did not get to: B–B's `gᵏ − 1` lower bound (needs the
   primary source first — quantifiers unpinned), and any `philipp_psi_mixing` /
   Fisher–Schmidt-style nodes (heavy; check the brief's difficulty notes).
2. If the host answers `ON-LINE-REQUEST.md`: reconcile the B–B constant, then
   either headline the improvement or retract the "half" phrasing; transcribe the
   lower bound into `Literature.lean`.
3. The factor `g` between `gᵏ − 1` and `g^(k+1)` is now THE open question of the
   chapter (novel-math candidate).  Upper side: the binding case of the shadow
   chain is `q = 1` (`x_n` near an integer), where the sweep hypothesis
   `|η| < g⁻ᵏ` fails for the shadow once `g|η| ≥ g⁻ᵏ`; a denominator-aware
   invariant or a second Dirichlet rational at that step might give `≈ (g−1)gᵏ`.
   Lower side: is `gᵏ − 1` sharp, i.e. does `m = gᵏ − 1` always succeed for the
   Liouville witness (yes: its digit string is `(g−1)ᵏ`), and can a cleverer `α`
   defeat `m` up to `c·gᵏ` with `c > 1`?  A numeric search over sparse `α` is the
   cheap first probe.
4. Commit each green build; end the turn after each commit (governor checkpoint).

## ⚠️ Gotchas

- **Never `cd` inside a Bash call** (persisted cwd broke `lake env lean` last
  session).  Absolute paths from the repo root.
- `maxHeartbeats` is **per declaration**: a long tactic proof shows "timeout" at
  three unrelated lines at once.  Split the proof (that is how `start_in_cell` was
  born), don't raise the limit.
- `le_or_lt` is gone on this pin — use `le_or_gt`.  `push_neg` is deprecated —
  `push Not`.  `N₀ + (i + 1)` vs `N₀ + i + 1` in `induction … | succ`: state the
  step lemmas with `N₀ + (i + 1)`, or `rw [show N₀ + (i+1) = N₀ + i + 1 from rfl]`.
- `Rat.sub_den_dvd`, `Rat.mul_den_dvd`, `Rat.den_intCast`, `Rat.den_natCast` are
  the denominator-tracking lemmas for the shadow rational.
- `WebFetch` is firewalled; `WebSearch` works but returns summaries only.

## 📁 Key files

- `src/NormalNumbers/MahlerMultiplier.lean` — theorem + lemmas, docstring = sketch.
- `src/NormalNumbers/LiteratureMahler.lean` — ledger edges (Thm M, B–B all `g`).
- `src/NormalNumbers/MahlerLowerBound.lean` — the `gᵏ − 1` lower bound.
- `src/NormalNumbers/Furstenberg.lean` — ×p×q rigidity (re-homed).
- `src/NormalNumbers/LiteratureFurstenberg.lean` — the `Int.fract` bridge.
- `src/NormalNumbers/Literature.lean` — ledger defs (docstrings mark WIRED).
- `ON-LINE-REQUEST.md` — B–B primary-source request (host to answer).
- `PENDING_WORK.md`, `BRIEF-literature-statements.md` — RESULT records.

---
**→ Next session: this is your starting point.  Don't summarize this doc back to
Trevor or wait for instructions.  Absorb the context above, then pick up at
"Next actions" #1: grep the ledger for remaining `unproved def` rows and attack
the next provable one.**
