# HANDOFF 2026-09-14 — G4B lap 14: **base-`b` theorem PROVED** for every `b ≥ 3`

Branch `wip/g4-disjunctivity`, HEAD = the commit after `20c43d8` (not pushed).  Build 🟢
`lake build` **8930 jobs**.  `DIRECTION.md` CURRENT DIRECTIVE (campaign G4B) is **met**; it is
owned by altitude laps and was not edited.

## 1. What was proved (declaration names)

* `NormalNumbers.G4.isDisjunctive_base {b} (hb : 3 ≤ b) : IsDisjunctive b (primeLambertAtBase b)`
  — `G4SchedBAssembly.lean`.  `#print axioms = [propext, Classical.choice, Quot.sound]`.
* `isDisjunctive_primeSum` (`∑_p 1/(bᵖ−1)` form via `primeSumAtBase_eq_primeLambertAtBase`),
  `every_word_occurs_base`, `isDisjunctive_root` (`cᵏ ≥ 3` ⟹ disjunctive in base `c`),
  `isDisjunctive_four'` (the `b = 4` instance).  `isDisjunctive_four`, `isDisjunctive_two`
  are untouched and still axiom-clean.

Route, bottom-up, each step a green commit:

| commit | what |
|---|---|
| `97179b1` | the concrete §4 layer in any base `bb ≥ 2`: `coeffAL bb`, `Sval bb`, `blockSum bb G`, `farPart bb G`, `rowCoeff bb G`, `gridFrame bb hbb G …`; masses `rowL1 b K = (2/b)^K/(b−1)`, `rowL2 b K = (2/b²)^K/(b²−1)`, `farBound b J C = b^{−J}((C+2J+2)/(b−1) + 2/(b−1)²)`; `gridFrame_propA/B/C/D` in `bb` (PropC with the seed as hypothesis `hsep`) |
| `56e4808` | `G4FreqSep` in any base: `freqDepthB`, `freqSeed b K = b^{−4}(2/b²)^K`, `sum_sq_distZ_freqDepthB_ge`, `sum_sq_distZ_coeff_ge_gen`, `gridFrame_propC_gen` |
| `3aef25c` | `ScheduleWitnessB bb ℓ w`, `separatingFrameExists_of_witnessB`, `isDisjunctive_of_witnessB` |
| `700a4b6` | `SchedB` parameters: `m₁ b K = 1000·b^{2K+4}·K^{2K+1}`, the ladder under `Hyp b K := (3 ≤ b) ∧ (2b² ≤ K) ∧ (100 ≤ K)`, all `X`-dependent size lemmas |
| `c9f446f` | `hbudget` in base `b`: main term with `θ₀·m₁ = 1000·2^K·Kr`; terms (a)–(d) unchanged |
| `20c43d8` | `hM`, `hB` (via `gridB_bound`, `⌈log₂ b⌉ ≤ b`), `hN` (`2^K D ≤ 2^{N−1}`, `clog_anti_left`), `hbig`, `hfar` at the worst case `b = 3`, the witness `scheduleWitnessB`, and `isDisjunctive_base` |

The schedule in `(b, ℓ)`: `k₄ = 8464·ℓ²·b^{2ℓ+2}`, `K = 4k₄`, `M = ⌈K log 2/(4ℓ log b)⌉₊`,
`η = 2^{−k₄}`, `ε = 1/K`, `D = Dj K k₄`, `R/Y/X/Mc = SchedB.R/Y/X/Mc b K`, `δ's = 1/8`.

Two places where the port needed a real idea rather than a rename:
* the seed shrinks with `b` (`(2/b²)^K`), so `m₁` had to grow as `b^{2K+4}` — the ladder
  survives exactly because `2b² ≤ K` gives `b^{2K+4} ≤ (K/2)^{K+2}`;
* the masses grow as `b` shrinks, so `hbig`/`hfar` were settled at `b = 3`, using
  `(2/3)⁴ < 1/4` and `(2/9)⁴ < 1/256` against `η = 2^{−K/4}`.  The base-four numeric slack
  (`(1/2)^K = η⁴`) is not needed.

## 2. Refuted / not attempted

* Base two: still refuted on this route — `rowL1 2 K = 1` (now a named constant), so the
  pointwise very-large-prime bound cannot close.  Stretch target only (`PENDING_WORK.md`).
* No new axioms, no `sorry` in any G4/G4B file.

## 3. Housekeeping the next lap may do (not mathematics)

`SchedB` is a parallel schedule; the base-four `G4Schedule{Far,Big,Harmonic,Budget,Assembly}`
are now redundant with `isDisjunctive_four'` but kept (nothing deleted).  A cleanup lap could
retire them or turn `Sched` into the `b = 4` instance; `DIRECTION.md`'s rule "instance, not
sibling" is satisfied at the level of the theorem.

## 4. Untouched, off this campaign (unchanged)

`MahlerDriftOne.exists_prime_nonresidue` and `PrimeLambertOscillation.phaseOscillation`
(both on `DIRECTION.md`'s forbidden-drift list).
