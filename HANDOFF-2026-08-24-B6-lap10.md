# HANDOFF 2026-08-24 — B6 campaign (affine images): metric substrate + toolkit DONE, crux isolated

**Branch/HEAD**: master @ `2a1c2e2`, `lake build` green (8755 jobs).
No uncommitted Lean edits (only untracked `CLAUDE.local.md`, expected).
Supersedes `HANDOFF-2026-08-27-1800.md` (B5′ is COMPLETE; this run pursued the
attended B6 directive in `DIRECTION.md`).

## Directive (unchanged, obey it)
`DIRECTION.md` CURRENT DIRECTIVE = **B6 CAMPAIGN** (Vandehey §7 affine images).
ADDITIVE ONLY — never edit frozen B5′ modules (`TBrick*`, `CFSchedule`,
`CFLogTail`, `Headline`, `KhinchinDefs`, …). Both B5′ headlines must stay
character-identical + trust-triple every lap. Full detail: `PENDING_WORK.md`
(B6 section at top, laps 1–10).

## What this run proved (all axiom-clean `[propext, Classical.choice, Quot.sound]`)

Three NEW additive modules, target reduced to ONE disclosed crux `sorry`:

**`CFIntervalGood.lean`** (imports `CFDigitLaw` + `CFSchedule`):
- `volume_cfCylinder_le_fib` — rank-n cylinder length ≤ 1/fib(n+1)².
- **L1** `volume_interval_sdiff_covered_le` — rank-n cylinders inside (a,b)⊆(0,1)
  cover it up to 4/fib(n+1)² (soft M-neighborhood proof, no straddler count).
- **L2** `length_le_two_mul_good_add_err` — `|b−a| ≤ 2·vol(goodInInterval a b n m)
  + 4/fib(n+1)²` (composes L1 with `goodC_half` via measure_biUnion tsums).
- `goodInInterval_pos_of_lt` — good mass inside a nondegenerate interval is
  STRICTLY positive beyond a rank (per-stage feasibility).
- `take_eq_of_mem_cfCylinder` — cylinder nesting ⇒ word prefix.
- `eq_of_mem_cfCylinder_chain` — nested extending chains pin a unique point.

**`CFAffine.lean`** (imports `CFIntervalGood`): the affine map ψ(x)=q·x+r (q>0):
`preimage_affineMap_Ioo`, `image_affineMap_Ioo`, `volume_preimage_affineMap_Ioo`,
`volume_preimage_affineMap` (general |q⁻¹| scaling), `good_mass_in_affine_preimage`
(transports L2 through the pullback).

**`CFOrbitFreq.lean`** (imports `CFWordBridge` + `Headline`):
`isCFNormal_of_orbit_freq` and `isCFNormal_of_irrational_orbit_freq` — reduce
`IsCFNormal y` to a Gauss-ORBIT frequency criterion (x-generic; via existing
`blockCount_sub_countOccurrences_bounds` + squeeze).

**`CFScheduleA.lean`** (imports `CFAffine` + `CFOrbitFreq`):
- `CFOrbitEquidist y` predicate.
- **`exists_cfNormal_and_affine_cfNormal {q}(hq:0<q)(r) : ∃ x, IsCFNormal x ∧
  IsCFNormal (affineMap q r x)`** — the B6 single-map target, **PROVED modulo one
  crux** (assembly is real, uses the orbit-freq interface).
- **THE ONE CRUX** (`exists_interleaved_affine_witness`, disclosed `sorry`,
  CFScheduleA:56/61): `∃ x` with both `x` and `ψ(x)` irrational in (0,1) with
  equidistributing orbits (`CFOrbitEquidist`).

`src/` carries exactly ONE active `sorry` — this isolated crux (decomposition,
NOT regression). All B6 substrate below it is axiom-clean.

## Route-decisive finding (recorded in PENDING_WORK)
`IsCFNormal(ψ x)` is a REAL theorem, not mechanical threading: `T` (Gauss map)
does NOT commute with `ψ`, so B5′'s "prescribe x's digits" trick doesn't transfer.
L1–L3 are necessary but not sufficient. The route is an **interleaved (diagonal)
schedule**: alternate x-stages (refine to a good x-cylinder) and ψ-stages (refine
so ψ(x) enters a prescribed good ψ-cylinder — feasible by
`good_mass_in_affine_preimage`/`goodInInterval_pos_of_lt`). No shortcut: the
pointwise ergodic theorem for the Gauss map is NOT in the mathlib pin (checked),
which is exactly why B5′ built the explicit route.

## NEXT STEPS (discharge `exists_interleaved_affine_witness`)
The metric + limit TOOLKIT is complete (see PENDING_WORK "TOOLKIT NOW COMPLETE").
What remains is PURELY schedule bookkeeping — no new analytic content. Concretely:
1. **Obligation (A)** `ψ(x)` irrational in (0,1): nearly mechanical — combine
   `eq_of_mem_cfCylinder_chain` (uniqueness) with `exists_irrational_mem_iInter_
   cfCylinder` (`CFLimit`) applied to the ψ-word chain, given the containments
   `cfCylinder(wx N) ⊆ ψ⁻¹(cfCylinder(wz s))` from the construction.
2. **Obligation (B)** orbit equidistribution: build the light interleaved
   `SchedState` (word `wx` + ψ-word `wz` + nonempty-combined-interval invariant),
   append good CF-blocks alternately (feasibility via `goodInInterval_pos_of_lt`
   + `take_eq_of_mem_cfCylinder` to see good cylinders as extensions), then mirror
   `CFCorrect`'s frequency telescoping (`xstar_cf_freq_tendsto`) for BOTH streams.
   **Tier-1 needs only CF-normality — do NOT drag in TBrick's daryCell/khinchin.**
   Consider a fresh light schedule, not extending TBrick.
3. Escape valve (judge): Tier-1 φ headline `x, φx, x+φ` = 2-element family;
   general family the stretch. After ANY schedule work re-`#print axioms
   exists_absolutely_normal_cf_normal_khinchin` (must stay trust-triple).

## Faithfulness
All 10+ new lemmas re-verified `#print axioms` = trust triple this run. B5′
headlines untouched (new modules are downstream leaves; `NormalNumbers.lean`
only gained import lines).
