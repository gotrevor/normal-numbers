# HANDOFF 2026-08-23 20:40 — W3 COMPLETE: all 4 CFMixing sorries proved

**Branch/HEAD**: master, clean, `lake build` green, `src/` SORRY-FREE.
Supersedes HANDOFF-2026-08-23-1719.md.  W3 (the core of expedition B5′)
is DONE — every frozen statement in `CFMixing.lean` is a kernel-checked
theorem with `#print axioms = propext, Classical.choice, Quot.sound`:

- `measurePreserving_gaussMap` (Track B flag B1)
- `volume_inter_preimage_eq_integral` (route step 1)
- `cylinder_mixing` — C = 8 log 2, ρ = 9/10 (geometric envelope, no
  escape valve needed)
- `gauss_kuzmin` (Track B flag B4)

## Module map (laps 3–8, all new/extended, all axiom-clean)
- `CFDensity.lean` — conditional density identity by reverse word
  induction (`volume_inter_preimage_aux`); single-branch substitution
  kernel; `tParam` calculus.
- `CFRecursion.lean` — `horizonSet`/`horizonIntegral` (B_k, G_k);
  `volume_inter_preimage_horizon` (|I_w ∩ T^{-(|w|+k)}A| = G_k(t_w)|I_w|);
  `horizonIntegral_succ` (G_{k+1} = stepOp G_k, first-digit partition);
  `tailDensity_lipschitz` (2-Lipschitz in t, explicit factorization);
  `horizonIntegral_lipschitz` (Lip G_k ≤ (9/10)^k·2|A|).
- `CFInvariance.lean` — `gaussMeasure_preimage` (γ(T⁻¹S) = γ(S)) via
  branch telescope Σ 1/((b+y)(b+1+y)) = 1/(1+y) (= hasSum_stepWeight).
- `CFPin.lean` — mixture Fubini (∫₀¹ h_s(y) dλ(s) = Gauss density,
  λ = ds/((1+s)log 2)); `integral_horizonIntegral_eq_gauss`
  (∫ G_k dλ = γ(A)); `abs_horizonIntegral_sub_gauss`
  (|G_k(t) − γ(A)| ≤ (9/10)^k·4|A|); `horizonIntegral_envelope`.
- `CFMixing.lean` — assembly only; statements NEVER touched (frozen).

## Next: W4 (the only remaining work package)
KHINCHIN.md "W4": LD/Chebyshev assembly + b-ary side (Lemmas 8, 9 of
Becher–Yuhjtman; overlaps existing Counting/Visits modules).  Consumes
`cylinder_mixing` as the sole correlation input.  Read KHINCHIN.md
§"W4" + ROADMAP.md Phase 2 before scaffolding; freeze W4 statements
first (same discipline), get judge sign-off if JUDGE.md requires.

## Fresh gotchas (this session)
- This mathlib pin: `mul_le_mul_right'`/`le_or_lt`/`zero_le _` GONE →
  use `gcongr` / `le_or_gt` / `bot_le`.  `inv_le_inv₀` for inv
  monotonicity.  `Ioo_ae_eq_Ioc` is in MeasureTheory root namespace.
- `rw [defName]` on a plain def sometimes fails ("equation theorems")
  esp. under `convert`; use `simp only [defName]` or an explicit
  `have heq : … := by field_simp; rw [heq] at h`.
- `ℝ≥0∞` notation needs `open scoped ENNReal`; plain `ENNReal` works.
- Fubini recipe that worked: `Measure.prod_restrict` + `ae_restrict_mem`
  for the a.e. bound, `Integrable.mono'` vs a constant (needs explicit
  `have : IsFiniteMeasure (volume.restrict _)` instances),
  `integral_integral_swap`.
- FTC on Ioo: `setIntegral_congr_set Ioo_ae_eq_Ioc` →
  `intervalIntegral.integral_of_le` →
  `integral_eq_sub_of_hasDerivAt` with `huIcc : uIcc 0 1 = Icc 0 1`
  rewritten via `huIcc ▸ hs` / `huIcc.subset`.

**Gates**: `#print axioms` target unchanged.  Commit every green build.
