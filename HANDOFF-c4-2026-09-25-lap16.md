# HANDOFF C4 — 2026-09-25, laps 15–16

Branch `wip/c4-infinite`, HEAD `c5c4a94`, working tree CLEAN.  `lake build` green (9264 jobs).
Objective `KICKOFF-2026-09-24-c4.md`.  **Read `DIRECTION.md` → CURRENT DIRECTIVE first** — lap 15
was a review lap and reset it to C4 with the route below; it outranks this baton.

## The crux
`c4_realizable_of_mem_one` (`src/NormalNumbers/AbelianWindowSets.lean:506`), the one open `sorry`
in the C4 chain.  `#print axioms` (re-run lap 15): `c4_realizable` and
`c4_realizable_of_mem_one` = trust triple + `sorryAx`; `abelianAt_one_of_abelianAt`,
`c4_realizable_singleton_one`, `c4_realizable_odd`, `c4_realizable_compl_singleton` = trust
triple, clean.

## Lap 15 (review): the route changed — why, and to what

**The ceiling that forced it.**  For a `blockSeq` of period `q`, the window law at length `L`
depends on `L` only through `L mod q` once `L ≥ q` (the trace on each block is a fixed
suffix/prefix, the full-block factor a fixed power of the block gf).  So a single block length
has an **eventually `q`-periodic** exact abelian set — `c4_realizable_odd` (`q = 2`) is that
ceiling being hit.  C4 asks for arbitrary `S ∋ 1` (e.g. `{1} ∪ {2^k}`, neither `S` nor `Sᶜ`
eventually periodic), so lap 14's plan ("finite complement by superposing coordinate groups")
cannot reach it: **infinitely many block scales in ONE sequence are required.**

**The nested-layer gadget route** (four items, `PENDING_WORK.md` has the full version):
1. *(DONE lap 15)* `AbelianWindowPerturb.lean` — the limit transfer.
2. *(2a DONE lap 16, 2b OPEN)* the gadget at width `q`: removal + the single-gadget closed form.
3. the multi-gadget block law + interval geometry ⇒ finite-complement `S`.
4. the layer layout + assembly ⇒ `c4_realizable_of_mem_one`.

Architecture: layer `m` places the `a_m`-gadget at every position `≡ o_m (mod q_m)`, periods
nested (`q_1 | q_2 | …`), `o_m + a_m + 1 < q_m`, quadruples pairwise disjoint.  Then
`s⁽ⁿ⁾ :=` (layers `1..n` applied to a binary normal `x`) **is** `blockSeq g_n (blockOf 2 q_n x) q_n`
— `isNormalSequence_pow` gives normality of the base-`2^{q_n}` digits — so the existing engine
computes its law exactly.  Layer `m`'s defect has density exactly `1/q_m` in EVERY `s⁽ⁿ⁾` with
`n ≥ m`, so **the stage laws agree on decided lengths** (this is what removes the uniformity /
double-limit problem that blocked lap 14's plan), and `s := lim s⁽ⁿ⁾` differs from `s⁽ⁿ⁾` only on
layers `> n`, of density `≤ 8∑_{m>n} 1/q_m → 0`.

## PROVED lap 15 — `src/NormalNumbers/AbelianWindowPerturb.lean` (axiom-clean)
* `card_diffWin_le` — one changed digit spoils at most `L` windows of length `L`;
* `abs_onesFreq_sub_le` — `|onesFreq s L j N − onesFreq t L j N| ≤ L·diffCount s t (N+L)/N`;
* `tendsto_onesFreq_of_approx` — the 3ε transfer.  The transferred value is ARBITRARY, so this
  one lemma both proves abelianness at `L ∉ D` and refutes it at `L ∈ D`;
* `tendsto_onesFreq_of_linear_diff` — the packaged form the construction will call
  (`diffCount s (T n) M ≤ c n + η n · M`, `η n → 0`).

## PROVED lap 16 — `src/NormalNumbers/AbelianWindowGad.lean` (axiom-clean)
* `gadTrig q p a d`, `gadBit q p a r d` — `rectG` read as a 4-bit gadget on
  `quadSet p a = {p, p+1, p+a, p+a+1}`, placeable anywhere in a width-`q` block;
  `multiG q gs` for a list of gadgets, with `multiG_of_notMem`, `multiG_congr_off`.
* Bit surgery: `swapBits`, `bitw_ext`, `bitw_swapBits`, `bitw_of_ge`; `gadSwap` (exchange the low
  pair AND the high pair), `gadTrig_gadSwap` (**the swap preserves the trigger**),
  `gadSwap_involutive`, `gadPhi` (swap on triggers, identity off them; an involution of
  `range (2^q)`), `bitw_gadPhi_off`.
* **The removal lemmas** — `blockGf_cons_of_high` (trace holds both-or-neither of the HIGH pair
  ⇒ the gadget changes no one-count pointwise) and `blockGf_cons_of_low` (both-or-neither of the
  LOW pair ⇒ the gadget drops out of `∑_d X^{ones_I}` by re-indexing along `gadPhi`).  Neither
  needs polynomial algebra.  Supporting: `onesTr_eq_sum`, `sum_quadSet`, `sum_split_quad`,
  `pair_swap_eq`, `blockGf`.

## Next actions, in order
1. **Item 2b — the single-gadget closed form at width `q`:**
   `blockGf q (multiG q [(p,a)]) I = ∏_{i<q} w_i(I) + (∏_{i ∉ quadSet p a} w_i(I))·corr(I)`,
   `w_i(I) = if i ∈ I then 1+X else 2`,
   `corr(I) = (X^{[p+a∈I]} − X^{[p+a+1∈I]})·(X^{[p∈I]} − X^{[p+1∈I]})`.
   Two routes: port the `rfac` / `rect_sum_forced` / `rect_key` chain of `AbelianWindowRect.lean`
   (re-index `a+2 → q`, `spos → quadSet p a` — `sum_prod_bits` is already width-generic), or the
   cheaper split found lap 16: on the fully-separated trace, split the digit sum over the trigger
   set by `ε = bit p`, giving `(1−X)X·F + (X−1)·F = −(X−1)²·F` with
   `F = ∏_{i ∉ quadSet} w_i(I)` (hand-checked against `rect_segGf`: agrees).
2. **Item 3 — the multi-gadget block law.**  Interval geometry: full separation forces `lo = p+1`
   and `hi = p+a+1`, so at most one gadget matters and `L = a`; suffix `[lo,q)` and prefix
   `[0,hi)` traces separate none (would need `a = 0`).  Feed `blockGf_cons_of_high/low` down the
   gadget list, then `blockFreq_eq_binomial_of_seg` / `isAbelianAt_blockSeq_of_binomSeg`.
   Corollary: `c4_realizable_of_finite_compl`.
3. **Item 4 — the layout.**  Choose `o_m mod q_{m−1}` avoiding ≤ `16(m−1)` residue classes (a
   `Finset.card` pigeonhole: the forbidden set has card `< q_{m−1}` once `∑_{m'<m} 1/q_{m'}` is
   small), then assemble with `tendsto_onesFreq_of_linear_diff`.

## Confidence
C4 is TRUE: **high**.  Nothing left needs a limit theorem beyond the 3ε transfer (proved); the
remaining work is one polynomial identity, interval bookkeeping, and a pigeonhole.
