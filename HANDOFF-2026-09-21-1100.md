# Handoff: 2026-09-21 11:00 — Brun arithmetic count lap complete (STOP)

**Branch**: `wip/g5-prime-subset` · **HEAD**: `3a15d2c` · Build: `lake build` green (9115 jobs).

## Status

`KICKOFF-brun-arithmetic-count.md` is fully discharged; the treadmill stop sentinel is set
(`box done --green`).  Both named targets are proved in the new module
`src/NormalNumbers/PrimeModelBrunCount.lean` (0 `sorry`, imported from `src/NormalNumbers.lean`):

* `NormalNumbers.PrimeModel.BrunCount.brun_remainder_le_square`
* `NormalNumbers.PrimeModel.BrunCount.brun_sifted_count_lower`

`#print axioms` on both: `[propext, Classical.choice, Quot.sound]` (the `#print axioms` lines
live at the bottom of the module).  No frozen module edited; no new analytic hypothesis.

Full mathematical detail, exact hypothesis list, proof structure and the mathlib gotchas hit
are in **`HANDOFF-2026-09-21-brun-arithmetic-count-DONE.md`** — read that first.

## Commits this lap

* `63ecf42` — `brun_remainder_le_square` (injective subset-product route, no divisor machinery).
* `3a15d2c` — `brun_sifted_count_lower` + the detail handoff.

## Exact next steps (for whoever reopens the sieve thread)

1. **Encoding / cardinality of the selected primes** — turn the sifted count into a count of the
   intended prime tuples.  Out of scope this lap by operator instruction.
2. **Phase decay** and **parameter assembly** in `papers/prime-model-sieve-assessment.md`
   (`Q ≤ x^{1/8}`, `T^h = x^{1/4}`, `R = x^{1/4}` gives normalized remainder `≤ x^{-1/8}`, so the
   `R^2` remainder is already sufficient — do not build the sharp divisor-sum estimate).
3. Do **not** claim the final selected-prime theorem from the counting result alone.

Nothing is in flight; no Aristotle job open.
