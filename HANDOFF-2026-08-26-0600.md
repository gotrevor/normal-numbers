# HANDOFF 2026-08-26 06:00 — PILLAI'S THEOREM PROVED (Tier 1 crux cleared)

**Branch/HEAD**: master @ `e7705ee`, clean tree, `lake build` green (8743
jobs). Supersedes HANDOFF-2026-08-26-0100.md.

**⚠️ OBEY `DIRECTION.md` CURRENT DIRECTIVE** (refreshed this review lap): LOCK
TIER 1 = `xstar` absolutely normal ∧ CF-normal. The d-ary side and **Pillai's
theorem are now DONE**. The **sole remaining Tier-1 obligation is the headline
conjunction** (`Headline.lean:93`). W6/Khinchin (Tier 2, `Headline.lean:100`)
stays fenced off — do NOT start it.

## This lap's progress — Pillai fully proved (`Pillai.lean`, all axiom-clean)

Started from the disclosed `sorry` in `windowCount_eq_sum_phaseCount`; ended with
Pillai's theorem complete and `Pillai.lean` **sorry-free**. New lemmas (in build
order), each its own green commit:

1. `windowCount_eq_sum_phaseCount` (`d5dcc48`) — the `Q`-scale↔`N`-scale phase
   count identity (closed last lap's sorry). `Finset.card_nbij' (i ↦ i/r)`;
   dodged the `r*(i/r)` vs `(i/r)*r` omega-atom trap by anchoring on
   `Nat.div_add_mod` + one `Nat.mul_comm`.
2. `phaseOccCount_tendsto_atTop`, `phaseOccCount_div_tendsto` (`299f0c7`) —
   `P_s(N) → ∞` and `P_s(N)/N → 1/r` (squeeze).
3. `phaseWindowFreq_div_N_tendsto` (`ac50099`) — per-phase `N`-scale limit
   `phaseCount_s(N)/N → b^{-L}/r` (Tendsto.mul of the two factors).
4. `sum_nonStrad_phaseCount_div_tendsto`, `sum_strad_phaseOccCount_div_tendsto`
   (`0e04b45`) — the two finite phase-sum limits (`Lo_r → ℓ_r`, gap `→ (L-1)/r`).
5. `windowCount_div_sandwich` (`41ef67e`) — `Lo_r(N) ≤ W(N)/N ≤ Hi_r(N)`.
6. **`windowFreq_tendsto`** (`60635ba`) — THE double-limit crux (the handoff's
   "genuinely hardest remaining step"): block frequency `→ b^{-L}` via the
   ε-in-`r` squeeze (`Metric.tendsto_atTop`, `linarith` from
   `ℓ_r ≤ b^{-L} ≤ ℓ_r+(L-1)/r`).
7. **`pillai`** (`e7705ee`) — **Pillai's theorem**: `(∀ r≥1, simple normal at
   b^r) ⇒ IsNormalSequence b (digitOf b y)`. Bridge = `countOccurrences_range_map`
   (`Counting.lean`) + a `MatchesAt ↔ ofFn-window=w` predicate lemma
   (`List.ofFn_injective`/`List.ofFn_get`, avoiding the getElem dependent-motive
   trap), closed via `Tendsto.congr`.  Added `import NormalNumbers.Counting`.

## NEXT — the headline conjunction (`Headline.lean:93`, directive item 3)

Only classical wiring, no new math. Discharge
`exists_absolutely_normal_cf_normal : ∃ x, IsAbsolutelyNormal x ∧ IsCFNormal x`
with witness `xstar`:

1. **`IsAbsolutelyNormal xstar`** (= `∀ b≥2, IsNormal b xstar`). `IsNormal b x`
   unfolds (RealDefs.lean:31) to `IsNormalSequence b (digitOf b (Int.fract x))`.
   Apply **`pillai`** with `y := Int.fract xstar` (∈ [0,1)); its `hsn` is simple
   normality at every `b^r`, supplied by **`xstar_dary_freq_tendsto (b^r)`** at
   base `d = b^r`. FIRST CHECK: (a) the exact form of `xstar_dary_freq_tendsto`'s
   conclusion (`DaryCorrect.lean:934`) vs `pillai`'s `hsn` — does its frequency
   expression match `((range Q).filter (fun q => digitOf (b^r) y q = c)).card / Q`?
   (b) whether `digitOf d xstar = digitOf d (Int.fract xstar)` (digitOf should
   depend only on the fractional part — verify in `DigitInterval`/`RealDefs`);
   (c) `2 ≤ b^r` from `2 ≤ b`, `r ≥ 1` (`Nat.le_self_pow`/`le_trans`, cf.
   `Pillai.lean` `hbr2`).
2. **`IsCFNormal xstar`** — JUDGE addendum says this is a WRAPPER of the proven
   `xstar_cf_freq_tendsto` (CFCorrect.lean), not new math. Match its conclusion
   shape to `IsCFNormal`'s (`countOccurrences v ((range p).map (cfDigit xstar))/p
   → γ(cfCylinder v).toReal`). May need a `countOccurrences`↔ window bridge like
   Pillai's (or one already exists in `CFWordBridge`/`Counting`).
3. Assemble: `refine ⟨xstar, ?_, ?_⟩`. Find `xstar`'s def + name it (grep
   `def xstar`/`xstar_irrational` in `CFSchedule`/`CFLimit`/`CFCorrect`).

**Tier 2 (`Headline.lean:100`) stays `sorry`** — needs `KhinchinTypical` (W6),
fenced off until Tier 1 is locked. It holds the self-stop gate open (JUDGE).

## Gotchas confirmed this lap
- **`div_le_div_of_nonneg_right (hab : a≤b) (hc : 0≤c) : a/c ≤ b/c`** — exists in
  this pin (GroupWithZero/Basic), takes `0 ≤ c` (not `0 < c`).
- **`set f := …` then `apply`/`gcongr`/`add_le_add_left`** zeta-unfolds the
  let-binding and/or leaves eta mismatches (`t.sum f` vs `t.sum (fun x => f x)`)
  → `add_le_add_left` typeclass-stuck. Write filter sums OUT explicitly, use
  `gcongr` for `a + ∑ ≤ a + ∑`.
- **`rw [finsetEq]` inside `↑(_.card)/↑n`** → "motive not type correct" (card is
  dependent). Rewrite at the Nat level: `rw [congrArg Finset.card hFilterEq]`.
- **`rw [← h]` where `h : ofFn=w` into a `w[j]'hj`** → motive fails (index proof
  `j < w.length` ≠ `j < ofFn.length`). Use `List.ofFn_injective` + `List.ofFn_get`
  instead of getElem rewriting.
- Deprecations (cosmetic, unfixed): `tendsto_finset_sum`→`tendsto_finsetSum`,
  `Set.mem_setOf_eq`→`Set.mem_ofPred_eq`.
