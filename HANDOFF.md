# HANDOFF — pointer

**Thin pointer (plain file, never a symlink).** Durable overview + live baton
live elsewhere:

- **STATUS.md** — living project overview + axiom ledger (refreshed reflection lap).
- **DIRECTION.md** — CURRENT DIRECTIVE (binding; OUTRANKS this file). Currently:
  **TIER 1 LOCKED** (`exists_absolutely_normal_cf_normal`, axiom-clean). Objective
  = **Tier 2 Khinchin** (`Headline.lean:136`). **Route C′ RATIFIED**: summable
  Markov log-tail family, machinery COMPLETE + axiom-clean; the gap is pure
  WIRING. CFSchedule family-rewire (step 1) DONE this lap; next = assemble the
  crux `xstar_log_tail_uniform` (step 2). Do NOT build more upstream machinery.
- **Newest dated baton** — `HANDOFF-2026-08-24-lap10-reflection.md` (reflection
  synthesis + CFSchedule family rewire; detailed step-2 assembly plan).
- **PENDING_WORK.md** — open-items / attack-path scratchpad (top entry = the
  dated reflection section).

Build: 🟢 green (8750 jobs). `src/` open sorries: `Headline.lean:136` (Tier-2
headline, awaiting route D′ layering) and `Khinchin.lean:527`
(`xstar_log_tail_uniform`, the sole crux) — the active decomposition, not
regressions.
