# HANDOFF — pointer

This is a thin pointer (plain file, never a symlink). The durable overview and
the live baton live elsewhere:

- **STATUS.md** — living project overview + axiom ledger (refreshed on review laps).
- **DIRECTION.md** — CURRENT DIRECTIVE (binding; outranks any baton). Currently:
  LOCK TIER 1 (`xstar` absolutely normal ∧ CF-normal); Pillai + d-ary DONE, only
  the headline conjunction (`Headline.lean:93`) remains. W6/Khinchin fenced.
- **Newest dated baton** — `HANDOFF-2026-08-26-0600.md` (Pillai's theorem PROVED;
  next = Tier-1 headline conjunction). Discover the newest by:
  `ls HANDOFF-*.md | sort | tail -1` (dated form sorts correctly).
- **PENDING_WORK.md** — open-items / attack-path scratchpad (top entry is newest).

Build: 🟢 green (8743 jobs). `src/` open sorries: `Headline.lean:93` (Tier 1,
next up) and `Headline.lean:100` (Tier 2 / Khinchin, deliberately fenced).
