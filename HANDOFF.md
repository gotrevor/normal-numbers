# HANDOFF — pointer

**Thin pointer (plain file, never a symlink).** Durable overview + live baton
live elsewhere:

- **STATUS.md** — living project overview + axiom ledger (refreshed reflection lap).
- **DIRECTION.md** — CURRENT DIRECTIVE (binding; OUTRANKS this file). Currently:
  **TIER 1 LOCKED** (`exists_absolutely_normal_cf_normal`, axiom-clean). Objective
  = **Tier 2 Khinchin**. **Route C′ steps 1+2 DONE**: the crux
  `xstar_log_tail_uniform` is PROVED, so `xstar_khinchinTypical` is PROVED
  axiom-clean. **Only step 3 (route D′ layering) remains** to close the headline.
- **Newest dated baton** — `HANDOFF-2026-08-24-lap10-reflection.md` (reflection
  synthesis + CFSchedule family rewire; step-2 assembly now landed too).
- **PENDING_WORK.md** — open-items / attack-path scratchpad; TOP `GRIND` note has
  the exact route-D′ (layering) recipe to close the last sorry.

Build: 🟢 green (8750 jobs). ONLY remaining `src/` sorry: `Headline.lean:134`
(`exists_absolutely_normal_cf_normal_khinchin`, the Tier-2 headline) — awaiting
route D′ (move `khinchinK₀`/`KhinchinTypical` defs upstream so Headline can
invoke the proved `xstar_khinchinTypical`). See PENDING_WORK top for the recipe.
