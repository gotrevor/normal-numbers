# Handoff: run+jump chain PROVED — `M(p,1) > b(p−b−1) − 1`; treadmill stopped on `box done`

**Date**: 2026-09-08 07:44 UTC · **Branch**: `wip/adder-tower-c9` · **HEAD**: `8b00d1f` · tree clean
· last observed `lake build`: **green, 8888 jobs** (pre-commit hook on `8b00d1f`)

## 🎯 What we're doing
Pin the optimal Mahler multiplier `M(p,1)` for prime bases (`DIRECTION.md` §CURRENT DIRECTIVE,
set 2026-09-08 by the deep-reflection lap).  Its mandated move — the run+jump chain theorem in
`MahlerRunJump.lean` plus the two `b ∣ p+1` corollaries — is **complete**.  This run stopped itself
(`box done --green`, accepted) because the only `sorry` left in `src/` is the directive-parked
`exists_prime_nonresidue`, and trigger **T3** says: state the endpoint, don't grind for uniformity.
**The next lap should be an ALTITUDE (review/reflection) lap**; it owns the directive.

## 🧠 Context to carry forward
* **The theorem** (`MahlerRunJumpWalk.lean`): prime `p`, `3 ≤ b < p/2`, `p^f ≡ −1 (mod b)` for some
  `f ≥ 1` ⟹ `M(p,1) > b(p−b−1) − 1`.  Cycle = descending run of consecutive backgrounds
  `p−b, …, b` closed by the jump `b → p−b`; the only arithmetic condition is at the bottom.
* **Why it proved cleanly**: (a) the certificate is validated by ONE generic two-denominator edge
  lemma `d·E·E' + N'·E = p·N·E' + δ` (`MahlerRunJump.lean` §generic) instantiated on three edge
  types; the content is `cost_ge` (`M < Dn·(p−Dh)`, min `b(p−b−1)`) and `jmod`.  (b) the data needs
  only CONGRUENCES: `key_of_congr` turns departure-mod-`Dh` + landing-mod-`Dn` + coprimality into
  the exact identity.  Residues are powers of `p` with a common exponent `T = 2φ((p−b)!)`
  (`p^T ≡ 1` mod every background), so the walk is a three-block position formula, no lists.
* **The surprise (lap 4 census, `experiments/mahler_runjump_admissible.py`)**: the theorem's
  hypothesis is satisfiable within `j = p − 2b ≤ 33` of `p/2` at EVERY prime `< 2000`, so the
  proved bound is `1/4 − O(1/p)` there — `p ≡ 1 (mod 12)` included.  The T3 dichotomy
  (`3/16` vs `1/12`) is the *unconditional* floor; the *conditional* `1/4` is now the theorem
  `mahler_lower_bound_runjump_near_half` (`p = 2b + j`).  Against the exact census the bound is
  EXACT at `(p,b) = (13,5), (23,10), (31,14)` and never exceeds `M(p,1)` for `p ≤ 31`.
* **What is NOT provable by grind**: removing the hypothesis.  `b = (p−j)/2` is admissible iff
  `−1 ∈ ⟨j⟩ (mod (p−j)/2)`; a uniform small odd `j` is an Artin/short-interval statement, same
  tier as the parked `exists_prime_nonresidue`.  Don't attack it in a grind lap.
* **Kickoff reconciliation**: `KICKOFF-2026-09-07-mahler-quarter.md` asked for the multi-scale
  `g²/4` bound — that had already landed on 09-07 (`PENDING_WORK.md` §"THE MULTI-SCALE BOUND
  LANDED"); the directive outranked it and was followed.

## ✅ State (all observed this session)
* `src/NormalNumbers/MahlerRunJump.lean` — sorry-free; `RunJump.valid` axiom-clean.
* `src/NormalNumbers/MahlerRunJumpWalk.lean` — sorry-free; `mahler_lower_bound_runjump`,
  `…_of_dvd`, `…_three` (`2/9`), `…_four` (`3/16`), `…_near_half` (conditional `1/4`), anchors
  `…_base13_runjump` (`M ≥ 35`), `…_base31_runjump` (`M ≥ 224`); `#print axioms` =
  `[propext, Classical.choice, Quot.sound]` on all checked.
* `STATUS.md` (log entry + audit row + T3 endpoint), `PENDING_WORK.md` (§GRIND laps 2, 3;
  §MEASURED lap 4), wired in `src/NormalNumbers.lean`.  Commits `b77bd2a`, `00a0e4d`, `3517563`,
  `8b00d1f`.  Stop sentinel written: `~/src/.treadmill/normal-numbers.stop`.

## 🎬 Next actions
1. **Altitude lap**: read `PENDING_WORK.md` §MEASURED 2026-09-08 and §GRIND lap 3, then rewrite
   the directive.  The honest options: (a) declare the prime-base chapter at its endpoint
   (unconditional `3/16` / `1/12`, conditional `1/4`, exact at three primes) and move the objective
   (`k ≥ 2`? composite `g`? the upper-side gap `⌊p/2⌋² − {0,1,4}` vs `(p²+6p+1)/4`?); or (b) mandate
   the deficit study: why the run+jump misses the census at `p = 19, 29` (`77` vs `80`, `187` vs
   `192`) — a hybrid with a second jump may close it.  Decide there, not in a grind lap.
2. If a grind lap runs first anyway: nothing is open on-route; the only permitted Lean work would
   be off-route sorries, and there are none besides `exists_prime_nonresidue` (forbidden).  Idle is
   correct; say so.

## ⚠️ Gotchas
* `nlinarith` times out on the certificate's block inequalities; supply the product facts and use
  `linarith`.  `linear_combination` over ℕ: no negation — flip the hypothesis (`h.symm`).
* Under `include hb hp`/`include A`, every lemma in the section gets them; `omit … in` any lemma
  that doesn't use them or call sites break ("expected `3 ≤ b`", "don't know how to synthesize `p`").
* `subst hnT` (`hnT : n = T`) eliminated `T`; use `rw`.  `rw [← lemma_with_RHS_1]` rewrites every
  `1`, including in `L − 1`; rewrite the hypothesis forward instead.
* `field_simp` often closes cast identities itself: end with `try ring`.
* The kickoff/handoff clock: stamp dates from `date`, never from filenames.

## 📁 Key files
- `DIRECTION.md` §CURRENT DIRECTIVE — binding; altitude laps only edit it
- `PENDING_WORK.md` top three sections — the full account of laps 2–4
- `src/NormalNumbers/MahlerRunJump.lean`, `src/NormalNumbers/MahlerRunJumpWalk.lean`
- `experiments/mahler_runjump_admissible.py` — the admissible-`b` census (pure Python, no sympy)
- `docs/mahler-exact-values-2026-09-07.md` — the census the bound is checked against

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or wait for
instructions, and don't offer other projects from the KB — this doc IS the chosen thread. Absorb the
context above, then pick up at "Next actions" #1: run the altitude lap and rewrite the directive.**
