# Handoff: (E) assembled — `RoughRowVarianceLower` reduced to arithmetic hypotheses

**Date**: 2026-09-15 · **Branch**: `wip/g4-entropy` · `lake build` 🟢 · tree clean.

## What landed (laps 146–147), all in `src/NormalNumbers/G4RowVariance.lean`

The previous wrap listed a three-step "final assembly (no new mathematics)".  Both steps that
needed Lean are done.

1. **The weight norms** (lap 146).  Released weights `c_{α,j} = A_{να} 4^{-j}` over the window
   `K′ < j ≤ K′ + L`, indexed by `Fin m × Fin L`:
   * `geom_offset` — offset geometric sum;
   * `sum_abs_released` : `Σ|c| = m · 4^{-K′}(1 − 4^{-L})/3`;
   * `sum_sq_released` : `Σ c² = m · 16^{-K′}(1 − 16^{-L})/15`;
   * `ratio_released` : `(Σ|c|)² ≤ (5m/3) Σ c²` — the truncation factor *helps*
     (`(1−x)² ≤ 1−x²`), so the constant is clean for every `L`;
   * `released_row_lower` — `rowVariance_half` at those weights: near-orthogonality needed is
     `ε ≤ 3v/(20m)`, i.e. `ε ≲ v 2^{-K}` at `m = 2^K`;
   * `roughRowVarianceLower_of_released` — delivers `Budget.RoughRowVarianceLower` verbatim with
     constant `(v/2)(1 − 16^{-L})`.

2. **The arithmetic instantiation** (lap 147).
   * `fluct S r n = Σ_{p∈S} (1[p ∣ n+r] − 1/p)`;
   * `table_le_split` — the three-case table of `cross_shift_corr_le` splits into the
     gap-divisor part `Σ_{p ∈ S, p ∣ Δ} 4/p` plus a uniform `Σ_{p∈S} 3/p²`;
   * **`roughRowVarianceLower_arith`** — on the CRT progression `{c₀ + Qk : k < N}` with rough
     primes `S` (pairwise coprime, coprime to `Q`), released weights of the standard shape, and a
     gap-divisor budget `g`, the row second moments satisfy `Budget.RoughRowVarianceLower`.
     Its hypotheses are exactly: `v ≤ Σ_p(1/p − 1/p²) − 3|S|²/N`,
     `g ≥ Σ_{p ∣ ρ_i − ρ_j} 4/p` for all released pairs, `ε ≥ g + Σ 3/p² + 3|S|²/N`, and
     `ε·(5·2^K/3) ≤ v/2`.

3. **The gap-divisor budget** (lap 148).  `card_rough_divisors_le` — pairwise-coprime divisors
   `≥ T` of a nonzero `Δ` with `|Δ| < T^{k+1}` number at most `k` (their product divides `Δ`);
   `gap_divisor_sum_le` — hence `Σ_{p ∈ S, p ∣ Δ} 4/p ≤ 4k/T`, the `g` of
   `roughRowVarianceLower_arith`, with no logarithms in the statement.

4. **The dyadic instantiation** (lap 149) — **(E) is closed.**  `dyadic_variance_ge`
   (`Σ_p(1/p − 1/p²) ≥ m/(4T)` on `S ⊆ [T,2T]`, `T ≥ 4`), `dyadic_sq_sum_le`
   (`Σ 3/p² ≤ 3m/T²`), `dyadic_scales` (the numeric core: at `T ≥ 1440A`, `m ≥ 1920Ak`,
   `N ≥ 1440AmT`, each term of `ε` is `≤ m/(480AT)` so `ε·(5A/3) ≤ m/(96T) ≤ v/2`), and
   **`roughRowVarianceLower_dyadic`** — `Budget.RoughRowVarianceLower` with *no hypothesis left
   over* beyond the structural data (dyadic rough primes, coprimality, bounded shift gaps, the
   three scale conditions).

**Status of (E).**  Discharged.  It is no longer an unproved `Prop` standing between the arithmetic and the
verdict.  What is left is a *numeric* check on the schedule's own parameters: that the rough-prime
range makes `Σ_p(1/p − 1/p²)` large, `N` large enough to kill `3|S|²/N`, and the gap-divisor
budget `g` (at most `log|Δ|/log T` primes, each `< 1/T`) small enough that
`ε·(5·2^K/3) ≤ v/2`.  That is the next lap: instantiate `roughRowVarianceLower_arith` at
`Sched`'s `K i`, `dmin i`, `T i` and discharge the four inequalities with `omega`/`norm_num`
style arithmetic, then chain to `Budget.budget_forces_two_layers` and `Sched.balanced_union_le`.

5. **The verdict with (E) proved** (lap 150).  `dyadic_v_pos` (strict positivity of the variance
   constant) and **`two_layers_of_dyadic`** — `Budget.budget_forces_two_layers` with the variance
   hypothesis *supplied rather than assumed*: a sampler whose released-row second moment fits the
   capture budget `c·2^{-K/2}` must cancel at least two layers, and two balanced layers is exactly
   the hypothesis of `Sched.balanced_union_le`.  `#print axioms` on both
   `roughRowVarianceLower_dyadic` and `two_layers_of_dyadic`:
   `[propext, Classical.choice, Quot.sound]`.

6. **The capstone** (lap 151).  `LayersCancelled` (the sampler cancels its first `K′` layers —
   DESIGN §4's definition of cancellation is row-balance of `layer j d t`),
   `two_balanced_of_cancelled`, and **`cancelled_union_le`**: a family of samplers each
   cancelling at least two layers reads, below `L`, at most
   `L/dmin^{H/2} + (2dmax+1)^{2E}·H·m` positions.  With `two_layers_of_dyadic` supplying
   `2 ≤ K′`, the chain runs **arithmetic of `ω` on the progression → variance lower bound →
   capture budget → two cancelled layers → upper density `≤ dmin^{−H/2}`** with every link
   machine-checked and axiom-clean.

## Unchanged
`src/` carries only the two pre-expedition off-path `sorry`s
(`PrimeLambertOscillation.phaseOscillation`, `MahlerDriftOne.exists_drift_one_background`), both
on DIRECTION's forbidden-drift list.  Nothing here is a claim about normality of `G₄`.
