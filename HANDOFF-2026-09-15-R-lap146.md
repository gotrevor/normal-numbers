# Handoff: (E) assembled — `RoughRowVarianceLower` reduced to arithmetic hypotheses

**Date**: 2026-09-15 · **Branch**: `wip/g4-entropy` · `lake build` 🟢 · tree clean.

## What landed (laps 146–147), all in `src/NormalNumbers/G4RowVariance.lean`

The previous wrap listed a three-step "final assembly (no new mathematics)".  Both steps that
needed Lean are done.

1. **The weight norms** (lap 146).  Released weights `c_{α,j} = A_{να} 4^{-j}` over the window
   `K′ < j ≤ K′ + L`, indexed by `Fin m × Fin L`:
   * `geom_offset` — offset geometric sum;
   * `sum_abs_released` : `Σ|c| = m · 4^{-K′}(1 − 4^{-L})/3`;
   * `sum_sq_released` : `Σ c² = m · 16^{-K′}(1 − 16^{-L})/15`;
   * `ratio_released` : `(Σ|c|)² ≤ (5m/3) Σ c²` — the truncation factor *helps*
     (`(1−x)² ≤ 1−x²`), so the constant is clean for every `L`;
   * `released_row_lower` — `rowVariance_half` at those weights: near-orthogonality needed is
     `ε ≤ 3v/(20m)`, i.e. `ε ≲ v 2^{-K}` at `m = 2^K`;
   * `roughRowVarianceLower_of_released` — delivers `Budget.RoughRowVarianceLower` verbatim with
     constant `(v/2)(1 − 16^{-L})`.

2. **The arithmetic instantiation** (lap 147).
   * `fluct S r n = Σ_{p∈S} (1[p ∣ n+r] − 1/p)`;
   * `table_le_split` — the three-case table of `cross_shift_corr_le` splits into the
     gap-divisor part `Σ_{p ∈ S, p ∣ Δ} 4/p` plus a uniform `Σ_{p∈S} 3/p²`;
   * **`roughRowVarianceLower_arith`** — on the CRT progression `{c₀ + Qk : k < N}` with rough
     primes `S` (pairwise coprime, coprime to `Q`), released weights of the standard shape, and a
     gap-divisor budget `g`, the row second moments satisfy `Budget.RoughRowVarianceLower`.
     Its hypotheses are exactly: `v ≤ Σ_p(1/p − 1/p²) − 3|S|²/N`,
     `g ≥ Σ_{p ∣ ρ_i − ρ_j} 4/p` for all released pairs, `ε ≥ g + Σ 3/p² + 3|S|²/N`, and
     `ε·(5·2^K/3) ≤ v/2`.

3. **The gap-divisor budget** (lap 148).  `card_rough_divisors_le` — pairwise-coprime divisors
   `≥ T` of a nonzero `Δ` with `|Δ| < T^{k+1}` number at most `k` (their product divides `Δ`);
   `gap_divisor_sum_le` — hence `Σ_{p ∈ S, p ∣ Δ} 4/p ≤ 4k/T`, the `g` of
   `roughRowVarianceLower_arith`, with no logarithms in the statement.

4. **The dyadic instantiation** (lap 149) — **(E) is closed.**  `dyadic_variance_ge`
   (`Σ_p(1/p − 1/p²) ≥ m/(4T)` on `S ⊆ [T,2T]`, `T ≥ 4`), `dyadic_sq_sum_le`
   (`Σ 3/p² ≤ 3m/T²`), `dyadic_scales` (the numeric core: at `T ≥ 1440A`, `m ≥ 1920Ak`,
   `N ≥ 1440AmT`, each term of `ε` is `≤ m/(480AT)` so `ε·(5A/3) ≤ m/(96T) ≤ v/2`), and
   **`roughRowVarianceLower_dyadic`** — `Budget.RoughRowVarianceLower` with *no hypothesis left
   over* beyond the structural data (dyadic rough primes, coprimality, bounded shift gaps, the
   three scale conditions).

**Status of (E).**  Discharged.  It is no longer an unproved `Prop` standing between the arithmetic and the
verdict.  What is left is a *numeric* check on the schedule's own parameters: that the rough-prime
range makes `Σ_p(1/p − 1/p²)` large, `N` large enough to kill `3|S|²/N`, and the gap-divisor
budget `g` (at most `log|Δ|/log T` primes, each `< 1/T`) small enough that
`ε·(5·2^K/3) ≤ v/2`.  That is the next lap: instantiate `roughRowVarianceLower_arith` at
`Sched`'s `K i`, `dmin i`, `T i` and discharge the four inequalities with `omega`/`norm_num`
style arithmetic, then chain to `Budget.budget_forces_two_layers` and `Sched.balanced_union_le`.

5. **The verdict with (E) proved** (lap 150).  `dyadic_v_pos` (strict positivity of the variance
   constant) and **`two_layers_of_dyadic`** — `Budget.budget_forces_two_layers` with the variance
   hypothesis *supplied rather than assumed*: a sampler whose released-row second moment fits the
   capture budget `c·2^{-K/2}` must cancel at least two layers, and two balanced layers is exactly
   the hypothesis of `Sched.balanced_union_le`.  `#print axioms` on both
   `roughRowVarianceLower_dyadic` and `two_layers_of_dyadic`:
   `[propext, Classical.choice, Quot.sound]`.

6. **The capstone** (lap 151).  `LayersCancelled` (the sampler cancels its first `K′` layers —
   DESIGN §4's definition of cancellation is row-balance of `layer j d t`),
   `two_balanced_of_cancelled`, and **`cancelled_union_le`**: a family of samplers each
   cancelling at least two layers reads, below `L`, at most
   `L/dmin^{H/2} + (2dmax+1)^{2E}·H·m` positions.  With `two_layers_of_dyadic` supplying
   `2 ≤ K′`, the chain runs **arithmetic of `ω` on the progression → variance lower bound →
   capture budget → two cancelled layers → upper density `≤ dmin^{−H/2}`** with every link
   machine-checked and axiom-clean.

7. **Non-vacuity** (lap 152).  `exists_rough_primes`: for every roughness threshold `T ≥ 2` and
   every target `V` there is a finite set of primes, all `≥ T`, with `Σ (1/p − 1/p²) ≥ V`.
   Proof via mathlib's `not_summable_one_div_on_primes` — the partial sums over `range M`
   diverge, and dropping the primes below `T` costs at most `T`.  So the per-term variance
   hypothesis of the chain is satisfiable at every roughness scale, not vacuously true.

8. **The `|S|`-free remainder, and (E) in Mertens form** (lap 153).  `dyadic_sq_sum_le`'s
   `3|S|/T²` forces `|S| ≪ T`, which fights `exists_rough_primes` (large `Σ1/p` needs *many*
   primes).  `inv_sq_le_telescope` + `sum_inv_sq_rough_le` replace it by the `|S|`-free tail
   bound `Σ_{p∈S} 1/p² ≤ 1/(T−1)` for distinct naturals `≥ T`.  Then
   **`roughRowVarianceLower_mertens`**: `S` any finite set of primes above `T > Q`, correlation
   level `ε = 4k/T + 3/(T−1) + 3|S|²/N`, variance `V` from `exists_rough_primes`, coprimality
   derived (distinct primes; `p > Q`).  The two ingredients are now simultaneously satisfiable:
   pick `T` large (kills `4k/T + 3/(T−1)`), then `V` large, then `N` large.

9. **Non-vacuity of the whole chain** (lap 154).  `scales_satisfiable`: for **every** row size
   `K`, gap exponent `k` and frozen modulus `Q` there exist `T`, a rough prime set `S` and a
   sample length `N` meeting every numeric hypothesis of `roughRowVarianceLower_mertens` at
   `V = 1` — take `T > 16A(4k+3) + 4 + Q` with `A = 5·2^K/3`, then `exists_rough_primes`, then
   `N > 24A|S|² + 6|S|²`.  So (E) is not a statement that holds because its hypotheses are
   unmeetable.

10. **R's open item 3, made exact** (lap 155).  The design's informal grouped-sample criterion
    "`w ≳ 2 log H / log dmin`" is now a logarithm-free theorem: `grouped_coeff_le` shows the
    grouped density coefficient `G·|𝓕|·m·H·dmax/(2 dmin^w)` drops to `dmin^{−w/2}` — the same
    rate `Sched.balanced_union_le` achieves at `w = H` — exactly when
    `|𝓕|·m·H²·dmax ≤ 2·dmin^{w − w/2}`; `grouped_union_le'` states the resulting confinement
    bound.  Taking logarithms recovers the design's `w/2 ≥ (2 log H + log(|𝓕| m dmax))/log dmin`.

11. **R's open item 2, discharged** (lap 156).  `union_le_of_determining`: confinement for an
    **arbitrary** invariant `Inv` pinned by its values on a set `Sdet` — the read set below `L`
    is at most `(2dmax+1)^{2|Sdet|}·(L m H dmax/(2 dmin^H) + H m)`.  Nothing in the confinement
    uses `MDF`; `Sched.balanced_union_le` is the instance `Inv = MDF`, `Sdet = skel`.  So a
    different tensor matrix `A` costs only a determining set for `A`'s own balance relations,
    with no new confinement proof — item 2 is now a theorem, not a remark.

12. **Cube-local balance** (lap 157).  `toggle`, `sum_alt_toggle_zero` and
    `balanced_of_cube_ignores`: `ρ` is row-balanced as soon as on **every unit cube** *some*
    coordinate leaves it unchanged — a different one per cube is allowed.  This strictly
    generalizes `RowBalance.balanced_of_update_invariant` (global invariance) and is the tool
    needed to promote R's single `K = 3` counterexample to a family for every `K ≥ 3`:
    the family's cube-by-cube analysis is Case A (`f ≡ 0` on the cube), Case B (depends on two
    coordinates), Case C (`c₀ = 1`, depends on `0,1,2` — the `K = 3` `decide` case, with a spare
    coordinate to ignore once `K ≥ 4`).

13. **The `K ≥ 4` counterexample family** (lap 158) — R's refutation upgraded from one searched
    witness to an explicit family.  The **pointer function** `ptrFun α = α (α 0)` (coordinate `0`
    names the coordinate to read) is:
    * `ptrFun_balanced` (`K ≥ 4`): on any unit cube `α 0` takes only `c 0` and `c 0 + 1`, so
      `ptrFun` reads at most the three coordinates `0`, `c 0`, `c 0 + 1` there — with `K ≥ 4` a
      fourth is always free, and `balanced_of_cube_ignores` applies.  No `decide`, no search;
      the proof is uniform in `K` and `s`.
    * `ptrFun_not_ignoring` (`K ≥ 2`, `K ≤ s + 1`): every coordinate genuinely moves it.

    With `ex` at `K = 3` and `ptrFun` at every `K ≥ 4`, the threshold
    `ignores_coord_of_balanced_two` (`K ≤ 2` rigid) is **exact at every `K ≥ 3`**, not just at
    the one searched size.

14. **`threshold_exact`** (lap 159).  One statement: for every `K ≥ 3` there are an alphabet `s`
    and a row-balanced `ρ` on `(Fin K → Fin (s+1))` ignoring no coordinate (`ex` at `K = 3`,
    `ptrFun` at `K ≥ 4`).  With `ignores_coord_of_balanced_two` (rigid at `K ≤ 2`) this pins the
    rigidity threshold exactly.  `mdf_of_balanced` still applies to these witnesses, so
    `union_le_of_determining`'s confinement is untouched — the refutation does not reopen the
    deformation.

15. **The cube-local criterion is strictly sufficient** (lap 161).  `cube_ignores_not_necessary`:
    `ex` is balanced, yet on the cube `c ≡ 1` (every coordinate ranging over `{1,2}`) no single
    coordinate can be toggled without changing `ex`.  A probe first established `c ≡ 1` is the
    only such cube for `ex`.  So balance is genuinely a level-set condition, not a disguised
    coordinate-degeneracy — settling the converse question `balanced_of_cube_ignores` raised.

16. **Balance ⊋ MDF** (lap 162).  `balanced_strictly_stronger`: `α ↦ α₀ + α₁` on
    `(Fin 2 → Fin 2)` is `MDF` but not balanced (its level set `{ρ = 1}` has alternating sum
    `−2` on the unit square).  Since the confinement consumes only `MDF`, a balanced family is
    constrained by more than the bound uses — a determining set for *balance* smaller than
    `skel` would sharpen `Sched.balanced_union_le`'s coefficient directly, via
    `union_le_of_determining`.  That is the one visible route past `dmin^{−H/2}`.

17. **The sharpening route is refuted** (lap 163).  `balance_not_determined_by_axes`: the axis
    skeleton `skel₁` (polynomial size) does *not* determine balanced functions —
    `pairWit α = [α₁ = 1 ∧ α₂ = 1]` is balanced (ignores coordinate `0`), nonzero, and vanishes
    on `skel₁`.  A probe at `K = 3, s = 2` found `15786` colliding pairs among the `128`
    `0/1`-valued balanced functions first; the witness above is the clean extract.  So the
    exponential `skel` is not an artifact of routing through `MDF`, and the `dmin^{−H/2}`
    exponent is not improvable this way.

18. **The counting sharpening is refuted too** (lap 164, probe).
    `PROBE-2026-09-15-balanced-count.md`: exhaustive enumeration of `0/1`-valued balanced
    functions shows the `skel` bound is loose only by a *constant factor in the exponent*
    (`K = 3, s = 2`: true `log₂` count `13.96` against the bound's `19`), and at `K = 2` the
    count is exactly `2^{s+2} − 2`.  A constant-factor gain moves the coefficient
    `(2dmax+1)^{2E}`, not the density rate `dmin^{−H/2}`, which comes from `union_card_le`.
    So no sharpening of the rate is available from counting.  Do not retry.

## Unchanged
`src/` carries only the two pre-expedition off-path `sorry`s
(`PrimeLambertOscillation.phaseOscillation`, `MahlerDriftOne.exists_drift_one_background`), both
on DIRECTION's forbidden-drift list.  Nothing here is a claim about normality of `G₄`.
