# HANDOFF 2026-08-25 05:00 — Lemma 13 COMPLETE (both cases) + seed + limit point

**Branch/HEAD**: master @ `07c8e67`, clean tree, `lake build` green (8738 jobs).
Supersedes HANDOFF-2026-08-25-0200.md.

## Session headline (continuation of the Lemma-13 lap)

W5's route-decisive wall is fully down, and the construction skeleton exists:

1. `6628061` **`TBrick.exists_refinement`** — B–Y Lemma 13, t'=t case
   (see HANDOFF-2026-08-25-0200 for the payload details).
2. `64218d3` **`TBrick.exists_extend_succ`** (t→t+1 on the same word; new base
   via standalone `exists_cell_block`) + **`exists_seed_brick`** (a 2-brick on
   any genuine word) — the brick recursion of §2.1 is closed.
3. `07c8e67` **`exists_irrational_mem_iInter_cfCylinder`** (`CFLimit.lean`) —
   the limit point x*: any strictly extending sequence of genuine CF words has
   an IRRATIONAL point in all its cylinders.  Coprimality-free irrationality:
   `rat_dist_ge` (|p/q − P/Q| ≥ 1/(qQ)) vs the exact gap 1/(K(K+K')) forces a
   rational onto BOTH endpoints once cfK > denominator (fib growth).  Public
   packaging `cfCylinder_endpoints` added to CFCylinder.lean.

All new theorems verified `#print axioms` = trust triple.  src/ sorry-free.

## NEXT (the remaining W5 bookkeeping, in order)

- **(A) Schedule + recursion** (§2.1): build `w : ℕ → List ℕ` by iterating
  `exists_seed_brick` → `TBrick.exists_refinement` (+ occasionally
  `exists_extend_succ`).  KEY DESIGN CONSTRAINT (from the §2.2 chains): t must
  grow SLOWLY — stay at fixed t for many stages so that
  `n(s) ≤ 2ε(s₀)·Σ_{j<s} n(j)` and its d-ary analog hold eventually (if t
  incremented every stage, N(s) grows superexponentially and the condition
  fails).  We are free of complexity constraints: pick n(s) := N-threshold
  exactly, and increment t only when s exceeds a threshold σ(t) chosen large
  enough.  Suggest: define the recursion with state (B_s, t_s, history sums)
  via `Nat.rec` and prove the two dominance conditions as invariants.
- **(B) Correctness chains** (§2.2): CF normality of x* via `CFDiscLt.append`
  / `cfDiscLt_append_take` / `cfDiscLt_short_append` (payload (ii) of
  exists_refinement matches Lemma-7's hypothesis once δ(s), ε(s) scheduled);
  simple d-normality via BaryConcat's `HasDiscLt` chains on x*'s digit blocks
  (payload (iv) applies to y = x* directly — x* ∈ every refined cylinder).
  Need a d-ary digit-extraction bridge: x*'s base-d digit list between orders
  m_d(s) and m_d(s+1) IS the block β of payload (iv) (blockNatVal ↔ digit
  list; `exists_block_of_lt` inverts, `floor_subCell_bounds` locates).
- **(C) Headline statement**: "x* is simply normal to every base d ≥ 2 and
  CF-normal"; then Pillai powers-equivalence (simple normality to all bases
  b^k ⇒ full normality — check Wall.lean/Counting.lean for reusable pieces)
  to phrase "absolutely normal".  Freeze statement shapes for JUDGE.
- **(D) W6**: Khinchin graft (digit caps; see KHINCHIN.md).

## Gotchas (this session, beyond HANDOFF-0200's list)

- `Set.uIcc`/`uIoo`: no `isClosed_uIcc` in this mathlib — `rw [Set.uIcc]` then
  `isClosed_Icc`.  No `Set.mem_uIoo` iff-lemma — use `Set.mem_uIoo_of_lt` /
  `Set.mem_uIoo_of_gt`.  `Set.mem_uIcc` (disjunction form) exists and is the
  robust way to case-split.
- Structure-literal fields (`fun d => if d = t+1 then … else …`): prove per
  clause with `by_cases h : d = t+1` + `rw [if_pos/if_neg]`; a bare
  `simp only [if_neg h]` can leave `True ∧ True` shapes — plain `simp [h]`
  closes them.
- `IsCompact.nonempty_iInter_of_sequence_nonempty_isCompact_isClosed` is the
  nested-compacts lemma; compactness of `closure (cylinder)` via
  `IsCompact.of_isClosed_subset (isCompact_Icc : IsCompact (Icc 0 1))`.
- `Rat.cast_def : (r : ℝ) = r.num / r.den` gives the denominator handle;
  `Irrational x` is defeq `¬ x ∈ range Rat.cast`, so `not_not.1` unpacks a
  non-irrational directly to a rational witness.
