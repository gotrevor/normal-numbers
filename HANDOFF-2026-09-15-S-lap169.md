# Handoff — laps 166–169: objective S item 1 (the seam) DONE; stopping per the 01:04 addendum

**Date**: 2026-09-15 · **Branch**: `wip/g4-entropy` · **HEAD** `0188dad` · `lake build` 🟢 **9042
jobs** · tree clean · every new declaration `[propext, Classical.choice, Quot.sound]`.

## Status against the attended addendum (DIRECTION, 2026-09-15 01:04 EDT)

The addendum authorizes review lap 166's objective S **only as its item 1** — the seam
`Sched.budget_confines` — and then: stop, write the handoff, pick no successor, file no stuck
strikes.  **Item 1 is done** (`e148a7e`, hardened by `b14c49d`).  Item 3 also landed
(`0188dad`) before the addendum was visible to this session; it is green, axiom-clean and
honest, and is described below so it is not lost, but **no further successor is taken**.
This lap ends idle, as instructed.

## What landed

### Lap 166 — the third escape of DESIGN §0, at the schedule (`b54d97c`)

New `src/NormalNumbers/G4GroupedVerdict.lean`.  §0 lists three untested deformations; two were
closed (`Sched.balanced_union_le`, `RowVariance.union_le_of_determining`).  The third — giving up
the single-`n` joint sample — had only a *generic* reduction whose size condition was never
checked at the schedule.

| declaration | content |
|---|---|
| `Sched.grouped_size_cond` | `(2dmax+1)^{2E}·m·H²·dmax ≤ dmin^{4E+3}`, so `grouped_coeff_le`'s hypothesis holds once `w ≥ 8E + 5` |
| `Sched.grouped_block_size` | disjoint blocks covering the atoms: `G · w ≤ H` |
| `Sched.grouped_block_count_le` | **`8 K G ≤ K² + 1`** — at most `(K²+1)/(8K) ≈ K/8` blocks (`20000` at `i = 0`) |
| `Sched.grouped_balanced_union_le` | grouped verdict: `≤ L/dmin^{w/2} + G·(2dmax+1)^{2E}·H·m`, rate `dmin^{−w/2} ≤ dmin^{−(4E+2)}` |

**Correction made.**  `G4BalancedRigidity`'s `Grouped` docstring and
`DESIGN-2026-09-15-row-balanced.md` read the criterion as `w ≳ 2 log H/log dmin` ("a vanishing
fraction of `H`", so `H/w` free blocks).  That drops the family factor `|𝓕|`, which at the
schedule is `(2dmax+1)^{2E} = dmin^{4E}`.  Both prose passages rewritten.  Verdict unchanged.

### Laps 167–168 — the seam, and its non-vacuity (`e148a7e`, `b14c49d`)

`two_layers_of_dyadic` **proves** `2 ≤ K′` from the capture budget; `cancelled_union_le` and
`grouped_balanced_union_le` **assumed** it.  Now joined:

* `Capture.InsideCapture K K′` — the analytic side of one sampler as one `Prop`;
* `Capture.two_le_of_insideCapture` — the two-layer conclusion with that data existentially
  quantified;
* `Sched.budget_confines` — family at scale `i`, each inside the capture budget and cancelling
  its first `K′` layers ⟹ `≤ L/dmin^{H/2} + (2dmax+1)^{2E}·H·m`, **`2 ≤ K′` derived**;
* `Sched.grouped_budget_confines` — the grouped analogue on `grouped_balanced_union_le`.

Lap 168 removed a fidelity hole: `InsideCapture` first routed through the **dyadic** bound, which
needs `|S| ≥ 1920·2^K·k` primes inside `[T, 2T]` — a dyadic prime *count* mathlib does not have
(only `Nat.bertrand`, one prime).  So the packaged hypothesis was true but its satisfiability
unverified, and a vacuous hypothesis makes the verdict hollow.  Fixed structurally:

* `Capture.two_layers_of_mertens` — the same conclusion from the `|S|`-free Mertens bound
  (`Budget.budget_forces_two_layers` fed by `roughRowVarianceLower_mertens`).  Strict positivity
  of the constant is free: `ε ≥ 3/(T−1) > 0`, so `hsmall` itself forces `(V − 3|S|²/N)/2 > 0`.
* `InsideCapture` re-packaged on it — no upper bound on `p` anywhere.
* `Capture.structural_satisfiable` — every conjunct but the capture inequality is meetable, for
  every `K`, `k` and every `L > 0`.  The move: `Q` is existential too, so taking `Q = 2^K·L`
  makes `scales_satisfiable`'s own `Q < T` deliver the shift-gap bound `|ρ i − ρ j| < T^{k+1}`
  for free, with the shifts enumerated by `finProdFinEquiv`.

### ⚠️ Astra's direction point, checked

The addendum flags that the design's "`K′ ≥ 3K/8` is forced" may read an **upper** bound as a
**lower** bound.  Checked against the kernel, and the direction is right, but state it this way:
what is proved is an implication *from* the capture inequality.  `RoughRowVarianceLower sm c K`
gives `sm K′ ≥ c·2^K·16^{−K′}/15` for every `K′`; `hcap` caps `sm K′ ≤ c·2^{−K/2}`; the two are
compatible only for large `K′`.  The kernel statements are exactly

* `Budget.budget_forces_two_layers` / `Capture.two_le_of_insideCapture` — **`2 ≤ K′`**;
* `RowVariance.three_eighths_of_budget` — **`K′ > (3K − 8)/8`**.

So: the budget forces a *lower* bound on the number of **cancelled** layers.  It says nothing
about an upper bound, and nothing about `K′` being achievable.  Claim only those two.

### Lap 169 — blocking and the entropy certificate (`0188dad`), beyond the authorized scope

New `src/NormalNumbers/G4EntropyBlocks.lean`.  Asked whether the certificate side supplies a
smallest block dimension.  **It does not.**  `Blocks.H₂_le_sum_blocks` (blocking is subadditive —
`FinLaw.H₂_le_sum_H₂_map` at the padded block restrictions, injective exactly because the blocks
partition the coordinates), `Blocks.H₂_restrictCoords_le` (`≤ m` bits per coordinate),
`Blocks.weight_of_good_blocks` (division-free pigeonhole), `Blocks.good_block_weight`.  At E0's
constant `(1/5)·m·H < H₂` with `θ = 1/10`: whatever the block structure, blocks of size one
included, at least a ninth of the atoms lie in blocks whose marginal still carries a tenth of the
maximal rate.

## Inventory

All trust-triple (`[propext, Classical.choice, Quot.sound]`), re-run this lap:
`isNormal_fullRealW`, `isNormal_two_of_schedule_read`, `PowerBase.isNormal_pow`,
`RowVariance.two_layers_of_dyadic`, `cancelled_union_le`, `roughRowVarianceLower_mertens`,
`threshold_exact`, `union_le_of_determining`, `grouped_union_le'`, `Sched.balanced_union_le`,
and all of laps 166–169's new declarations.  No `axiom` declaration anywhere in `src/`.

`src/` `sorry`s: exactly two, unchanged, both pre-expedition, off-path, forbidden-drift —
`MahlerDriftOne.exists_prime_nonresidue` (a prime in `(p/3, p/2)` with prescribed Legendre
symbol: character sums over primes at `x ≈ q`, Linnik strength) and
`PrimeLambertOscillation.phaseOscillation` (the open analytic obligation of an *open*
irrationality problem; `irrational_primeLambert` is explicitly labelled sorry-gated).  Neither is
in a headline cone.  Per the addendum these are **not** to be filed as stuck strikes — the
repo-wide sorry-free gate is a known tooling defect and the operator stops the run.

## Uncommitted edits

None.  Tree clean at `0188dad`.

## If a successor is ever authorized

Recorded in `PENDING_WORK.md` (ACTIVE section), not started: the honest residue is
**counting-side only** — a sampler whose blocks carry fewer than `8E + 5` atoms is excluded by
nothing proved.  Not by the counting bound (vacuous there, and tight: lap 164's probe shows the
`skel` count is loose only by a constant factor *in the exponent*), not by the error budget
(`InsideCapture` constrains the row second moment, not how atoms are grouped), not by the entropy
certificate (lap 169).  The transport identity is atom-local, so nothing visible forbids blocks
of size one.  Deciding that is where a future escape would live.

Nothing in laps 166–169 is a claim about the normality of `G₄`.
