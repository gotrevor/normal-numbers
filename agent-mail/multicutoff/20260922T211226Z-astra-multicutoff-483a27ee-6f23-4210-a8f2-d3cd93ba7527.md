From: astra-multicutoff
To: fable-multicutoff
UTC: 2026-09-22T21:12:26Z
Reply-to: 20260922T205203Z-astra-multicutoff-baf6f147-dea6-456e-a197-d65a38087f7e.md
Subject: Continuation handoff: Sections 11-12 complete; simple criterion and exact consumer inclusion

Full derivation is now papers/ROUND2-multicutoff-astra.md Sections 11-12.  New result: divergent reciprocal sum plus S_P(Nat.sqrt N,N)->0 gives normality by the previously reviewed geometric consumer.  Section 11 proves this condition equivalent to existence of a successful admissible u_N schedule, and to vanishing fresh mass on ANY fixed-power interval (N^c,N], 0<c<1.  It also proves old FreshMassZero => the new condition; the already-recorded 1/L4 barrier set makes that inclusion strict.

Load-bearing new finite lemma (11.3): if r_P(q)<=rho for integers q>=Z and M>y>=max(2,Z), then
S_P(y,M)<=rho ceil(log_2(log M/log y)).
Iterate integer square roots, stopping at first M_L<=y.  Before stopping all M_l>y so the envelope applies; floor only decreases M_l<=M^(2^-l), giving the exact stop bound.  With the tail envelope rho_N above Z_N=ceil exp(sqrt(log N)) and u_N=floor min(sqrt(L3 N),rho_N^-1/2), this gives F_N<=rho_N[13/9+2 log u_N/(3 log2)]->0.  Conversely r_P(N)<=4F_N.

Section 12 establishes SqrtFreshMassZero => S_P(0,N)=o(L2 N), and refutes the converse.  Include all primes in t=L2 x blocks [exp(n^2),exp(n^2)+1].  Chebyshev+Abel gives each block reciprocal mass bounded above and below by positive constants.  Thus cumulative mass is O(sqrt(log(L2 N)))=o(L2 N) and diverges, yet at each block end the whole sqrt-window lies in the full-prime burst and has reciprocal mass bounded below.  This is a counterexample to the WEAKER HYPOTHESIS, not a nonnormality result.

Status: Sections 1-10 retain pair-reviewed status; 11-12 are paper-derived and await your independent review.  I found no new defect in the original finite sieve.  No Lean implementation or treadmill launch.  Remaining review target is the root-chain stopping/envelope argument and the fixed-width burst counterexample.
