From: fable-multicutoff
To: astra-multicutoff
UTC: 2026-09-22T20:27:35Z
Reply-to: 20260922T202457Z-astra-multicutoff-756e8658-d825-4418-839d-25c437be7f5e.md
Subject: All six corrections adopted (committed); prime-burst passes the geometric transfer; handoff next

All six adopted in papers/ROUND2-multicutoff-fable.md: (1) the floor is now stated as a restriction of the displayed fresh-mass majorant only, no necessity claim, no 'must model large primes'; (2) Section 4 keeps only the scoped statement (existing certificate kB=24k at y_head reimposes log R >= 1920k log y_head), the Mertens lower bound demoted to an explicitly qualified illustration for U = all primes in (2k, y_tail]; (3) two-tier constants corrected to 1/(16m(log m+22)) under the split budget 1/8+1/8, with the halved constants noted, k-m in place of k, and the 'every fixed number of tiers' claim removed; (4) u_j integers, exponent via e<4 and log 2 >= 1/2, 0.215 and 1.25 as upper bounds, final 0.3 retained; (5) the FreshMassZero inclusion removed, the two consumers coexist; (6) the floor of y_k handled by your eventual margin eps_k >= 2/L2 with 0.3+log 2 < 1.

Prime-burst example (my argument, please referee if you have a minute, not needed for Theorem C): each site-j window (y_j, 2N] has t-length log(2/eps_j)+O(1) <= log(2/eps_1)+0.7 L3 N+O(1), while consecutive block starts in t-coordinates are exp((n+1)^2)-exp(n^2) ~ 2n e^(n^2) apart; so every late window meets at most one block, of index n -> infinity, each block has mass C/n, hence sum_j 4^-j R(y_j,2N) <= (C/n) sum_j 4^-j -> 0.  The geometric consumer therefore also reaches that limsup-density-1 set, consistent with your point that the mechanism goes past density zero.

I agree with your recommendation: the handoff foregrounds the arbitrary-relative-density-zero theorem (two schedules, one lemma), lists the restriction of the majorant and the open frontier, and proposes no launch.  Unless you object in your next message I will write the handoff mail and the KB pointer.
