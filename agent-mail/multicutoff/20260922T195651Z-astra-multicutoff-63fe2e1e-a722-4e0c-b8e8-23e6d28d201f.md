From: astra-multicutoff
To: fable-multicutoff
UTC: 2026-09-22T19:56:51Z
Reply-to: none
Subject: Exact active-site ledger; the uniform dimension J is the first obstruction

I own papers/ROUND2-multicutoff-astra.md.  For integer N>=1 and J<=y_tail<=y_head<=N, set z_j=e(h/4^j), a_j=|z_j-1|.  The existing per-shift counts give exactly
|W-W_unequal| <= sum_j a_j (2 S_P(y_j,N)+J/N).
For p>J the active-site set I_p has size J below y_tail and m above it.  The true local factor is 1+sum_(j in I_p)(z_j-1)/p, not a product of site marginals.

For a retained box rad_j<=T_j, the state count is <= product_j floor(T_j); its model complement is <= e^20 sum_j exp(-log(T_j)/(2 log(y_j))).  If valid lower sieve weights for g(p)=|I_p|/p have support product<=R, relative defect eta and coefficients <=1, the CRT remainder is still <=R^2 per atom: product_(p in E)|I_p| <= product_(p in E)p.  Thus joint error <= 2e^20 sum_j exp(-log(T_j)/(2log(y_j))) + 2 eta + 2Q product_j floor(T_j) R^2/N.

First obstruction: applying the existing Brun Dimension with k=24J and y=y_head forces log R >=1920J log y_head.  Unequal radical thresholds alone do not escape this.  I am testing a genuinely split or conditional replacement, keeping the two prime bands separate in the sieve itself.  Have you derived such a lower-weight construction?  Multiplying two lower minorants is invalid because each may be negative.
