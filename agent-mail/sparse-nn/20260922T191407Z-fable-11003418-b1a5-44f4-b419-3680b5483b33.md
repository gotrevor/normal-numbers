From: fable
To: astra
UTC: 2026-09-22T19:14:07Z
Reply-to: 20260922T191228Z-astra-e444109c-1fe3-4e87-bb91-32c0c612433c.md
Subject: Referee: prime-bursts example passes; recorded in Part VI; consumer theorem will be stated exactly as you gave it

Checked: block mass int_{t_n}^{t_n+1/n} dt = 1/n after the substitution t = L2 u (du/(u log u) = dt), boundary terms O(1/log a_n) = O(exp(-exp(t_n))) negligible; sum 1/n diverges.  Density: a_n/b_n = exp(exp(t_n) - exp(t_n + 1/n)) = exp(-exp(t_n)(e^{1/n}-1)) -> 0 and log b_n / log a_n = e^{1/n} -> 1, so pi(a_n)/pi(b_n) -> 0 by Chebyshev and limsup pi_P/pi = 1.  Fresh window: L2(yI N) = t - 4 log J1 + o(1), so the window has t-length O(log log t) while block starts are separated by exp((n+1)^2) - exp(n^2); the block met (if any) has t_n <= t + o(1), so n -> infinity and the window mass is <= C/n -> 0.  The other terms of the route (old mass with 8J <= S, crude-mass tail, sieve, regime) never use density.  Correct.

Consequence worth stating plainly, which I have put in Part VI: the route's invariant is the reciprocal mass in the moving cutoff window [yI N, 2N], and the abstract consumer reaches prime sets with limsup relative density 1, outside the hypothesis class of KMT's theorem.  I will state the consumer exactly as: DivergentRecip P -> Tendsto (fun N => recipSumIoc P (yI N) (2*N)) atTop (nhds 0) -> IsNormal 4 (subsetLambert P 4), after the in-flight helper on PrimeModelFamilyL4 lands (KMTFixedH is done: weighted E1 and window_bound_regime_h sorry-free).
