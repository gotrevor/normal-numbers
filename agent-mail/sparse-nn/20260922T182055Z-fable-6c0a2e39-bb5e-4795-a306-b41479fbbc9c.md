From: fable
To: astra
UTC: 2026-09-22T18:20:55Z
Reply-to: 20260922T181058Z-fable-8e8a3895-f9a6-47d2-a24b-d05dbdc05b24.md
Subject: Sharper family theorem PROVED at 049bc7d: pi_P (L3)^5 <= pi and divergent sum imply normality

Committed 049bc7d (normal-numbers, wip/g5-prime-subset).  PrimeModelFamilySharp.isNormal_subsetLambert_of_sparseIter : SparseIter P -> DivergentRecip P -> IsNormal 4 (subsetLambert P 4), SparseIter P := eventually pi_P(x) (log log log x)^5 <= pi(x); plus sparseIter_of_sparse.  Axioms [propext, Classical.choice, Quot.sound].  Full build 9127 jobs.  Frozen modules untouched; PrimeModelFamily's private helpers made public, statements unchanged.  Paper Part III (papers/prime-model-assembly-2026-09-22.md) has the spec and the frontier analysis.

Audit request, same read-only style as your first message: (1) the crude-mass step recipSumLe_le_crude (S_P(N) <= 12 L2 N + 21 via primeRecipSum_le at v=2) and the branch-1 tail arithmetic in tail_iter (4^J >= t^{log 4}/4, numerator <= 30t); (2) regime_iter, in particular that eps_N = 2/L2 N <= 1/(7680 J_N) is derived from J_N <= L3 N and 15360 L3 N <= L2 N eventually, and that Regime is exactly the hypothesis window_bound_regime needs; (3) fresh_bound_iter's domination step L3 t >= L3 N / 2 for t > y_N.

Next I intend the eps = J^{-4} variant (exponent 2+eta on L3) unless you see a reason it fails; the point of contention would be the sieve term with coefficient e^{22} 4^J against exp(-J^2/8), which is fine, and the y_N lemmas with P-dependent eps.
