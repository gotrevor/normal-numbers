From: fable
To: astra, incoming reviewers (sparse-nn lane)
UTC: 2026-09-22T19:28:21Z
Reply-to: none
Subject: FINAL HANDOFF (sparse-nn, fable): headline declarations, hypothesis list, obstruction example

Branch wip/g5-prime-subset of /Users/gotrevor/src/normal-numbers; mathematics complete at ab71135 (HEAD 4fccc01 adds mailbox commits).  Full `lake build` green (9133 jobs).  Every declaration below depends only on [propext, Classical.choice, Quot.sound] (checked with #print axioms 2026-09-22).  Frozen modules G4WiringSparse, PrimeModelBrunCount, PrimeModelRadicalState are byte-identical to 7bb2159.  I am closing mailbox duty with this message; audit findings go to the handoff file's readers (next fable session), not to me.

HEADLINE DECLARATIONS (exact names; P : ℕ → Prop with [DecidablePred P] throughout; DivergentRecip P := ¬ Summable (fun p => if p.Prime ∧ P p then 1/p else 0); IsNormal 4 (subsetLambert P 4) is the frozen normality target)

1. NormalNumbers.PrimeModel.KMT.KMT_quant₂_primeModel : KMT_quant₂ C₁ C₂
   with C₁ k = Real.exp (4*k), C₂ k = Real.exp (Real.exp (k+7)).  The literal frozen statement (G4WiringSparse), uniform in S and h.
2. NormalNumbers.PrimeModel.KMT.exists_sparse_normal_unconditional :
   ∃ (S : ℕ → Prop) (_ : DecidablePred S), DivergentRecip S ∧ IsNormal 4 (subsetLambert S 4).
3. NormalNumbers.PrimeModel.Family.isNormal_subsetLambert_of_sparse (hS : Sparse P) (hP : DivergentRecip P) : IsNormal 4 (subsetLambert P 4),
   Sparse P := ∀ᶠ x in atTop, (piP P x : ℝ) * log (log x) ≤ x.primesBelow.card, piP P x := (x.primesBelow.filter P).card.
4. NormalNumbers.PrimeModel.FamilyIter.isNormal_subsetLambert_of_sparseIterPow {β : ℝ} (hβ : 2 < β) (hS : SparseIterPow P β) (hP : DivergentRecip P) : IsNormal 4 (subsetLambert P 4),
   SparseIterPow P β := ∀ᶠ x, (piP P x : ℝ) * (L3 x)^β ≤ x.primesBelow.card, L3 x = log log log x.  (Also the exponent-5 and exponent-3 instances isNormal_subsetLambert_of_sparseIter / _sparseIter3.)
5. NormalNumbers.PrimeModel.KMT.windowMean_sub_windowMeanLe_le_h (y k) (h : ℤ) (x) (hx : 1 ≤ x) :
   ‖windowMeanS S k h x - windowMeanLe S y k h x‖ ≤ (4π|h|/3) * (2 * recipSumIoc S y x + k/x)        [Astra's phase-weighted E1]
   and window_bound_regime_h (h) (hntw : NontrivialWindow k h) (hR : Regime x k ε) :
   ‖windowMeanS S k h x‖ ≤ (4π|h|/3)(2 recipSumIoc S (yOf x ε) x + k/x) + exp(3k) exp(-recipSumLe S (yOf x ε)) + (2k² + 2k e^20 + 4 + 2·4^k) exp(-1/(8k²ε)).
6. NormalNumbers.PrimeModel.FamilyIter.isNormal_subsetLambert_of_sparseL4o (hS : SparseL4o P) (hP : DivergentRecip P) : IsNormal 4 (subsetLambert P 4),
   SparseL4o P := Tendsto (fun x => (piP P x : ℝ) / x.primesBelow.card * L4 x) atTop (𝓝 0), L4 x = log log log log x.
7. NormalNumbers.PrimeModel.FamilyIter.isNormal_subsetLambert_of_freshMassZero (hF : FreshMassZero P) (hP : DivergentRecip P) : IsNormal 4 (subsetLambert P 4),
   FreshMassZero P := Tendsto (fun N => recipSumIoc P (yI N) (2*N)) atTop (𝓝 0), yI N = ⌊N^(1/(⌊L3 N⌋₊)^4)⌋₊.
   Corollary direction: freshMassZero_of_sparseL4o : SparseL4o P → FreshMassZero P.  This is the route's actual invariant; no density hypothesis.

HYPOTHESIS LIST (what is assumed, and where)
- Frozen inputs consumed: KMT_along / TailOK wiring (isNormal_subsetLambert_of_KMT_along), tail_error_L1, Brun lower sieve brun_sifted_count_lower, radical-state and lower-atom transfer lemmas — all pre-existing, unchanged.
- Regime x k ε (PrimeModelParameters): 3 ≤ x, 1 ≤ k, 1/log log x < ε, ε ≤ 1/(7680 k).  Supplies k ≤ y, Brun thresholds, y ≥ e².  Every window bound is stated under it; R1 (ε > 1/(7680k)) is trivial via the sieve term ≥ 1.
- NontrivialWindow k h: some site 1 ≤ j ≤ k with h/4^j ∉ ℤ (needed for the model contraction; holds once k ≥ |h|+1).
- Family schedules: J₁ = ⌊L3 N⌋₊, ε = J₁⁻⁴, y = yI N, J = min(J₁, ⌊recipSumLe P y / 8⌋₊).  Divergence is used only through recipSumLe P (yI N) → ∞ and 8J ≤ S; no lower divergence rate anywhere.
- Tail: paid by the universal crude mass recipSumLe P N ≤ 12 L2 N + 21 (no density) in the cap branch; by recipSumIoc P y (2N) ≤ 1 eventually in the mass-limited branch.
- Density enters ONLY via FreshMassZero (item 7); items 3, 4, 6 are dominated-Abel corollaries (PrimeModelDensityMass.recipSumIoc_le_of_dominated', hypotheses y < t ≤ N+1).
- P is an arbitrary decidable predicate; all sums filter for primes; ω counts distinct prime divisors; window sites are n+j+1, j < k, matching windowMeanS.

OBSTRUCTION EXAMPLE (paper-level, refereed by fable; NOT formalised; Astra, mails 20260922T190915Z and 20260922T191228Z; paper Part VI)
- Density-zero divergent set on which the criterion FAILS: with w = L4, F(u) = u/w(u) (0 < F' < 1 for large u), let p_n ∈ P iff ⌊F(n)⌋ − ⌊F(n−1)⌋ = 1.  Then π_P(x)/π(x) ~ 1/L4(x) (relative density zero), S_P(x) ≍ L2 x / L4 x → ∞ (divergent), and uniformly for 1/L2 N < ε < 1/2, recipSumIoc P ⌊N^ε⌋₊ N ≍ log(1/ε)/L4 N.  With ε = J₁⁻⁴, log(1/ε) ~ 4 L4 N, so the fresh mass is bounded below and FreshMassZero P is FALSE.  More generally any schedule using the present old/sieve/tail majorants has 4^J ≫ S_P ⇒ J ≥ c log L2 N, and the sieve forces J²ε → 0 ⇒ log(1/ε) ≥ 2 L4 N − O(1), so the weighted transfer majorant (4π|h|/3)·R is bounded below for each fixed h ≠ 0.
- Meaning, stated precisely: THE CURRENT MAJORANTS FAIL for this set.  This is NOT a statement that subsetLambert P 4 is non-normal for it; normality of that constant is open.
- Converse illustration: prime bursts (all primes in [⌈exp exp e^{n²}⌉, ⌊exp exp(e^{n²} + 1/n)⌋]) have limsup π_P/π = 1, divergent Σ1/p, and FreshMassZero P holds, so item 7 gives normality outside any density-zero class.

NEXT RESEARCH QUESTION (not started; no launch)
Does the four-log boundary reflect actual correlations, or only these majorants?  Work on it is warranted only with (a) a concrete estimate to improve — the candidate is the transfer term: the primes in (y, x] are unmodelled and enter only through their expected hit mass R; a treatment that models them (KMT Prop. 4.3's large-prime machinery) or a head/tail decorrelation estimate whose window need not be ≈ log S_P — and (b) a mechanism that survives the recorded obstruction, i.e. one that does not reduce to R → 0 along any schedule with 4^J ≫ S_P and J²ε → 0.  Nothing proposed here meets (b) yet.

REFS: papers/prime-model-assembly-2026-09-22.md (Parts I–VI), HANDOFF-2026-09-22-kmt-quant2-PROVED.md (addenda 1–4), STATUS.md dated block; commits e222677, 40e1c17, 049bc7d, d03f483, dc0247e, a86a0d8, ab71135.  Modules: PrimeModelPhaseAlgebra, PhaseFactor, JointLaw, Parameters, KMT, KMTFixedH, DensityMass, Family, FamilySharpMass, FamilySharp, FamilyIterMass, FamilyIter, FamilyIterPow, FamilyL4, FamilyConsumer.
