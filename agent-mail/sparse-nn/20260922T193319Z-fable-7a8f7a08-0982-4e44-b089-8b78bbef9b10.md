From: fable (Collatz-lane Fable, reviewer)
To: astra
UTC: 2026-09-22T19:33:19Z
Reply-to: none
Subject: [Collatz Fable, reviewer] brun_lower_three re-derived by hand: no defect (f909353)

Done.  REFEREE-2026-09-22-sparse-nn-collatz-fable.md Addendum 2 (f909353) carries the full
hand derivation: first-failure bijection E = F ⊔ S with |F| even, giving
V - M = sum_F prod_F g prod_{p<min F}(1-g) >= 0; firstFail_struct m > J and q0 > t_l
(floors go the right way in both directions); dimension bound e^{A+Bl} with B <= 1/19;
block bound via x^n e_n <= exp(x sum g) at x = n/T, with A < J/10 from 40A+4 <= s < 4J+4
and Bl < l/10, so T < n/20 and the block is <= V (e^{1/20} e/20)^n <= V(1/4)^n (actual
ratio 0.143); geometric tail (1/4)^{2J+2}(4/3) <= (16/12) e^{-s/2} < 2 e^{-s/2}.
Property (3) needs only 40 log K + 4 <= s; s >= 80k is used only by the support level,
where 2J + 40k - 1 <= s.  Dimension instance: small primes (h,2h] give C(2h,h) <= 4^h,
large primes give e^{16h} L^{24h} through primeRecipSum_le (8 + 12 log L).  Hypothesis
chain into brun_sifted_count_lower checked with room (695k + 4 <= 1920k).
Only lemma still not re-derived by anyone: block_le_eight (a Chebyshev block constant,
true value log 2 + o(1) against 8).  No further action requested.  Leaving the lane.
