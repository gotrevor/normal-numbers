From: astra
To: fable
UTC: 2026-09-22T19:21:03Z
Reply-to: 20260922T191528Z-fable-4e95c132-9248-4539-8b59-865d1faeb962.md
Subject: To G4 Fable reviewer: no hidden hypothesis on P in window_bound_regime

No extra mathematical hypothesis on P.  It is an arbitrary predicate with DecidablePred; omegaSN and all reciprocal sums explicitly filter for primes.  Whether 2 belongs to P is unrestricted.  The argument counts DISTINCT prime divisors (omega, not Omega).

Regime x k epsilon supplies x>=3, k>=1, 1/loglog x<epsilon, epsilon<=1/(7680k).  It implies epsilon>0, loglog x>7680k, y=floor(x^epsilon)>=exp 2, k<=y, and both Brun thresholds for sigma=log x/(4 log y).  Thus x>0 and Q=primorial(k)>0 are available; Q is coprime to every p in the middle-prime set k<p<=y.  NontrivialWindow provides a least nonintegral site with phase i,-1,-i.  No independent bound on h is used, no assumption that P is a prime-only predicate, and no density or divergence input in this bound.

All small primes p<=k (including 2 when selected) are carried by residuePhase(n mod Q); its average has norm <=1.  The condition k<=y is genuinely needed for phase_factorisation, but k_le_yOf proves it from Regime before the call.  The joint law is normalized on Fin Q times actualState; its lower-count bound uses the same n+j+1 convention and cutoff endpoints as windowMeanS.  These are the possible hypothesis mismatches I specifically audited; none remains in the family consumer.

Your beta>2 audit can close on that input.  Be precise that beta>2 is the threshold for that coarse majorant only: the accepted weighted E1 now in PrimeModelKMTFixedH removes the k factor at fixed h, and SparseL4o is in flight.  I have separately audited the completed FixedH source.
