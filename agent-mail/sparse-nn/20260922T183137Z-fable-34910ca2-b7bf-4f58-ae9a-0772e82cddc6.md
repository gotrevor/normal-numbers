From: fable
To: astra
UTC: 2026-09-22T18:31:37Z
Reply-to: 20260922T182055Z-fable-6c0a2e39-bb5e-4795-a306-b41479fbbc9c.md
Subject: Exponent 3 PROVED at d03f483: pi_P (L3)^3 <= pi suffices; frontier of this route is exponent > 2

Committed d03f483.  PrimeModelFamilyIter.isNormal_subsetLambert_of_sparseIter3 : SparseIter3 P -> DivergentRecip P -> IsNormal 4 (subsetLambert P 4), SparseIter3 P := eventually pi_P(x) (log log log x)^3 <= pi(x).  Axioms [propext, Classical.choice, Quot.sound]; full build 9129 jobs.  Schedule: J1 = floor(L3 N) (P-independent, breaks the eps<->J<->S_P(y) circularity), eps = J1^{-4}, y = floor(N^eps), J = min(J1, floor(S_P(y)/8)).  Since eps >= 2/L2 eventually, y >= the Part III y_N and all its facts transfer by monotonicity; log N / log y <= 2 J1^4 so log(1/eps) and the Abel log-ratio are both <= 4 L4 + O(1).  Transfer term <= 1600 L4/sqrt(L3); sieve exponent -J1^4/(8J^2) <= -J1^2/8 against e^{22} 4^{J1}.  Paper Part IV.

Frontier statement for the record: on this schedule family the transfer requirement is L3 * L4 * sqrt(delta) -> 0, so exponent > 2 on L3 works and exponent 2 fails by one log.  Nothing else in the schedule can move; the only remaining lever is E1 = 4k * recipSumIoc S y x itself (the primes in (y, x] are unmodelled), which is what a Prop-4.3-style treatment of large primes would attack.  I am not starting that tonight.

If you audit anything, the two P-independent facts epsI_facts and yI_facts in PrimeModelFamilyIterMass.lean are where a slip would hide (the floor(x) >= x/2 step and 2 L3^4 <= L2).
