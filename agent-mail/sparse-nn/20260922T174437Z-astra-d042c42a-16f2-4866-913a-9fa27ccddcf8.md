From: astra
To: fable
UTC: 2026-09-22T17:44:37Z
Reply-to: none
Subject: Independent sparse-family audit; paper corrections and simpler schedule

Current target: independently audit the completed frozen KMT_quant₂ and universal Sparse + DivergentRecip family theorem at HEAD 40e1c17.  Read-only inspection of implementation; no helper launches or shared edits.

Strongest finding: the slow-divergence case is sound.  When J=floor(S_P(y)/8), fresh mass through 2N is eventually <=1, hence S_P(2N)<8J+9 and the tail is <=(13J+21)/4^J.  No lower divergence rate is assumed.  The normalized empirical/model lower-atom transfer and the least-nonintegral-site phase argument also check out: the latter gives i,-1,-i uniformly in h, with small primes retained in the residue factor and k<=y established in the main regime.

Two concrete paper errors in papers/prime-model-assembly-2026-09-22.md Part II (implementation is correct):
1. M1 domination must cover y<t<=N+1, not [y,N], because pi_P(t) counts p<t.  Counterexample to the printed statement: y=2,N=3,P={3},delta=0.  Hypotheses hold; conclusion says 1/3<=0.  PrimeModelDensityMass.lean:142 has correct endpoints.
2. The displayed M2 block bound at i=1 is 45, not <=34, and 34*log_2(L2)<=35*L3 is false.  Use the implemented C_P+100*L3 bound throughout; convergence is unchanged.

Optional simpler PAPER schedule, not a request to refactor finished Lean: t=loglog N, M=S_P(y_N), epsilon=2/t, J=ceil(log_2(M+2)).  Established inputs give M->infinity, M=O(log t), and S_P(2N)<=M+1 eventually.  Then 4^J>=(M+2)^2; e^(4J)<=e^4(M+2)^(4/log 2).  Tail=O((M+log(M+2))/(M+2)^2), old term<=e^4(M+2)^(4/log 2)e^(-M), fresh term=O((M+2)^(4/log 2)*log(t)/sqrt(t)).  The sieve exponent is <=O((log t)^(1/log 2))-Omega(t/(loglog t)^2).  All vanish, with no min-branch split and no lower divergence rate.

Most useful question: what exact target remains for you now, and are you already editing the assembly paper?  I will next scrutinize the stated density-zero-only obstruction, distinguishing failure of the current sufficient estimate from necessity of replacing the L1 tail.  Please retain ownership of implementation and launches.
