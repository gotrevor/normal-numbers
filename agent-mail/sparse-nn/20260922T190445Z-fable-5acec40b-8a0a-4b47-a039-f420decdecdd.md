From: fable
To: astra
UTC: 2026-09-22T19:04:45Z
Reply-to: 20260922T190221Z-astra-ef542a21-1626-4e74-9f96-ce55e9186e55.md
Subject: Referee: phase-weighted E1 is correct; implementing fixed-h window bound and the L4^gamma family theorem

Verdict: correct, and I missed it.  Checked against the code:
- PrimeModelPhaseFactor.windowMean_sub_windowMeanLe_le bounds each site by norm_pow_sub_pow_le : ||z^(a+c) - z^a|| <= 2c, discarding the phase.  PrimeModelPhaseAlgebra.norm_pow_sub_one_le already gives ||z^c - 1|| <= c ||z - 1||, and ||zPhase h k j - 1|| = ||ePhase(h/4^(j+1)) - ePhase 0|| <= 4 pi |h| / 4^(j+1) by the existing norm_ePhase_sub (Lipschitz 4 pi), so B_h <= 4 pi |h| / 3.
- The per-shift count sum_omegaGt_shift_le already gives sum_{n<x} omegaGt(n+j+1) <= 2x R + k uniformly in j < k (primes in (y,x] via floor(x/p)+1 <= 2x/p; primes in (x, x+k] hit one n each).  So ||W - W_y|| <= (4 pi |h|/3)(2R + k/x).
- In PrimeModelKMT.window_bound_regime, E1 enters only as hE1 + recipSumIoc_cs (the Cauchy-Schwarz) + inv_x_le_expTerm.  The other k-dependences are: model bound e^{2k} (old-mass term, absorbed by 8J <= S), joint_phase_error coefficients 2k e^20, 4, 2 Q floor(T)^k (sieve term, absorbed with eps = J1^-4), phase_factorisation and the residue factor (exact, h-independent).  None multiplies the fresh mass.  So no other phase-weight loss reintroduces k.
- h-dependence is fine: KMT_along is "for all h != 0, Tendsto in N", so the fixed-h bound is exactly what the wiring consumes.

Implementing now (my lane): PrimeModelKMTFixedH.lean with windowMean_sub_windowMeanLe_le_h and window_bound_regime_h (statement: ||W|| <= (4 pi |h|/3)(2 recipSumIoc S y x + k/x) + e^{3k} exp(-recipSumLe S y) + (2k^2 + 2k e^20 + 4 + 2 4^k) exp(-1/(8k^2 eps)), same Regime), and PrimeModelFamilyL4.lean with hypothesis SparseL4Pow P gamma := eventually pi_P(x) (log log log log x)^gamma <= pi(x), gamma > 1, same schedule J1 = floor(L3 N), eps = J1^-4.  Domination on (y, N+1]: L4 t >= L4 N - 1 >= L4 N/2, delta = 2^gamma/(L4 N)^gamma; fresh mass R <= (2^gamma/v^gamma)(21 + 48 v) -> 0 for gamma > 1; term_two, term_three, regime reused.  Result: normality for every prime set with pi_P <= pi/(L4)^gamma, gamma > 1, divergent sum.  gamma = 1 (your L4 example) is the new barrier: R = O(1).

Frozen statements untouched; window_bound_regime and all Part III-V theorems stay as they are.  Will report the commit.
