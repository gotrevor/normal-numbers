# HANDOFF 2026-09-14 — G4B lap 15 (confirming lap): campaign objective re-verified, `box stuck` strike 2

Branch `wip/g4-disjunctivity`, HEAD `3d6f2fa` + this commit.  No proof edits this lap.

## What was re-verified from scratch this lap

* `lake build` — **8930 jobs, green**.
* `#print axioms` = `[propext, Classical.choice, Quot.sound]` for all of:
  `NormalNumbers.G4.isDisjunctive_base`, `isDisjunctive_four`, `isDisjunctive_two`,
  `isDisjunctive_primeSum`, `every_word_occurs_base`, `isDisjunctive_root`.
* `grep -rn sorry src/NormalNumbers/*.lean` → exactly two live `sorry`s, both on
  `DIRECTION.md`'s forbidden-drift list: `MahlerDriftOne.exists_prime_nonresidue` (line 380)
  and `PrimeLambertOscillation.phaseOscillation` (line 95).  Neither is on the G4/G4B cone.
* No `axiom` declaration anywhere in `src/`.

## Note on the kickoff

`KICKOFF-2026-09-14-g4-disjunctivity.md` states a *lap-1* objective (pin the endpoint, name the
A–E graph, prove the conditional contradiction).  That objective and the whole campaign after it
(laps 1–14, `HANDOFF-2026-09-14-g4-lap{1..14}.md`) were completed before this lap: the base-four
headline landed at `b937072`, the base-`b ≥ 3` theorem at `20c43d8`.  Base `b = 2` is refuted
on this route (`rowL1 2 K = 1`, see `PENDING_WORK.md`).

## Stop gate

`DIRECTION.md`'s CURRENT DIRECTIVE (G4B) is met.  The repo-wide sorry gate refuses `box done`
solely because of the two forbidden-drift sorries above, which no grind lap may touch.  Lap 14
filed `box stuck` strike 1; this lap is the confirming lap and files strike 2.  The operator's
options: a new attended objective, or a scoped run
(`--done-when 'sorry-free:src/NormalNumbers/G4SchedBAssembly.lean'`).
