# HANDOFF 2026-09-14 — G4 disjunctivity, lap 10 (DEEP REFLECTION + the `B` inequality proved)

Branch `wip/g4-disjunctivity`.  Build 🟢 `lake build` **8923 jobs** — and that number now MEANS
something for this campaign, see "Build gate" below.  Every headline prints
`[propext, Classical.choice, Quot.sound]`.

## This was the altitude lap — read `PENDING_WORK.md` §Reflection 2026-09-14 first

**ROUTE VERDICT: CONTINUE.**  No registered trigger fired.  G-T3 is now the live one (A–D are
discharged) and it did NOT fire: all five `ScheduleWitness` inequalities were re-derived from
the **Lean definitions** this lap and they close.  Full synthesis in `PENDING_WORK.md`;
binding orders in `DIRECTION.md` → CURRENT DIRECTIVE.

### Two recorded §5 parameter values REFUTED (lap 9b's paper check was wrong twice)

* **`lam = 1` makes `hbudget` FALSE.**  `smallPrimeBound`'s third error term is
  `2(2e/lam)^{Mc}∏_{p∈sm}(1+e^{lam}T/p)`; at `lam = 1` the first factor is `exp(+1.69 Mc)`, so
  the term is `exp(+Θ(TL)) → ∞`.  Need `lam > 2e` and `C > e^{lam}/log(lam/(2e))` in
  `Mc ≍ C·T·L`; that minimises near `lam ≈ 6.4` at `≈ 3690`.  **Use `lam = 13/2`, `lam' = e`,
  `Mc = ⌈10⁴·T·L⌉`.**
* **`N ≈ 10K log K` makes `hfar` FALSE.**  `farC = log((X+Dm)/|P|) + log(log(X+Dm)+1)`; the
  second term is `≈ log log X = L`, which lap 9b dropped.  The far bound is then
  `2^K L^{0.86} → ∞`.  **Use the brief's `J = ⌈3 log₂ L⌉`** (`N = J − K = Θ(log L)`), which is
  also what `farAvg_le`'s own docstring assumes.

### New structural finding — `K` is squeezed from BOTH sides

`0.58 log log L ≲ K ≲ log L/(2 log log L)`.  Upper edge = `schedule_budget` (C4).  **Lower edge
(stated nowhere before)**: C forces `R = X^{1/(20Mc)}` with `Mc ≳ TL`, so `log Mc ≥ log L`, and
D's medium range pays `0.52·8^{−K/2}√(log Mc)` against `εη = 2^{−K/4}/K`, i.e.
`2^{1.25K} ≳ K√(log L)`.  **With `K` frozen that term diverges** — that, not the budget, is the
mechanism behind the brief's "do not freeze `K`".  Do not "simplify" `K` downward.

## Proved this lap — `src/NormalNumbers/G4ScheduleB.lean` (new, trust triple)

**`gridB_bound`** and its interface form **`gridParams_hB`**: the `hB` field of
`ScheduleWitness`, with `δ₁ = 1/8` and `ε = 1/K`, holds for every `GridParams` with `s = K²`
as soon as

    K ≥ 33856·ℓ²·16^ℓ = (184·ℓ·4^ℓ)²,   K ≤ 8ℓM ≤ K + 8ℓ,   η⁴ ≤ 2^{−K},
    Lg ≤ r(log 2 + 23√K).

This is the first of the five §5 inequalities to be discharged, and it is the one the brief
calls "a central formalization target, not routine bookkeeping".  It mentions no `X`, no
primes and no progression.

Supporting lemmas (all trust triple):
* `log_four_pow_sub_one_le` — `log(4^ℓ−1) ≤ 2ℓ log 2 − 4^{−ℓ}`: the omitted word's covering
  deficit, the entire reason the tube volume tends to zero.
* `hDim_le_exp_mul_rDim` — `H = (K²+1)^K ≤ e^{1/K}·K^{2K}`; `rDim_le_hDim` — `r ≤ H`.
* `exp_inv_mul_le` — `e^{1/K}(1−1/K) ≤ 1`, whence `(e^{1/K}−1)(K/4) ≤ 1/2`.
* `shape_factor_le` — `√(2πe/g)·√(H+g) ≤ 7` (the Gaussian-comparison factor is an absolute
  constant because `H/r → 1` and `g/r → 1`).
* `deficit_dominates` — `K ≥ (184ℓ4^ℓ)²` ⟹ `K·4^{−ℓ}/(8ℓ) ≥ 11.5√K + 2.8ℓ + 5.66`.
* `log_seven_le_two`.

Per unit of `r`, the log-scale ledger the proof closes is
`(e^{1/K}−1)(K/4)log2 [≤ log2/2] + e^{1/K}2ℓ log2 [≤ 2.8ℓ] − K4^{−ℓ}/(8ℓ) + log2/4 + log2/2
 + 11.5√K + log 7 [≤2] + 4 log 2 ≤ 0`.

## Build gate — FIXED this lap (was a real hole)

`src/NormalNumbers.lean` did **not** import any `G4*` module, so the pre-commit `lake build`
(8888 jobs) never compiled the G4 wing; earlier handoffs' "8888 green" did not gate this
campaign.  All 33 G4 modules are now imported by the root, and `lake build` is 8923 jobs green.
(`CFChainFreq`, `PrimeLambert*`, `StonehamBoundary*`, `DiagonalRigidity`, `HotSpot`, `Pillai`,
`KhinchinDefs`, `LiteratureBMStrong`, `CFLogTail` remain outside the root — untouched here.)

## Resume here — the §5 order set by the directive

1. **`gridParams_of_KN`** — an explicit `GridParams` from `(K, N)`: `B = s·J + 1`,
   `U ≥ max gridU`, **`Q = U!`** (`Nat.dvd_factorial` gives `hQdvd` free; no `lcm` needed),
   `D₀ ≥ max gridV`, plus `Dm ≥ every d_α`, `Mx`, and an explicit **`log P₀` bound**
   (`log P₀ = log Mprod + log freezeQ`, dominated by `T²·log(J·Q·D₀)`; `L^{0.07+o(1)}`).
   Gates 2–4.
2. **`card_apSample`** lower bound `(X:ℝ)/P₀ − 1 ≤ card` — gates `hne`, `hbig`, `hfar`,
   `hbudget`.
3. **`hfar`** (needs 1 + 2 + `J = ⌈3 log₂ L⌉`), then **`hbig`** (mind the two-sided `K`
   window), then **`hbudget`** (`schedule_budget` + the four `smallPrimeBound` error terms at
   `lam = 13/2`, `lam' = e`, `Mc = ⌈10⁴TL⌉`).
4. Assemble `Nonempty (ScheduleWitness ℓ w)` eventually in `X`, then
   `separatingFrameExists_of_witness` ⟹ `isDisjunctive_four_of_witness` unconditional.

`isDisjunctive_four_of_witness` / `…_of_frames` stay CONDITIONAL and are never the endpoint.
