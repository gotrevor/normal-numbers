# Handoff: W1 campaign complete — CF cylinder toolkit fully proved

**Date**: 2026-08-23 15:29 UTC · **Branch**: `master` · **HEAD**: `e5386f6`

## 🎯 What we're doing
Expedition B5′ (`KHINCHIN.md`): one witness real that is absolutely normal +
CF-normal + Khinchin-typical. This session ran the whole W1 work package:
the 12 frozen statements in `src/NormalNumbers/CFCylinder.lean`
(Becher–Yuhjtman §1.1 continuant/cylinder algebra). **All 12 are now proved.**
`box done --green` was signalled; the treadmill is stopping. The next
campaign is W2 (Gauss-measure comparison), then W3 (mixing/Chebyshev).

## 🧠 Context to carry forward
- **Statement discipline held**: none of the 12 frozen statements was
  weakened; all extra machinery is `private` lemmas in the same file.
- **`cfK_append` (Euler gluing)** went by list induction with `cfK.induct`
  (cases `[]`/`[a]`/`a::b::l`), *not* B–Y's α_{r,s} combinatorics — as
  planned in the campaign brief, and it worked on the first serious attempt.
- **`volume_cfCylinder` route** (the crux, ~300 lines): endpoints are
  `cfVal w` and `cfVal (bumpLast w)` (`bumpLast` = increment last digit —
  deliberately avoids `cfP []`, which is junk `1` not `0`, making `cylMap`
  wrong for singletons; don't "fix" that, just avoid `cfP` off nonempty
  drops). Length by ℚ-algebra induction (`abs_cfVal_sub_bumpLast`), no
  separate ± determinant identity needed. Set-level: `cfCylinder w ⊆ uIcc`,
  and `uIoo \ ℚ ⊆ cfCylinder w` (irrationals survive the Gauss map;
  `Set.uIoo` unordered intervals kill all parity/orientation case splits).
  Squeeze with `measure_mono` — valid for arbitrary sets, so **no
  measurability of the cylinder is ever proved or needed**.
- **Distortion pair**: plain quasi-multiplicativity (`K(wu) ≤ 2K(w)K(u)`)
  is a factor of 2 too lossy for the lower half. The sharp tool is
  `K(wu) ≤ (K(w)+K(w⁻))·K(u)` straight from gluing + drop-monotonicity;
  same shape for `w ++ u.dropLast` (case-split `u.dropLast = []`).
- Abandoned: nothing of note; no dead ends survived more than one compile
  cycle.

## ✅ State
- `lake build` green (observed; pre-commit hook re-verified at every commit).
- `src/` sorry-free; the only `grep sorry` hit in `CFCylinder.lean` is a
  docstring word.
- `#print axioms` on all 12 headline lemmas: exactly
  `propext`/`Classical.choice`/`Quot.sound` (observed this session).
- Commits: `8fa056f` (algebra 9/12) → `3eb475a` (crux volume formula) →
  `f76a1b1` (distortion pair, 12/12) → `e5386f6` (HANDOFF refresh).
  Not pushed (no egress; host pushes).
- `HANDOFF.md` (repo master) rewritten as the W1 completion record; the
  campaign brief is archived at `archive/handoff/HANDOFF-2026-08-23-W1-campaign-brief.md`.
- Stop sentinel written (`box done --green`), host will verify and halt.

## 🎬 Next actions
1. Read `KHINCHIN.md` §W2 and stage the W2 scaffold: Gauss-measure vs
   Lebesgue comparability on cylinders (`gaussMeasure` is already defined in
   `CFDefs.lean`; the density is trapped in `[1/(2 log 2), 1/log 2]`).
2. Then W3 setup: the mixing/Chebyshev argument on top of
   `tailDensity_mem_Icc` + the distortion pair — decide statement shapes and
   freeze them with kernel-checked anchors, per the planted-scaffold
   doctrine.

## ⚠️ Gotchas
- `cfP []` = 1 is junk (true `p₀` = 0): `cylMap [a]` is wrong; use
  `bumpLast`-style endpoints instead of `cfP w.dropLast` for one-digit words.
- `ring` cannot distribute `⁻¹` over products; use
  `div_mul_div_comm`/`div_le_div_iff₀` (bit us twice).
- Revert `hpos`/`hw` before `induction w using cfK.induct` — IHs then carry
  the hypotheses.
- `ℝ≥0∞` notation needs `open ENNReal`; `CFCylinder.lean` writes
  `(2 : ENNReal)` instead.
- mathlib names in this pin: `Real.volume_interval` (uIcc), `Real.volume_uIoo`,
  `measure_sdiff_null`, `natCast_floor_eq_intCast_floor`, `inv_anti₀`,
  `lt_inv_comm₀`, `Irrational.sub_intCast`.

## 📁 Key files
- `src/NormalNumbers/CFCylinder.lean` — the finished W1 toolkit (12 theorems + private scaffolding)
- `src/NormalNumbers/CFDefs.lean` — Gauss map / digits / cylinders / `gaussMeasure` / `tailDensity`
- `KHINCHIN.md` — the B5′ plan, W1–W6
- `PENDING_WORK.md` — per-lemma record of the three laps
- `papers/becher-yuhjtman-2019-abs-normal-cf-normal.md` — dependency map of the source paper

---
**→ Next session: this is your starting point. Don't summarize this doc back to
Trevor or wait for instructions, and don't offer other projects from the KB - this
doc IS the chosen thread. Absorb the context above, then pick up at "Next actions"
#1: read `KHINCHIN.md` §W2 and stage the W2 scaffold.**
