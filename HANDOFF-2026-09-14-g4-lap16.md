# HANDOFF 2026-09-14 — lap 16 (FRESH-MIND REVIEW): corollaries landed, base two RETIRED, campaign G5 set

Branch `wip/g4-disjunctivity`, HEAD `f68b40f`.  Working tree **clean**.  `lake build` 8936
jobs **green**.  Three commits this lap, all green and axiom-clean.

## Read this first

`DIRECTION.md` CURRENT DIRECTIVE was rewritten this lap and **outranks this file**.  Laps 14
and 15 were pure re-verification + `box stuck` strikes; a third strike, and any lap whose
deliverable is re-running the audit, are now forbidden drift.  Do not re-audit; build.

## Mathematics PROVED this lap (all `[propext, Classical.choice, Quot.sound]`)

`src/NormalNumbers/DisjunctiveCorollaries.lean` (new, general, any base)
* `IsDisjunctive.irrational` — `x = a/q` makes `q·orbit b x n ∈ ℤ`, so the orbit misses
  `[1/(2|q|), 1/|q|)`, an interval disjunctivity must hit.
* `IsDisjunctive.exists_ge` — every interval is hit **arbitrarily late**: cut `[a,c)` into
  `N+1` disjoint pieces, each is hit, hitting times are distinct, so one is `≥ N`.
* `IsDisjunctive.exists_occursAt_ge` — the word form.

`src/NormalNumbers/G4Irrational.lean` (new, instances of `isDisjunctive_base`)
* **`G4.irrational_primeSum : 3 ≤ b → Irrational (∑' p : Nat.Primes, 1/(bᵖ−1))`** — an
  independent machine-checked proof of the `b ≥ 3` half of the prime-Lambert irrationality
  family (Tao–Teräväinen arXiv 2512.01739 Thm 1.3 is the base-two case), by a different
  route and strictly stronger for those bases.
* `irrational_primeLambertAtBase`, `irrational_primeLambertFour`,
  `every_word_occurs_base_late`, `every_binary_word_occurs_late`.

`src/NormalNumbers/G4RowMassOptimal.lean` (new — the refutation, machine-checked)
* `LineSumsZero μ`, and `two_pow_le_sum_abs`: every nonzero integer array on
  `(Fin K → Fin (s+1))` with vanishing line sums has `∑|μ| ≥ 2^K` (induction: ≥ 2 nonzero
  slices, each `≥ 2^{K−1}`).
* `lineSumsZero_kronPow_diffZ`, `kronPow_diffZ_ne_zero`, `sum_abs_kronPow_diffZ_eq_min` —
  the tensor row attains it: **`D_s^{⊗K}` is L¹-optimal**, the design is not lossy.
* `rowL1_le_rowMass`, `rowL1_two`, **`one_le_rowMass_two`** — every design in the family has
  row mass `≥ rowL1 b K`, which at `b = 2` is `≥ 1`, while §4D needs `≤ δ_big·ε·η < 1`.

## REFUTED: base two, for the whole design family (not just our parameters)

1. A tensor coordinate kills exactly one layer (`ρ_{αj} = j·d_α − t_α` is affine in the atom
   digits, so `α_i` drops out of layer `j` iff `j·D_i = T_i`), so a test array annihilating
   layers `1..K` has vanishing line sums in `K` directions ⇒ `L¹ ≥ 2^K` (proved above).
2. Row mass `= (∑_α|μ|)·∑_{j>K} b^{−j} ≥ 2^K b^{−K}/(b−1) = rowL1 b K`, `= 1` at `b = 2`.
3. No cutoff escapes: medium range needs `Y ≲ √X` (pair error `π(Y)²rowL1²/|P|`), a
   non-cancelling far range costs `rowL1·log(log Mx/log Y) ≳ rowL1·log 2` unless
   `Y ≥ X^{1−o(1)}`.  The vise is closed at `b = 2`, open at `b ≥ 3` only because `rowL1`
   decays.
4. Only repair: **signed** cancellation for `p > Y`, i.e. shifted correlations of
   `1[P⁺(m) > Y]` — Bombieri–Vinogradov / Titchmarsh strength, and exactly the two-point
   correlation input the brief forbids inheriting.  Base two belongs to Tao–Teräväinen.
   The stretch target is **struck**, and `PrimeLambertOscillation.phaseOscillation` is
   therefore an honestly-disclosed wall, not a task.

## Next actions (campaign G5 — see DIRECTION.md for the full order and triggers)

1. **Decisive probe, before any port**: generalise C2 `G4LocalContraction.norm_localSum_le`
   from the indicator model (`p` residues, `k` active roots, default class mass ≥ 1/2) to the
   valuation model: default `v_p = 0`; active `v_p = 1` classes (mass `(1/p)(1−1/p)`, phase
   exactly `xᵢ`); junk `v_p ≥ 2` of mass `≤ k/p²`.  Paper check done: `∑_p k/p² = O(T) =
   L^{0.02+o(1)}` loses to the gain `θ₀L = L^{1−o(1)}`.  If it resists for 2 laps → W-T1:
   drop the interface, instantiate `Ω` directly.
2. Then the weight interface (`w(n) ≤ log₂ n`, `w(dm) = w(d)+w(m) −` defect) bottom-up:
   `PrimeLambertDefs`/`Tail` → `G4Transport` (defect vanishes for completely additive `w`) →
   `G4Remainder`/`G4FarTail`/`G4MediumPrimes` (`E[v_p] = 1/(p−1)`) → `G4SmallPrimeVector`.
3. The `Ω` series identity `∑_n Ω(n)/bⁿ = ∑_{q prime power} 1/(b^q − 1)`.
4. Second target if the port stalls twice: **effective** disjunctivity — replace
   `homit : ∀ m` by `∀ m ≤ M₀` through `G4Wiring`/`G4Covering` to extract the explicit
   first-occurrence bound `N(b, ℓ)` already implicit in the schedule.

`ω` must stay an **instance**: `isDisjunctive_base`, `isDisjunctive_four`, `isDisjunctive_two`
stay green and unweakened.  Scope honesty (W-T4): G5 is past the attended brief's finish line
— `STATUS.md` must keep saying which results are the attended endpoint.

## State of `src/`

Two `sorry`s, both off every unconditional headline, both correctly disclosed and both
deep-analytic: `MahlerDriftOne.exists_prime_nonresidue` (character sum over primes in an
interval `≍ p` with modulus `≍ p` — Burgess + Vinogradov/Karatsuba strength, reaches only a
CONDITIONAL theorem) and `PrimeLambertOscillation.phaseOscillation` (the old base-two route,
refuted above on this repo's route).  No `axiom` declaration anywhere in `src/`.
