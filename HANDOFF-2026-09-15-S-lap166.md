# Handoff — review lap 166: DESIGN §0's **third** escape closed at the schedule; objective S set

**Date**: 2026-09-15 · **Branch**: `wip/g4-entropy` · `lake build` 🟢 **9041 jobs** · tree clean ·
every new declaration `#print axioms`-clean (`[propext, Classical.choice, Quot.sound]`).

## The review call (why lap 165's stuck-bail is refused)

Lap 165 filed a stuck strike on the grounds that "R's checklist is done and the only remaining
`src/` `sorry`s are forbidden-drift".  The first half is true; the second does not follow.
`DESIGN-2026-09-15-deformation.md` **§0** names *three* deformations the `T(K′)` verdict does not
test:

1. a non-coordinatewise ("row-balanced") cancellation — closed, `Sched.balanced_union_le`;
2. a different matrix `A` in place of `D_s^{⊗K}` — closed, `RowVariance.union_le_of_determining`;
3. **giving up the single-`n` joint sample** — reduced to a *generic* size condition by
   `RowVariance.grouped_coeff_le` / `grouped_union_le'`, and **never checked at the schedule**.

Item 3 was open, inside the ACTIVE override's own scope, and route-decisive: if a decoupled
sampler were admissible the confinement `dmin^{−H/2}` evaporates and the whole deformation route
reopens.  This lap settled it.

## What landed — `src/NormalNumbers/G4GroupedVerdict.lean` (new module, one writer)

| declaration | content |
|---|---|
| `Sched.grouped_size_cond` | at the schedule `(2dmax+1)^{2E}·m·H²·dmax ≤ dmin^{4E+3}`, so `grouped_coeff_le`'s hypothesis `|𝓕| m H² dmax ≤ 2 dmin^{w−w/2}` holds once `w ≥ 8E + 5` |
| `Sched.grouped_block_size` | the blocks are disjoint and cover the atoms: `G · w ≤ H` |
| `Sched.grouped_block_count_le` | hence **`8 K G ≤ K² + 1`** — at most `(K²+1)/(8K) ≈ K/8` blocks (`20000` at `i = 0`) |
| `Sched.grouped_balanced_union_le` | the grouped verdict: a family with two row-balanced layers, atoms partitioned into `G` blocks each sharing one sample point, reads below `L` at most `L/dmin^{w/2} + G·(2dmax+1)^{2E}·H·m` — rate `dmin^{−w/2} ≤ dmin^{−(4E+2)}`, the same shape as the single-`n` `balanced_union_le` |

Here `K = KK i`, `s = K²`, `H = (K²+1)^K`, `E = K(K²+1)^{K−1}`, `m = kk i`, `dmax ≤ 2 dmin`,
`8 H m ≤ 2 dmin` (`key_size`).  The arithmetic is `2dmax+1 ≤ dmin²`, `m H dmax ≤ dmin²`,
`H ≤ dmin`, hence `|𝓕| m H² dmax ≤ dmin^{4E}·dmin³`, against `dmin^{w−w/2}` with
`w − w/2 = ⌈w/2⌉ ≥ 4E+3`.

## The correction this forced

`G4BalancedRigidity`'s `Grouped` docstring and `DESIGN-2026-09-15-row-balanced.md`'s
"what is still open" both read the criterion off `grouped_union_card_le` as

> `w ≳ (log G + log H)/log dmin ≈ 2 log H / log dmin` — "enormous absolutely, a vanishing
> fraction of `H`"; "the single-`n` sample is therefore *not* essential".

That **drops the family factor `|𝓕|`**, which at the schedule is `(2dmax+1)^{2E}`, i.e. already
`dmin^{4E}`.  The true threshold is `w ≥ 8E + 5 ≈ 8H/K`, and the number of blocks is capped at
`≈ K/8`, not `H/w`.  Both prose passages are rewritten (`git show` this commit); the design's
verdict is unchanged, only its estimate of *how much* joint sampling the confinement consumes.

## Also checked, and refuted — do not retry

**Sharpening `|𝓕|` with the pairwise-coprimality hypothesis.**  `hcop` forces `d` to be
injective on all `H` atoms (pairwise coprime values `≥ dmin ≥ 9` are distinct), so `|𝓕|` is at
most the number of injections into `[dmin, dmax]`, `≈ (4 H m)^H ≈ dmin^H`.  That is **weaker**
than the `MDF` bound `dmin^{4E} = dmin^{4HK/(K²+1)}` by a factor `≈ K/4` in the exponent.
Coprimality cannot lower the group threshold.

## Inventory (re-run this lap, real `#print axioms`)

Trust triple only: `isNormal_fullRealW`, `isNormal_two_of_schedule_read`,
`PowerBase.isNormal_pow`, `RowVariance.two_layers_of_dyadic`, `cancelled_union_le`,
`roughRowVarianceLower_mertens`, `threshold_exact`, `union_le_of_determining`,
`grouped_union_le'`, `Sched.balanced_union_le`, and all four new declarations.
No `axiom` declaration anywhere in `src/`.

`src/` `sorry`s: exactly two, both pre-expedition, off-path, forbidden-drift, and both genuinely
hard — `MahlerDriftOne.exists_prime_nonresidue` (a prime in `(p/3, p/2)` with prescribed Legendre
symbol: character sums over primes at `x ≈ q`, Linnik strength, unconditionally out of reach and
nowhere near mathlib) and `PrimeLambertOscillation.phaseOscillation` (the open analytic obligation
of an *open* irrationality problem; `irrational_primeLambert` is explicitly labelled sorry-gated,
not a theorem).  Neither is in a headline cone.

## Next lap — in order (binding: `DIRECTION.md` → CURRENT DIRECTIVE, objective S)

1. **The seam.**  `two_layers_of_dyadic` *proves* `2 ≤ K′` from the capture budget;
   `cancelled_union_le` *assumes* it.  Join them at the schedule: one theorem
   `Sched.budget_confines` — a family of samplers at scale `i`, each inside the capture budget,
   reads upper density `≤ dmin^{−H/2}`, with `2 ≤ K′` **derived**.  The work is carrying the
   analytic data (`S, T, N, ρ, w`, the three scale conditions of `roughRowVarianceLower_dyadic`)
   alongside the combinatorial family, and tying `LayersCancelled d t K′` to the `K′` of the
   budget statement.  Then the grouped analogue on top of `grouped_balanced_union_le`.
2. **The residue, sharply posed.**  Below `w = 8E + 5` the *counting* bound is vacuous — not the
   arithmetic, and nothing exhibits an escaping sampler.  It expires exactly where a block
   carries fewer atoms than `skel` needs (`|skel| ≤ E`, threshold `8E`).  Decide it from the
   **certificate** side: the smallest block dimension at which the E0 entropy/volume saving still
   certifies a block's windows.  Frozen `Prop` first; an explicit obstruction is a valid outcome.

Nothing in this lap is a claim about the normality of `G₄`.
