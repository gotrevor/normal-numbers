# Handoff: the offset 2-cycle — closed-form `p²/6` for `p ≡ 7 (mod 12)`

**Date**: 2026-09-08 05:28 UTC · **Branch**: `wip/adder-tower-c9` · **HEAD**: `c07628f` · tree clean
(untracked host files `docs/mahler-universal-constant-is-one-2026-09-07.md`,
`experiments/mahler_delta_star*.py`, `scratch/` are not mine; leave them).

## 🎯 What we're doing
`DIRECTION.md` CURRENT DIRECTIVE (2026-09-08 fresh-mind review): pin `M(p,1)`; THE crux is the
factor `3` in the uniform prime lower bound (`1/12` → near `1/4`).  The kickoff's literal
objective (`g^(k+1)/4` multi-scale) had already landed on 2026-09-07 (`MahlerQuarter.lean`).

## 🧠 Context to carry forward
* **Drift algebra** (review lap): a drift-one junction of offset `b ∣ p+1` has source
  `c₂ = −b/(p+1)` and lands on `+b/(p+1)`.  One junction closes iff `−1 ∈ ⟨p⟩ (mod D)`.
  The keys are exactly: keyA iff `bm < p(p−D)` (worst residue), keyB iff `bm + D < (b−1)pD`
  (proved in Lean for any `b`, `DriftOneB.keys`; measured exact).
* **The 2-cycle (lap 3)**: two junctions `b, b'` close iff `−b'/b ∈ ⟨p⟩`; at `D ∣ bp + b'`
  this is closed form and NO orbit condition remains.  Mixed drifts `(b,g),(b',g')` need
  `gg'p^s ≡ 1` plus an individual condition — never closed-form (checked at `|s| ≤ 2`).
  Drift `−1` junctions are REFUTED (trigger `w₀ = p−1` sits in the bad zone; keyB dies at
  channel `(p−1)/2`).
* **Coverage**: closed-form 2-cycles (`b,b' ≤ 12`, `k ≤ 8b`) cover every prime
  `p ≢ 1 (mod 12)` below 2000, worst ratio 0.40.  `p ≡ 1 (mod 12)` with `(p+1)/2` prime has
  only offset 2 → back to `−1 ∈ ⟨p⟩` → the classical sorry.
* **The remaining sorry** `exists_prime_nonresidue` (`MahlerDriftOne.lean`): prime
  `q ∈ (p/3, p/2)`, `q ∤ p+1`, `p` non-residue mod `q`, for `p ≥ 73` (false at 71, handled by
  hand).  Unconditionally Linnik-strength; not attackable in mathlib.  Don't burn laps on it.
* The directive's "forbidden closed-form background" refers to SINGLE-junction `D=(p+j)/2`;
  the 2-cycle is a new closure, within "attack the arithmetic crux".

## ✅ State (all observed: `lake build` green 8885 jobs, `#print axioms` trust triple)
* `MahlerDriftOne.lean`: `mahler_lower_bound_drift_one` (b=2), `DriftOneB.keys` (any b),
  instances `M(127,1) ≥ 3843`, `M(101,1) ≥ 2450`; conditional `mahler_lower_bound_prime_drift_one`
  (`≈ p²/6`, `p ≥ 61`) on the one sorry.
* `MahlerTwoJunction.lean`: `mahler_lower_bound_two_junction`, `closure_of_cycle`
  (`j=1, j'=3, e=4φ(D)`), `mahler_lower_bound_two_cycle` (general), and
  **`mahler_lower_bound_prime_seven_mod_twelve`**: `M(p,1) > p(p−1)/6 − 1` for every prime
  `p ≡ 7 (mod 12)`, `p ≥ 19`, unconditional.
* Scans: `experiments/mahler_drift_one_probe.py` (`--qnr`), `experiments/mahler_two_cycle_scan.py N BMAX KM`.

## 🎬 Next actions
1. Prove the classes `p ≡ 5, 11 (mod 12)` as theorems from `mahler_lower_bound_two_cycle`,
   sub-classing by the winning triples (`(2,3,5)` for `p ≡ 1 (5)`, `(2,6,5)` for `p ≡ 2 (5)`,
   `(3,6,15)` for `p ≡ 3 (5)`, then mod 7/8 for the rest) — each a copy of the
   `seven_mod_twelve` proof (substitute `p = 60k + r`, `nlinarith` the five bounds).  Find the
   exact finite triple set by running the scan with the residue printout and check it covers
   all residues mod `lcm` (a `decide`).
2. Then a single theorem "`M(p,1) ≥ c·p²` for every prime `p ≢ 1 (mod 12)`" with the class
   minimum `c` (expect `≈ 1/10`), unconditional — this retires the factor 3 for 3/4 of primes.
3. `p ≡ 1 (mod 12)`: needs a non-drift-one partner or a 3-cycle; measure first
   (`PENDING_WORK.md` §top item 2).

## ⚠️ Gotchas
* `omega` atomizes ℕ-subtraction of nonlinear terms (`p^2*D − p*D` becomes one atom): state
  identities additively (`X + pD = p²D`) before `omega`.  `2*b*M` and `2*(b*M)` are different
  atoms — add a `ring` bridge.
* `c₂_cast` lives in namespace `Background.DriftOne` (no `h` argument).
* The pre-commit hook runs the full `lake build`; commit only after your own green build.

## 📁 Key files
- `src/NormalNumbers/MahlerTwoJunction.lean` — the 2-cycle engine + class theorem
- `src/NormalNumbers/MahlerDriftOne.lean` — keys from the drift; the sorry
- `src/NormalNumbers/MahlerBackgroundCert.lean` — single-junction engine (reused lemmas)
- `PENDING_WORK.md` §top — full attack plan; `DIRECTION.md` — directive (read-only)

---
**→ Next session: this is your starting point. Don't summarize this doc back to Trevor or
wait for instructions, and don't offer other projects from the KB — this doc IS the chosen
thread. Absorb the context above, then pick up at "Next actions" #1: run
`python3 experiments/mahler_two_cycle_scan.py 2000 12 8`, extract the triple set, and start
the `p ≡ 5 (mod 12)` class theorem.**
