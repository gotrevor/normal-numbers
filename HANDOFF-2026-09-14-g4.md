# HANDOFF 2026-09-14 — G4 disjunctivity, lap 1 (complete; budget wind-down)

HEAD `897160e` on `wip/g4-disjunctivity` (4 commits after kickoff `795b7ef`: `73d5342`,
`6455e9f`, `1047461`, `897160e`).  Working tree clean.  Not pushed (host pushes).

Branch `wip/g4-disjunctivity`.  Kickoff: `KICKOFF-2026-09-14-g4-disjunctivity.md`; brief:
`~/personal/claude/knowledge/core/projects/normal-numbers-g4-disjunctivity-fable-handoff-2026-09-14.md`.
DIRECTION.md: its CURRENT DIRECTIVE is the archived Mahler chain; the attended override at its
top records that campaign COMPLETE and requires a new attended objective — the 2026-09-14 G4
kickoff is that objective, so this lap works G4 and does not edit DIRECTION.md.
Root module untouched (host owns pointers): build with
`lake build NormalNumbers.G4Wiring` (pulls `G4SeparatingTest`, `PrimeLambertFour`).
All headline declarations below print `[propext, Classical.choice, Quot.sound]`.

## Proved (lap 1, checkpoint 1)

* `PrimeLambertFour.lean` — frozen endpoint.  `primeLambertAtBase b`, `primeLambertFour`
  (`= primeLambertAtBase 4`; base-two `primeLambert` untouched, `primeLambertAtBase_two`),
  `summable_omegaR_div_pow` (all `b ≥ 2`), `primeSumAtBase b = ∑'_{p prime} 1/(bᵖ−1)`,
  Props `G4DisjunctiveFour`/`G4DisjunctiveTwo`, proved reduction `isDisjunctive_two_of_four`
  via `isDisjunctive_pow_iff 2 2`.
  **Series identity proved** (checkpoint 2): `primeSumAtBase_eq_primeLambertAtBase` for every
  `b ≥ 2`, `primeSumAtBase_four : ∑_p 1/(4ᵖ−1) = primeLambertFour`.  Route: double series
  `primePowTerm b (p,m) = b^{−p(m+1)}` (summable by comparison with a product geometric series),
  `Summable.tsum_prod'` + geometric inner sum on the prime side, `HasSum.tsum_fiberwise` along
  `(p,m) ↦ p(m+1)` + a `card_bij'` with `n.primeFactors` on the ω side.
  Endpoint item 2 of the kickoff is therefore closed.
* `G4SeparatingTest.lean` — §4E finite inequality, fully proved, no arithmetic content:
  `separating_test_bound`: probability space, finite sample, characters `χ` with `χ i₀ = 1`
  and `∫ χ i = 0` (`i ≠ i₀`), test `f` with `∫ f ≥ 1−δ₁`, sample avg `f ≤ δ₂`, nontrivial
  sample chars `≤ δ₃`, polynomial within `κ` with nontrivial ℓ¹ mass `≤ Λ`
  ⟹ `1 ≤ δ₁ + δ₂ + 2κ + Λδ₃`.  `separating_test_contradiction` is the `< 1 → False` form.
* `G4Wiring.lean` — the dependency graph as named Props on concrete objects and the wiring:
  torus `Fin r → UnitAddCircle` (Haar probability instance), `torusChar`, `fourierBox`,
  **orthogonality** `integral_torusChar_eq_zero` (via `fourierCoeff_fourier` + pi Fubini),
  average metric `dAv` (nonneg, triangle, `dAv ≤ sup-dist`), `dAvSet` (1-Lipschitz,
  continuous), `clipTest`, `tube`, and the Haar-side inequality
  `one_sub_tube_le_integral_clipTest : 1 − vol(tube) ≤ ∫ clipTest`.
  `Frame` structure (K, J, r, H, A, d, t, P, θ, γ, S, η, ε, D), `Frame.image = A(Cᴴ)+θ−γ`
  with `C = orbitClosure` (closure of `orbit 4 G₄`), `Frame.Ffull n` = exact transported
  vector (tsum over all layers `j ≥ 1`, base four) minus `γ`.
  Named inputs: `PropA` (Ffull n ∈ image on P), `PropB δ₁` (tube volume), `PropC δ₃`
  (uniform box Fourier decay of `S`), `PropD δ₂` (sample-avg `dAv(S n, Ffull n) ≤ δ₂ εη`),
  `PropJackson κ Λ`.  Proved: `sampleAvg_test_le` (A ∧ D ⟹ sample side),
  `one_sub_le_integral_test` (B ⟹ Haar side), **`Frame.finite_contradiction`**
  (A ∧ B ∧ C ∧ D ∧ Jackson ∧ `δ₁+δ₂+2κ+Λδ₃ < 1` ⟹ False),
  `SeparatingFrameExists` (omitted interval ⟹ closed frame), and the conditional headlines
  `isDisjunctive_four_of_frames`, `isDisjunctive_two_of_frames`.

* `G4Spectral.lean` (checkpoint 3, **B** started) — `gram s` = the `s×s` tridiagonal
  `(−1,2,−1)` Gram matrix `D_s D_sᵀ`; explicit `L D Lᵀ` factorisation (`gram_eq_LDLt`, unit
  lower-bidiagonal `L` with subdiagonal `−k/(k+1)`, `D = diag((k+2)/(k+1))`), hence
  **`det_gram : det T_s = s + 1`**.  This is `∏ λ_j = s+1`, i.e. the mean
  `(1/s)∑ log λ_j = log(s+1)/s` that the `O(r√K)` cancellation needs.
  Checkpoint 4: **sine eigenvectors proved** — `gram_mulVec_apply` (three-term row shape),
  `angle s j = π(j+1)/(s+1)`, `lam s j = 2 − 2cos(angle)`, `sineVec s j k = sin((k+1)·angle)`,
  `gram_mulVec_sineVec : T v_j = λ_j v_j`, `lam_pos`, `lam_le_four`, `lam_strictMono`,
  `lam_injective`, `gram_transpose`, `sineVec_orthogonal` (symmetry + distinct eigenvalues),
  `sineVec_self_pos`.  So `gram s` has the explicit orthogonal eigenbasis `{v_j}`.
  **Route decision for B (recorded):** replace the zonotope/Cauchy–Binet volume formula by the
  ellipsoid bound — the side-`η` box in `ℝ^{H+g}` lies in the ball of radius `η√(H+g)`, whose
  image under `L = [A_G, I_g]` has volume `ω_g (η√(H+g))^g √det(I + A_G A_Gᵀ)`
  `= η^g exp(O(H) + ½ log det)`; same exponent as draft (5.3), no Shephard formula needed.
  The irreducible spectral content is unchanged: `log det(I + AAᵀ) = O(r√K)`, and Hadamard
  (`≤ gK log 2`) is *not* enough (it needs `a > 1/2` in `η = 2^{−aK}`, incompatible with D).

## Open (none of these is proved; none is an axiom)

`PropA`–`PropD`, `PropJackson`, and the §5 schedule producing `SeparatingFrameExists`.

## Lap-1 checkpoint status vs. the kickoff

1. Endpoint pinned — done (`PrimeLambertFour.lean`), incl. the series identity.
2. Dependency graph A–E as named Props — done (`G4Wiring.lean`).
3. Finite separating-test contradiction conditional on A–D (+ Jackson) — **proved**
   (`Frame.finite_contradiction`; the abstract inequality in `G4SeparatingTest.lean`).
4. One load-bearing lemma taken far: **B** — 1-D spectrum of `D_s D_sᵀ` fully proved
   (`det_gram`, `gram_mulVec_sineVec`, orthogonality); tensor step and log-moment bound
   are the next open sub-goals.  Nothing refuted; the ellipsoid simplification of the
   zonotope volume step is recorded above.

## Next attacks

1. ~~Series identity~~ done.
2. **B** (crux), in `G4Spectral.lean`: next the
   tensor power `AAᵀ = T^{⊗K}` with product eigenvectors; `det(I + AAᵀ) = ∏(1 + Λ_j)`;
   then `∑_j (log Λ_j)² = r(K μ₂ + K(K−1) μ₁²)` with `μ₁ = log(s+1)/s` (from `det_gram`) and
   `μ₂ ≤ C` via `λ_k ≥ 4(k+1)²/(s+1)²` (Jordan `Real.mul_le_sin`) and `∑ log²((s+1)/m) ≤ Cs`.
   After that: ellipsoid volume bound (`addHaar_image_linearMap`), torus projection
   `vol_𝕋(π S) ≤ vol(S)`, covering `C^H`, and the omitted-word dimension deficit.
3. Jackson: product Fejér² kernel, first moment `O(1/D)` in `dAv`, ℓ¹ budget `(2D+1)^r`.
