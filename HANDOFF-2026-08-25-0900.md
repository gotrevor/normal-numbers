# HANDOFF 2026-08-25 09:00 — Schedule built, CF normality of x* PROVED, d-ary bridge started

**Branch/HEAD**: master @ `64ef6d3`, clean tree, `lake build` green (8742 jobs).
Supersedes HANDOFF-2026-08-25-0500.md.  All headline claims below verified
`#print axioms` = trust triple.

## Session headline (§2.1 + §2.2-CF are DONE)

1. `8ff87ae` **Uniform Lemma 13 + THE SCHEDULE** (`CFSchedule.lean`).
   Refactor: `TBrick.exists_refinement_uniform` — thresholds `kmin₀/N` depend
   only on `(t, F, δ, ε)`, quantified BEFORE the brick; the good-length `C` is
   the one global half-mass constant (`goodC`).  This makes the stage length
   `nFn t` CONSTANT within a t-epoch, so B–Y's dominance conditions come from
   the promotion rule alone (no log estimates needed on the CF side).
   Schedule: `wordFamily t` (patterns ≤ t), `schedEps t = 1/(t+1)`,
   `kminFn/nFn` (with `nFn t > t²`), promotion gate
   `promThreshold (t+1) = (t+1)·nFn (t+1) ≤ |w|` ("slow t"), `SchedStep`
   (records ALL Lemma-13 payloads incl. the intermediate-brick cell data
   `m₁/j₁/r₁` that chains consecutive stages), `sched : ℕ → SchedState`.
2. `9275ed2` **Invariants + x***: `sched_prom_invariant`, `sched_dominance`
   (`t·n(t) ≤ L`), `sched_t_tendsto` (levels unbounded), and `xstar` —
   irrational, in every scheduled cylinder (`CFLimit` applied).
3. `a04f752`+`4773b5f`+`2958cdd` **CF NORMALITY OF x*** (`CFCorrect.lean`):
   `xstar_cf_freq_tendsto` — ∀ genuine pattern v, window frequency of v in
   xstar's CF digit prefix → γ(I_v).  Route: digit identification
   (`cfPrefix_eq_wSched`), level margin (`exists_level_margin`), Lemma-7 chain
   (`tailSched_cfDiscLt`), boundary `cfDiscLt_short_append`, interior
   `cfDiscLt_append_take` + dominance, stage locator (`exists_stage`,
   `Nat.findGreatest`), metric limit.  ε-budget: ε' = ε/5, chain ε', boundary
   2ε', interior 4ε' < ε.
4. `09ce249` **d-ary digit extraction** (`DaryDigits.lean`):
   `digit_window_eq` — a point of the payload's order-(m+k) sub-cell
   `J·d^k + blockNatVal β` has base-d digits m..m+k−1 EXACTLY β (as lists),
   via `floor_pow_of_mem_daryCell` (cell → floor coarsening, Int division)
   and `blockNatVal_digit` (big-endian readout).  Connects to `digitOf`
   (RealDefs) = Track A's `IsNormal` surface.

5. `64ef6d3` **d-ary payload accessors** (`DaryCorrect.lean`): (a)+(b) of
   the plan below are DONE — `xstar_dary_window`: at every stage with base
   `d ≤ tSched s` active, `xstar` gains `k ≥ kminFn (tSched (s+1))` new
   base-`d` digits (positions `mSched s d ..+k−1`) forming a
   `schedEps (tSched (s+1))`-good block (`HasDiscLt`).  Also
   `two_pow_le_cfK` (exponential continuant growth `cfK u ≥ 2^(n/2)`) for
   (c).  Key trick recorded in Gotchas: `set k := …; clear_value` BEFORE
   destructuring `∃ β : Fin k → …`.

## NEXT: the d-ary chain (§2.2, base-d side) — plan

Target: simple normality of x* in EVERY base d ≥ 2 (digit frequency → 1/d);
then (C) headline packaging.

- (a) **Payload accessor** (`xstar_dary_step`): for `2 ≤ d ≤ tSched s`
  (so payload-5 gives m₁ = mSched s d), instantiate SchedStep payload 10 at
  y := xstar: ∃ i < 2, xstar ∈ cell(mSched s d, jSched s d + i) and its
  digit window (mSched s d)..(mSched (s+1) d)−1 is a good block
  (∉ badBlocks at schedEps (tSched (s+1))).  J ≥ 0 needed for extraction:
  xstar ∈ (0,1) ⇒ floor ≥ 0.
- (b) **badBlocks ↔ HasDiscLt bridge**: β ∉ badBlocks d k ε ↔ every digit
  count within εk of k/d — deviation form matches `HasDiscLt (ofFn β) ε`
  via `digitCount_eq_count_ofFn`.  (badBlocks def: ∃ s, ε·k ≤ |count−k/d|;
  complement gives strict <.)
- (c) **m-growth bounds** (the log-arithmetic piece, the only genuinely new
  math left): per-stage digit gain k_s(d) := mSched (s+1) d − mSched s d.
  Lower: ≥ kminFn ≥ 1 (payload 9) — NOT enough for the interior condition.
  Need: k_{s+1} ≤ ε·(m_d(s) − m_d(s₀)) eventually.  Route (multiplicative,
  from brick fields): d^{m'} ≤ 2/|I_{s+1}| (containment hsub at s+1),
  d^{m} ≥ 1/(2d|I_s|) (hratio at s) ⇒ d^{k} ≤ 4d·|I_s|/|I_{s+1}| ≤
  32d·cfK(u)² ≤ 32d·e^{2·goodC·n} (continuant submultiplicativity
  K(w++u) ≤ 2K(w)K(u) — CHECK repo for cfK append bounds, likely in
  CFDefs/CFCylinder).  Denominator: m_d(s) − m_d(s₀) = Σ k_j and
  k_j ≥ (2 log cfK(u_j) − log 8d)/log d with cfK(u_j) ≥ 2^{(n_j−1)/2}
  (prove: continuant doubling every 2 steps; digits ≥ 1) ⇒
  Σ ≥ (log2/(4 log d))·(L_s − L_{s₀}) for levels large.  Then
  k_{s+1}/denominator ≲ goodC·n_{s+1}/L_s ≤ goodC/t → 0 (dominance).
  All via Real.log monotone + log_pow; no fib needed.
- (d) **HasDiscLt chain** mirroring CFCorrect: per-stage blocks good at
  schedEps (tSched (s+1)) → chain (HasDiscLt.append, same-ε via mono) →
  boundary (hasDiscLt_short_append; the pre-s₀ digits are m_d(s₀) many,
  short relative to Σ k_j) → interior (hasDiscLt_append_take with (c)) →
  digit-position locator (findGreatest on m_d(s) ≤ p) → Tendsto
  (count of digit c in digitOf d xstar prefix)/P → 1/d.
- (e) **Headline packaging** (C): xstar simply normal to all d ≥ 2 +
  CF-normal.  For "absolutely normal" (full normality all bases): Pillai
  (simple normality to all b^k ⇒ normal to b) — check Wall.lean/Counting.lean
  for reusable pieces; possibly already derivable from `IsNormalSequence`
  machinery.  Freeze statement shapes for JUDGE.
- (f) W6: Khinchin graft (KHINCHIN.md).

## Gotchas (this session)

- **Dependent rw on SchedState**: `(sched s).B : TBrick (sched s).t` — rw
  of `.t`-equalities in goals mentioning `.B` fails (motive); use
  `congrArg promThreshold h` / prove equalities of APPLIED fields, or
  `set`/`generalize` the length first.
- **omega and `s + (k+1)` vs `(s+k)+1`**: distinct atoms under `sched`;
  normalize indices with `have : s+(k+1) = (s+k)+1 := by omega; rw [this]`.
- `Monotone` application un-beta-reduces: ascribe the type
  (`have h2 : t ≤ (sched (s+k)).t := sched_t_mono ...`).
- `Int.natCast_div` (NOT `Int.ofNat_div`) for `((V/d^e : ℕ) : ℤ)`;
  `push_cast` alone then handles it (knows nat-div cast on ℤ).
- `rw [hsplit]` where hsplit rewrites an exponent occurring in a `Fin k`
  binder's type: motive fails — use `rw [← pow_add]; congr 1` (+ omega if
  congr leaves the exponent goal; it often closes it via assumption).
- `Int.ediv_mul_le` wants `≠ 0` not `0 ≤`; `Int.lt_ediv_add_one_mul_self`
  wants `0 < b`.
- `Fintype.piFinset` + `List.ofFn` image = Finset of bounded words;
  membership via `Fintype.mem_piFinset` + `List.mem_ofFn`.
