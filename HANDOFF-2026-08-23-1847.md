# HANDOFF 2026-08-23 18:47 — W4 variance bound proved; Chebyshev next

**Branch/HEAD**: master @ `4805358`, clean tree, `lake build` green (8730 jobs).
Supersedes HANDOFF-2026-08-23-1827.md.

## State

DIRECTION.md CURRENT DIRECTIVE (binding, set by this session's review lap):
drive the **W4 block-frequency Chebyshev assembly** in `CFBlockFreq.lean`. That
directive OUTRANKS this handoff — read it first.

`src/` has **exactly one open sorry**: `chebyshev_blockCount` (CFBlockFreq.lean).
Everything else is proved and axiom-clean (verified `#print axioms` = trust
triple this session).

## Done this session (review lap + 2 grind checkpoints)

- **Review**: certified Track A COMPLETE + all 7 headlines axiom-clean; created
  STATUS.md + DIRECTION.md; fixed stale ROADMAP (Stoneham proved).
- **`CFBlockFreq.lean`** (NEW, in root import) — the W4 engine, all axiom-clean:
  - `integral_blockCount` (first moment `∫ S_n dγ = n·γ(A)`)
  - `gaussMeasureReal_pair_shift` (pair invariance)
  - `integral_blockCount_sq` (second moment = Σ pair correlations)
  - `abs_cov_le` (per-pair covariance from γ-mixing — route-decisive)
  - `abs_cov_pair_le` (per-pair at gap `|j−j'|`, uniform geometric dominator)
  - `sum_range_dist_le`, `geom_trunc_sum_le` (Finset gap-count + geometric tail)
  - **`variance_blockCount_le`**: `Var(S_n) ≤ (8|v|+80)·n·γ(I_v)`
  - `IsProbabilityMeasure gaussMeasure` instance, `volumeReal_cfCylinder_le_one`

## Next (the one open sorry)

`chebyshev_blockCount`: `γ{ x : δ ≤ |S_n(x)/n − γ(I_v)| } ≤ (8|v|+80)·γ(I_v)/(δ²n)`.
Plan (verified API exists, see PENDING_WORK.md for detail):
1. `MemLp (blockCount (cfCylinder v) n) 2 gaussMeasure` via `MemLp.of_bound`
   (bounded: `0 ≤ S_n ≤ n`; measurable: finite sum of `blockIndic∘iterate`).
2. `variance_eq_sub hMemLp : Var = γ[S²] − γ[S]²`; `integral_blockCount` gives
   `γ[S] = n·γ(I_v)`; then `variance_blockCount_le` bounds `Var` (Var ≥ 0, so
   `Var = |γ[S²]−(nμ)²| ≤ (8|v|+80)·n·γ(I_v)`).  ⚠ `γ[S²]` = `μ[X^2]` may need
   `simp only [Pi.pow_apply]` to match the `blockCount … ^ 2` integrand.
3. Set rewrite `{δ ≤ |S/n − μ|} = {δn ≤ |S − γ[S]|}` (n>0: `abs_div`,
   `le_div_iff₀`), then apply `ProbabilityTheory.meas_ge_le_variance_div_sq`
   (needs `IsFiniteMeasure`, have it) with `c = δn`.
4. `.toReal` of the ENNReal bound (`ENNReal.toReal_mono` + `toReal_ofReal`),
   then `Var/(δn)² ≤ (8|v|+80)μ/(δ²n)` by `field_simp; ...` (δ≠0, n≠0).

After Chebyshev: the conditioned-on-brick version (s-started identity → same
pin, `setIntegral_inter_preimage`), then the b-ary side (B–Y Lemmas 8/9 —
check Counting.lean/Visits.lean overlap), then W5.

Governance: the two W4 targets are lap *proposals* for judge ratification
(same status as CFGammaMixing groundwork), flagged in JUDGE.md territory —
do NOT reshape the JUDGE-frozen statements in CFCylinder/CFDigitLaw/CFMixing.

## Gotchas (this session)
- Bash cwd drifts if you `cd` into `.lake/packages/mathlib` for greps →
  later `lake`/`git` hit the wrong repo ("git exited 128", and background
  `lake build | grep` can MASK real Lean errors by matching the git line).
  Always `cd /Users/gotrevor/src/normal-numbers &&` before build/commit, and
  re-run a suspicious green build in the FOREGROUND from repo root to confirm.
- Higher-order unify picks constant functions: pass `g` explicitly to
  `sum_range_dist_le`, don't leave it `_`.
- `Set.indicator` ∘ preimage: close the `1 (f x) = 1 x` residual with
  `Pi.one_apply`.  omega over Finset membership needs `Finset.mem_range`
  unfolded first (use `simp only [Finset.mem_filter, Finset.mem_range]`).
