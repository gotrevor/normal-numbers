# HANDOFF 2026-08-27 07:00 — Tier-2 crux reduced to a single tail-mass bound

**Branch/HEAD**: master @ `6f18228`, `lake build` green (8745 jobs).
Supersedes `HANDOFF-2026-08-27-0000.md` (this run's review-lap baton).

**⚠️ OBEY `DIRECTION.md` CURRENT DIRECTIVE** (set 2026-08-24 review lap): Tier 1
LOCKED; objective = Tier 2 Khinchin; route SETTLED (additive W6 log-concentration
bad zone); schedule fence RELAXED to additive-only with the hard invariant that
`#print axioms exists_absolutely_normal_cf_normal` stays the trust triple.

## State of Tier 2 (the only open work)

The frozen headline `exists_absolutely_normal_cf_normal_khinchin` (`Headline.lean:134`,
`sorry`) now provably rests on a SINGLE schedule lemma. Kernel-verified: both
`xstar_log_digit_avg_tendsto` and `xstar_khinchinTypical` have `#print axioms`
= `[propext, sorryAx, Classical.choice, Quot.sound]` — the only non-trust
dependency is `sorryAx`, sourced entirely from:

**`xstar_log_tail_uniform`** (`Khinchin.lean`, the sole disclosed `sorry`):
`∀ε>0 ∃K₀ ∀K≥K₀ ∀n, |(1/n)Σ_{i<n}log aᵢ − (1/n)Σ_{a≤K}count[a]·log a| ≤ ε`.
Via `xstar_logTail_eq` (proved) this is a pure UPPER bound on the NONNEGATIVE
empirical tail `(1/n)Σ_{i<n, aᵢ>K} log aᵢ ≤ ε`.

## Proved this run (all axiom-clean, in `Khinchin.lean` unless noted)

- Reduction: `xstar_log_digit_avg_tendsto` (3ε assembly), `xstar_khinchinTypical`.
- Target value: `gaussKuzmin_logsum_hasSum`/`_tendsto` (Σ γ([a])log a = log K₀),
  `gaussKuzmin_logtail_tendsto` (tail → 0, the K-selection input).
- Moments: `summable_gaussKuzmin_logsq` (2nd moment; NOT on critical path now),
  `khinchinK₀_pos`, `khinchinTypical_iff_log_tendsto`.
- Value-count bridge: `countOccurrences_singleton`, `logTail_list_eq`,
  `xstar_logTail_eq`.

## NEXT (in PENDING_WORK.md top; route SIMPLIFIED to Markov)

The tail is nonnegative and only an UPPER bound is needed, so **Markov (first
moment) suffices** — no variance/Chebyshev, no L²-observable γ-mixing extension.
The general union lemma `exists_mem_notMem_union_of_bounds` (`TBrick.lean:244`,
two zones, `p+q<1/2`) needs NO change: add the Khinchin zone by unioning it into
the d-ary group (subadditivity) and tightening coefficients to `p+q+r<1/2`.

- **(A′)** First-moment integral (NEW file `CFLogTail.lean`, no TBrick edit):
  `∫ x, (Σ_{i<n} if K<cfDigit x i then log(cfDigit x i) else 0) ∂γ = n·Σ_{a>K} γ([a])·log a`.
  Structure: `logTailSum K n x = Σ_{k<n} h(gaussMap^[k] x)` with `h y = if K<cfDigit y 0
  then log(cfDigit y 0) else 0` (use `cfDigit_add`, `CFDensity.lean:243`); then
  `∫ = n·∫h` by T-invariance (mirror `integral_blockCount`, `CFBlockFreq.lean:115`;
  for the general observable use `lintegral_comp`/`integral_map` + `measurePreserving_gaussMap`);
  finally `∫h = Σ_{a>K}γ([a])log a` via MCT over finite digit-truncations
  (`h_M = Σ_{a=K+1}^M log a·1_{cfCylinder[a]}`, using `mem_cfCylinder_singleton`,
  `CFDensity.lean:147`) — avoids the tsum–integral interchange.
- **(B′)** `logBadZone K n η` + Markov measure bound `≤ (1/η)Σ_{a>K}γ([a])log a`.
- **(C′)** Union B₃ into `exists_good_avoiding_bad` (`TBrick.lean:470`), tighten
  coeffs, thread `exists_refinement_uniform` + schedule → discharge
  `xstar_log_tail_uniform` (target the clean nonneg-tail form via `xstar_logTail_eq`).
- **(D′)** Layering: move frozen `KhinchinTypical`/`khinchinK₀` defs (currently
  `Headline.lean`) to an upstream module VERBATIM so `Headline.lean:134` closes
  with `xstar_khinchinTypical`. Preserves the frozen statement (JUDGE-owned).

## No uncommitted edits. `src/` open sorries: `Headline.lean:134` + `xstar_log_tail_uniform`.
