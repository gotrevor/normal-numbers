# HANDOFF 2026-08-23 20:55 — B–Y LEMMA 13 (t'=t) PROVED

**Branch/HEAD**: master @ `6628061`, clean tree, `lake build` green (8737 jobs).
Supersedes HANDOFF-2026-08-23-2027.md.

## Headline of this session

**`TBrick.exists_refinement` (TBrickRefine.lean) — Becher–Yuhjtman's main
lemma (Lemma 13), t' = t case, PROVED and axiom-clean** (trust triple only).
The route-decisive W5 wall is down.  Statement: every t-brick admits, for
every `kmin ≥ kmin₀` and `n ≥ N(kmin)`, a refined t-brick `B'` with
`B'.w = B.w ++ u`, `u ∈ genWords n`, such that
- `K(u) ≤ e^{Cn}` (good length, C from `half_mass_long_extensions`);
- ∀ v ∈ F: `|count_v(u) − n·γ(I_v)| < δn + |v|` (CF payload, Lemma-7-ready);
- every base `2 ≤ d ≤ t` gains ≥ kmin new d-ary digits;
- EVERY point y of `cfCylinder B'.w` sits in a definite old-brick cell and
  its new d-ary block is ε-good (d-ary payload, Lemma-9-ready).

## Session commits (all green, axiom-clean)

1. `10a8c6e` zones cover both brick cells (24d²); irrational survivor;
   survivor unpacking toolkit; **(β) kmin(n) link, log-free**
   (`4·d^kmin < fib(n+1)²` ⇒ `|I_wu| < d^{−(m_d+kmin)}`).
2. `40875b6` `TBrick.exists_refined_cell`: greatest order m' + Prop 12
   2-cell block + 1/(2d) ratio (public `cfCylinder_subset_Icc_length`).
3. `f2c2b58` `goodBlock_transfer`: wide-zone avoidance at the survivor ⇒
   every point of the shared refined block has a good new digit block.
4. `6628061` **`TBrick.exists_refinement`** — the assembly.

## What remains for W5/W6 (see PENDING_WORK + DIRECTION)

- **t → t+1 case** of Lemma 13: given the t-refinement, adjoin base t+1 via
  `exists_refined_cell`-style Prop-12 choice for d = t+1 (no goodness needed
  at the first appearance of a base).  Should be a short corollary; the
  needed cell lemma is `exists_greatest_inv_pow_lt` + Prop 12 on
  `cfCylinder B'.w` (no fib condition needed — just `|J| < (t+1)^{-m}` for
  SOME m, which holds since |J| < 1... need |J| ≤ (t+1)^0 = 1 strictly: ✓
  cylinders sit in (0,1)).  Also need a seed brick (t = 2, w = some word,
  cells from Prop 12) to start the induction.
- **§2.1–2.2 schedule + limit**: t(s), ε(s), δ(s), n(s), kmin(s); iterate
  exists_refinement to get nested cylinders; x* = ⋂ (compactness/nested
  intervals); CF normality via Lemma 7 chains (`CFDiscLt.append`,
  `cfDiscLt_append_take`, `cfDiscLt_short_append`) on the digit word of x*;
  simple d-normality via Lemma 9 chains (`HasDiscLt` in BaryConcat) on x*'s
  d-ary digit blocks (payload (iv) applies to y = x* directly since
  x* ∈ every refined cylinder).
- **Pillai/powers equivalence** (simple normality all bases ⇒ absolute
  normality) to state the headline; then W6 Khinchin graft.

## Gotchas (this session)

- `measure_union_null` in this mathlib has the NULL set on the LEFT
  (`μ (s ∪ t) = μ t`); for null-on-right use measure_union_le + add_zero.
- `cfCylinder_subset_uIcc` was private → added public
  `cfCylinder_subset_Icc_length` in CFCylinder.lean.
- `div_le_div_of_nonneg_right` here wants `0 ≤ c` (not `0 <`).
- `inv_le_inv_of_le` gone; use `one_div_le_one_div_of_le` + `simpa [one_div]`.
- `Nat.findGreatest` under `rw` of the predicate breaks motive (decidability
  instance mentions P); avoid rewriting — P applications unfold definitionally
  (`not_lt.1 hnot` works directly).
- `choose!` (bang) turns `∀ d, 2 ≤ d → d ≤ t → ∃ m' j', …` into plain
  functions `m' : ℕ → ℕ`, `j' : ℕ → ℤ` — exactly what structure fields need.
- `Irrational x` is definitionally `x ∉ Set.range ((↑) : ℚ → ℝ)` — absorbing
  the rationals into a bad zone yields irrational survivors for free.
