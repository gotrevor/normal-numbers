# HANDOFF 2026-08-24 02:54 — Route A′ (first-moment log-tail integral) PROVED

**Branch/HEAD**: master @ `9d73a81`, `lake build` green (8745 jobs).
Supersedes `HANDOFF-2026-08-24-0243.md` for "what's proved"; that file's
route plan (A′/B′/C′/D′) is still the live plan, A′ is now DONE.

## What landed this lap (`Khinchin.lean`, both axiom-clean, `#print axioms`
= `[propext, Classical.choice, Quot.sound]`, no `sorryAx`)

- **`integral_logTailFn_eq (K)`**: `∫ x, logTailFn K x ∂gaussMeasure = log K₀
  − Σ_{k<K} γ([k+1])·log(k+1)` — EXACT identity, not an estimate. `logTailFn
  K x = if K < cfDigit x 0 then log(cfDigit x 0) else 0` is the single-digit
  (n=1) tail indicator. Proof: split by digit value into a countable family
  of disjoint cylinder-indicator terms `logTailTerm K n` (value `log(K+1+n)`
  on `cfCylinder [K+1+n]`), swap `∫` with `∑'`
  (`MeasureTheory.integral_tsum_of_summable_integral_norm`, licensed by
  `Summable` from `gaussKuzmin_logsum_hasSum`), collapse the tsum via
  `hasSum_nat_add_iff'` against that same `HasSum`. New small lemmas:
  `logTailTerm`, `logTailFn`, `logTailTerm_integrable`,
  `integral_norm_logTailTerm`, `integral_logTailTerm`,
  `gaussMeasure_compl_Ioo`, `ae_mem_Ioo_gaussMeasure`.
- **`integral_logTailFn_tendsto`**: `∫ logTailFn K dγ → 0` as `K → ∞` —
  immediate corollary via `gaussKuzmin_logtail_tendsto.congr`.

This is exactly route **(A′)** from `HANDOFF-2026-08-24-0243.md`, done as a
real proved integral identity (the handoff's sketch undersold it — no MCT /
finite-truncation approximation needed, the tsum-swap lemma gives the exact
value directly). `Khinchin.lean` now imports `NormalNumbers.CFBlockFreq` (no
cycle — verified).

## NEXT: routes B′ + C′ (unchanged from prior handoff, now with A′ done)

- **(B′)** `logBadZone K n η := {x : Σ_{i<n, digit_i(x)>K} log(digit_i(x)) >
  η·n}` (a genuine multi-step Birkhoff-sum bad zone, analogous to
  `blockCount` in `CFBlockFreq.lean` but for `n` steps with weight `logTailFn
  K ∘ gaussMap^[i]`, not just the single-step `integral_logTailFn_eq` proved
  here). Markov bound: `γ(logBadZone K n η) ≤ (1/(η·n)) · n · (∫ logTailFn K
  dγ) = (1/η) · ∫ logTailFn K dγ`, using **T-invariance** to reduce the
  n-step Birkhoff average back to the single-step integral (mirror
  `integral_blockCount`'s per-`k` T-invariance argument, `CFBlockFreq.lean:115`)
  + `MeasureTheory.mul_meas_ge_le_integral`-style Markov on a nonnegative
  Birkhoff sum. `integral_logTailFn_tendsto` is exactly the `K`-selection
  input this needs.
- **(C′)** Union `logBadZone` into `exists_good_avoiding_bad`
  (`TBrick.lean:470`) as a third zone via subadditivity (`p+q+r<1/2`,
  tightening the existing two `<1/4` coefficients — e.g. `<1/6` each); thread
  through `exists_refinement_uniform` + the `xstar` schedule to discharge
  `xstar_log_tail_uniform` (target its tail form via `xstar_logTail_eq`,
  already proved).
- **(D′)** Layering: move frozen `KhinchinTypical`/`khinchinK₀` defs
  (currently in `Headline.lean`) to an upstream module VERBATIM so
  `Headline.lean:134` closes with `xstar_khinchinTypical`.

## No uncommitted edits. `src/` open sorries: `Headline.lean:134` +
`xstar_log_tail_uniform` (unchanged — the crux; A′ is a proved PREREQUISITE
for B′, not a bypass of it).
