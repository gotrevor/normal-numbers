# HANDOFF C4 — 2026-09-25, lap 14

Branch `wip/c4-infinite`, HEAD `7368ec7`, working tree CLEAN.  `lake build` green (9262 jobs).  Objective `KICKOFF-2026-09-24-c4.md`.
(`DIRECTION.md`'s CURRENT DIRECTIVE, 2026-09-23, targets a different campaign; the C4 kickoff is
newer, ratified on this branch, and is the operator's named objective.  Recorded since lap 9.)

## The crux
`c4_realizable_of_mem_one` (`AbelianWindowSets.lean:506`), the one open `sorry` in the C4 chain.

## PROVED this lap — the single-exclusion witness
`c4_realizable_compl_singleton (a) (ha : 2 ≤ a)` : some binary sequence is abelian at EXACTLY the
window lengths `≠ a`.  Axiom-clean.  This is the first exact window set with a finite nonempty
complement, and it is the building block the general construction mixes.

The witness is the **rectangle design** (`DESIGN-2026-09-25-c4-rectangle.md`): block length
`q = a+2`, digit alphabet `B = 2^q`, and the table `rectG a` that exchanges bits `a` and `a+1`
exactly when `bit 0 ≠ bit 1 ∧ bit a = bit 1 ∧ bit (a+1) = bit 0`.  Driven by the base-`2^(a+2)`
digits of `fullRealW`.

### The chain, all in `src/NormalNumbers/AbelianWindowRect.lean`
1. `sum_prod_bits` — bit-level specialization of `sum_prod_wordOf`.
2. `rect_pattern` / `rect_not_pattern` — under the trigger the four coupled bits are exactly the
   pattern `vv ε` (`ε = bit 0`) and the outputs are `ww ε`; off it, no pattern is matched.
3. `rect_key` — the pointwise identity splitting the coupled table into product-set terms.
4. `rect_sum_forced` + `rect_segGf` — **the closed form**
   `∑_d X^{ones_I} = base(I) + Cfac(I)·(X^{[a∈I]} − X^{[a+1∈I]})·(X^{[0∈I]} − X^{[1∈I]})`.
   The correction vanishes unless `I` contains exactly one of `{a,a+1}` AND exactly one of `{0,1}`,
   which for an interval inside `[0,a+2)` happens only for `I = [1,a+1)`.
5. `rect_corr_eq_zero`, `segSet_eq_Ico_gen` — and the observation that a defective trace forces the
   window to lie strictly inside one block, hence `L = a`.
6. `rect_isAbelianAt_of_ne` — abelian at every `L ≠ a`.
7. `rect_defect_factor`, `rect_empty_factor`, `rect_not_isAbelianAt` — at `L = a` only residue
   `r = 1` is defective, `blockFreq` at `j = 0` is `1/2^a − 1/((a+2)·2^{a+2})`, so NOT abelian.

Supporting general engine in `AbelianWindowGf.lean`: `winGf_eq_binomial` (extracted this lap),
`blockFreq_eq_binomial_of_seg`, `isAbelianAt_blockSeq_of_binomSeg`, `sum_prod_wordOf`.

## Next attack, in order
1. **Finite complement.**  Superpose rectangles on DISJOINT coordinate groups: group `j` occupies
   `a_j + 2` consecutive bit positions, so the block law is a product of independent group laws and
   `W_I = ∏_j (1 + R_j(I)X²)` with at most one factor `≠ 1`.  In the present Lean coordinates this
   means generalizing `rectG`/`rect_key` from one quadruple `(0,1,a,a+1)` to a list of quadruples
   `(o_j, o_j+1, o_j+a_j, o_j+a_j+1)` — `rect_key`'s proof is already a sum over the two forced
   patterns per group, so the generalization is a product over groups of the same argument.
   Target: `c4_realizable_of_finite_compl (D : Finset ℕ) (hD : ∀ a ∈ D, 2 ≤ a)`.
2. **Infinite complement** (the remaining genuine gap).  One block length gives an eventually
   `q`-periodic abelian set, so infinitely many scales are needed.  No cancellation obstruction:
   every design's defect sits in the `x²` coefficient and at its own length, so ANY positive
   mixture of the `P_a` has abelian set exactly `S`.  What is missing is realizing a mixture by ONE
   sequence: either multi-scale concatenation with densities `λ_a` (chunk lengths `→ ∞`, rounds
   `→ ∞`, so every `L`-frequency converges), or a stationary renewal process whose block length is
   `q_a` with probability `λ_a` — ergodic, so a typical sequence works, and every prefix/suffix
   cancellation in `rect_segGf` survives verbatim at any block length.

## Confidence
C4 is TRUE: **high**.  Every finite complement is now settled on paper and in exact arithmetic,
and the `|Sᶜ| = 1` case is in the kernel.
