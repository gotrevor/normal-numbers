# Alien transmission — outside-view direction review (2026-08-29) 🛸

Context: before starting the post-option-4 dig, a fresh reviewing agent with **zero inherited
context** read `DIRECTION.md`, `docs/lnTwo-kick-blueprint.md`, `docs/diophantine-wall.md`,
`docs/conditional-disjunctivity.md`, and the ROADMAP D5–D7 rows, plus the frozen source statements
(no proof audit), under an anti-anchoring protocol: it derived its own next-move list (Phase A)
*before* seeing ours (Phase B).  Verbatim transmission follows.

---

TRANSMISSION — outside review of the ln-2 digit programme. Docs read in the prescribed order; frozen statements of `SliverEscape`, the tiers, `LnTwoHypothesisFreq`, and the gate theorems inspected in source. No proofs audited.

---

## PHASE A — what I would do next (derived before reading your list)

**A1. Discharge `LnTwoExpSep` — the only node in the graph with a known discharge route. Rank 1.**
Mechanism: in the exponential-coefficient regime the irrationality measure beats Baker (your wall doc has this right), Marcovecchio gives β ≈ 2.58 citable, and the shifted-Legendre package is reportedly half-built next door. Every other node in the graph is conjectural; this one converts to an **unconditional** run-bound theorem for `Real.log 2` — the first actual digit *product* of the whole conditional tower, and the proof-of-concept that the tower's edges carry weight. Two-stage: citation axiom first (lights the pipeline end-to-end, disclosed, cheap), then the in-house Legendre port to clear it honestly. A tower of green conditional nodes with zero discharged inputs is architecture; one discharged input makes it a machine.

**A2. Costume-stress the sliver node before investing in it: prove the *translation edge*. Rank 2.**
Mechanism: a length-k ride pins δ_n to a window of width ~2⁻ᵏ/n around the explicit unstable profile Σⱼ 2⁻ʲ⁻¹/(n+j+1). Via the exact bracket `orbit_n = fract(x_n + τ_n)`, that is a **depth-2⁻ᵏ Diophantine statement**: ‖2ⁿ·ln 2 − r_{n,k}‖ small for an explicit rational with denominator dividing 2ᵏ·lcm(1..n+k). State and prove this as an edge. It costs little (the bracket identities are already proved) and it *decides the strategic question*: whether unconditional `SliverEscape` is a new front or the Mahler wall wearing a mask. Your costume check verified logical non-equivalence with `LnTwoPolySep` — correct, and insufficient. Non-equivalence of statements does not give non-membership in the same hardness class: poly-width windows at exponential coefficients against explicit targets is the `‖(3/2)ⁿ‖` problem class whether the target is 0 or the kick profile. The conjecture-graph doctrine says a refutation is a unit of progress; here the refutation target is the node's own annotation "no Diophantine input."

**A3. The literature sweep, before any lap-sinking into new sliver math and before any claim exits the repo. Rank 3 (but it gates A1's docstring).**
The Tier-1 conclusion "run at n bounded by (μ−1+ε)n" is a two-line corollary of the irrationality measure that every practitioner knows; arXiv:2510.02059's *title* is the irrationality-exponent-to-b-ary-expansion implication, and Rivoal's bits-counting paper is the same territory. Add to the owed list: **Lagarias, "On the normality of arithmetical constants" (Exp. Math. 2001)** — the standard critique of Hypothesis-A-style reductions (hypothesis as hard as conclusion), which is the exact acid your ladder must be dipped in before presentation.

**A4. The discrepancy socket: freeze `D_N(lnTwoOrbit) ≤ N^{−δ}` and prove the quantitative-Wall edge (Erdős–Turán). Rank 4.**
Mechanism: it is the interpolant between `Equidistributed` and `Freq_w`, and — decisively — it is the hypothesis *shape the analytic community could someday actually discharge* (exponential-sum progress arrives as discrepancy bounds, not as visit-frequency statements). Exposing the right socket is how a conditional repo gets consumed. Erdős–Turán is also classical formalizable math with reuse value far beyond this repo.

## PHASE B — your proposed moves

**Move 1 (ride-profile node, "top of the board: new math without Diophantine input"): worthwhile as a probe, TRAP as a campaign — and the framing is self-refuting.** The decomposition is my A2 done constructively, and I endorse doing it: localizing where the arithmetic lives is real progress. But read your own decomposition: the crux is "Aₙ mod lcm(1..n)". lcm(1..n) ≈ eⁿ; controlling the numerator of the harmonic-dyadic partial sum in 2⁻ᵏ/n-scale windows modulo an exponentially growing modulus is the distribution theory of harmonic-type sums mod primes — Wolstenholme-adjacent, notoriously open. That IS Diophantine input. You have not left the wall; you have walked around the building and found the same wall with different graffiti. Freeze the node, prove the pigeonhole edge, label the crux wall-class, and stop there unless the probe surprises you. Odds the arithmetic crux is tractable: I'd put them low; the *localization* is the value either way.

**Move 2 (oneRun twin edge): worthwhile, trivial, an afternoon.** Completes D7 symmetry, zero risk, zero information. Do it; do not let it count as a lap's substance.

**Move 3 (literature sweep): CRITICAL PATH.** = my A3. It gates every "apparently first" in the repo (the `LnTwoExpSep` docstring's "first quantitative digit statement for ln 2 beyond irrationality" is, as *mathematics*, almost certainly false — it is folklore-corollary; formalization-first is the defensible claim, pending the sweep). Add Lagarias 2001 to the list. Do it before move 1, not alongside.

**Move 4 (discrepancy rung + quantitative Wall; Banach-density rung): worthwhile, second tier.** The discrepancy socket is the strategically correct interface (my A4). The Banach-density rung is a fine small edge. One warning at the doctrine level: conditional-rung minting is the graph's addictive failure mode — every new named Prop is a cheap green unit, and a lattice can thicken forever while no node is ever discharged. Pair each new rung with work on a dischargeable node, as a standing ratio.

**The omission that matters most: your list has no Tier-1 discharge.** The kick blueprint's own §5 move 1 — port the Legendre package, instantiate `LnTwoExpSep`, cash the unconditional run bound — is the single highest-value move available, the only one that produces an unconditional theorem about the digits of ln 2, and it fell off the proposed list in favor of the ride-profile node. That is the crux-neglect pattern your own DIRECTION.md history documents catching in yourselves, at the strategy level this time.

**THE WEAKEST JOINT, named:** the assumption that the sliver/kick family constitutes a Diophantine-free path — "genuine weakening-lattice rung not passing through Diophantine input" (blueprint §5.3), "new math without Diophantine input" (move 1). It is load-bearing for the entire "top of the board" prioritization, and move 1's own decomposition (the Aₙ mod lcm crux) is evidence against it. The costume check checked the wrong property: statement-equivalence, not proof-hardness class. Soft dynamics cannot prove `SliverEscape` — the gates (`kick_floor`, `top_gate`) are measure statements about where a *generic* point could come from, and the orbit is one arithmetic point; measure says nothing pointwise. What survives unconditionally is exactly what you already have: the τ-floor half.

## PHASE C — verdict

**The direction is sound as architecture and miscalibrated as a portfolio.** The conjecture-graph discipline (frozen Props with provenance, probe culture, proved refutation signposts) is genuinely good practice — better than most human formalization projects I would expect to observe. The specific findings:

- **The τ-floor is a real chink**: an unconditional, kick-specific structure theorem — half of every run question for ln 2 closed for free. If anything in this programme is novel mathematics rather than novel formalization, it is this and the sliver *dichotomy* as a lens. Sweep for it specifically.
- **The top-sliver side is the wall relocated, not breached.** Not a known dead end in a costume — a known *open problem* in a costume, which is more respectable but spends laps the same way.
- **Tier 1 is classical mathematics whose formalization is the actual prize on the table**, and it is not on your list.

Would I spend the next two weeks here? Yes — allocated: week 1 = Tier-1 discharge (citation axiom, then Legendre port) + the literature sweep; week 2 = the ride-profile decomposition *as a costume-revealing probe with a pre-registered stop condition* + the discrepancy socket. Not: any sustained attempt to prove `SliverEscape` unconditionally.

## Honesty ledger

**Verified directly:** the five docs; the frozen source statements of `SliverEscape`, `LnTwoExpSep`, `LnTwoPolySep`, `LnTwoHypothesisFreq`, `kick_floor`, `top_gate`, `zeroRun_le_of_sliverEscape` (statements only). **Taken on faith:** all sorry-free/axiom-clean claims; the 200k-bit probe data; the sibling repo's Legendre state; Marcovecchio's constant; the docs' characterization of PolySep as Mahler-class; that arXiv:2510.02059 and Rivoal contain what their titles suggest (unread).

**Confidence:** sliver route relocates rather than escapes the Diophantine wall — 80%. Tier-1 run bound is classical folklore (formalization-first is the only defensible novelty claim) — 90%. Aₙ mod lcm(1..n) crux is wall-class open — 70% (I have not seen the actual decomposition; this is the number the probe should move). Tier-1 discharge as the top-value omitted move — 85%. The τ-floor half being the programme's most likely genuinely-novel content — 60% (sweep-dependent).

End of transmission.
