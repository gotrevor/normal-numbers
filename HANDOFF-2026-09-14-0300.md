# Handoff: `S(7,1) ≤ 7` as a theorem + base-7 lower wiring landed

**Date**: 2026-09-14 03:00 UTC · **Branch**: `wip/adder-tower-c9` · **Kickoff**: `KICKOFF-2026-09-13-hitting-7-1.md`

## 🎯 What we're doing
Turn the base-7 hitting-set verdict (search found `{1,2,3,4,5,6,13}`; certificates
`experiments/certs/adder_cert_h71_d{0..6}.json`, Python-verified) into theorems with the signed
base-`g` engine, and wire the base-7 Mahler lower bound. This lap completed the whole kickoff scope.

## ✅ State
- New `src/NormalNumbers/HittingSetBase7.lean` (imported from `src/NormalNumbers.lean`, one line):
  * `hitting_7_1_seven : Literature.IsHittingSet 7 1 {1, 2, 3, 4, 5, 6, 13}` — **`S(7,1) ≤ 7`**.
  * `not_hitting_7_1_eight : ¬ IsHittingSet 7 1 {1,…,8}` — `Mahler.mahler_lower_bound_base7`
    unfolded at its witness, `w = [1]`.
  * `not_hitting_7_1_six : ¬ IsHittingSet 7 1 {1,…,6}` — `IsHittingSet.mono` contrapositive.
- Route: **chunked kernel decide** (`decide +kernel`, no `native_decide`, no sorry). Per digit,
  six `1560`-state `checkEdgesOnA` sweeps + one `checkForcedA`, glued by
  `checkEdgesOnA_of_chunks` / `checkCertA_of_edgesOn` from `HittingSetBounds.lean`.
- `#print axioms` on all three: `[propext, Classical.choice, Quot.sound]`.
- Module build: `1417 s` (≈ 24 min), peak lean RSS ≈ `9` GB, no memory-guard trips.

## 🧠 Context to carry forward
- **Direct whole-certificate `decide +kernel` works but is not usable here.** One `h71` certificate
  alone closed in ~11 min at ~10 GB. With seven in one file the elaborator runs the kernel checks
  concurrently and the box (19 GB) hit `avail < 1 GB` within two minutes — build killed. Chunking
  bounds the per-check working set; peak stayed at 9 GB. Lesson: on this box, ambient ≳ 9000 states
  needs chunks regardless of what a single-certificate probe says.
- Two probes run concurrently swap each other into the ground (both showed sys ≫ user time); time
  kernel probes one at a time.
- The certificate blocks were generated by a 60-line scratchpad script (h51-style sparse
  `List.lookup` tables for `rho`/`omega`/`forced`; `omega` here has ≤ 78 nonzero entries so no
  packed table was needed). Committed Lean is the artifact.
- `DIRECTION.md`'s CURRENT DIRECTIVE (run+jump chain / B6) is a different campaign; the operator
  scoped this session to the kickoff only, so nothing else was touched.

## 🎬 Next actions
1. Nothing remains in this kickoff. Open mathematics: the lower bound `S(7,1) ≥ 7` (every 6-set
   fails; the search only excludes 6-sets with all multipliers `< 24`) — node N4′ in
   `docs/hitting-set-invariant-2026-09-13.md`. Needs a new kickoff.
2. If build latency matters, split the seven digits into per-digit files (proofs unchanged).

## 📁 Key files
- `src/NormalNumbers/HittingSetBase7.lean` — new theorems + certificates.
- `src/NormalNumbers/HittingSetBounds.lean` — engine glue lemmas, h51 template, `IsHittingSet.mono`.
- `src/NormalNumbers/MahlerPrimeLowerBound.lean:80` — `mahler_lower_bound_base7`.
