# Handoff: entropy expedition lap 128 — the base-`2^k` upgrade is proved; treadmill STOP signalled

**Date**: 2026-09-15 02:39 UTC · **Branch**: `wip/g4-entropy` · **HEAD**: `dce89f1` · tree clean

## 🎯 What we're doing

`DIRECTION.md`'s ACTIVE operator override (entropy expedition) with its lap-126-close CURRENT
DIRECTIVE: strengthen the proved `IsNormal 2 fullRealW` — the schedule-only, strictly increasing
read of `G₄`'s binary digits — to **`IsNormal 4 fullRealW`** and then **`IsNormal (2^k)`**.

**That objective is MET this lap, axiom-clean.**  `box done --green` was accepted (sentinel at
`~/src/.treadmill/normal-numbers.stop`); the host verifies GREEN and halts.  So this handoff is a
checkpoint for whoever picks the repo up next, not a "resume immediately" baton.

## 🧠 Context to carry forward

* The directive's own route (steps 3–4: transport the residue-restricted certificate to the read)
  is **refuted** — lap 127, `ROUTE-ESCALATION-2026-09-15-base2k-step3.md`, trigger E-T13.  The
  local residue class alternates with the window index because `kk i = 40000 + i` is odd for odd
  `i`, and the compensating index is the sorted **rank** of a multiplier, which the entropy
  certificate cannot see.  Do not retry it; steps 1–2 of that directive (`abs_posAvgR_sub_le`,
  `abs_posAvgRes_sub_le`) are proved and kept, they just don't reach the read.
* The route that worked is the successor the escalation named: prove the **classical theorem**
  `IsNormal b x → IsNormal (b^K) x` outright, then instantiate at `b = 2`.  Its heart
  (`BlockRigidity.Sys.eq_uniform`, lap 127) is ergodicity of the Bernoulli shift done with two
  Cauchy–Schwarz steps on a finite energy — no measure, no compactness, no ergodic theorem.
* The missing analytic glue, built this lap: take the limit of the residue-restricted word count
  **along an arbitrary ultrafilter** `g ≤ atTop` (free, the sequence lives in `[0,K]`), check the
  three `Sys` relations survive (two exact, one exact to `O(1/n)`), apply rigidity, and conclude
  by `tendsto_iff_ultrafilter`.  This ultrafilter trick is the transferable idea.
* One supporting edit: `BlockRigidity.Sys.shift` now carries `k < b ^ m`.  Every call site already
  had the bound; this is precisely what lets the ultrafilter limit serve as `F` with no
  mod-`b^m` reindexing of the `shift` sum.
* **E-T7** (a lap that meets the 🎯 does not pick its own successor) is why this lap stopped
  rather than starting the converse.  The next altitude lap owns that call.

## ✅ State (all verified in real output this session)

* `lake build` 🟢 **9034 jobs**, exit 0.  `src/` carries exactly the **two** pre-expedition,
  off-path, forbidden-drift `sorry`s (`PrimeLambertOscillation.phaseOscillation`,
  `MahlerDriftOne.exists_drift_one_background`).  Everything added this lap is sorry-free.
* All of these print `[propext, Classical.choice, Quot.sound]`:
  `PowerBase.tendsto_resCount`, `PowerBase.isNormalSequence_pow`, `PowerBase.isNormal_pow`,
  `G4.Sched.isNormal_four_fullRealW`, `G4.Sched.isNormal_two_pow_fullRealW`,
  `G4.Sched.isNormal_two_pow_of_schedule_read`, `G4.Sched.isDisjunctive_four_fullRealW`.
  The six `decide +kernel` anchors depend on **no** axioms at all.
* Commits this lap: `a010cd1`, `2bd68ef`, `5eadf1f`, `6f0ec8f`, `35cd03b`, `63f921c`, then the
  statement module and handoff updates through `dce89f1`.  Nothing pushed (no egress).

## 🎬 Next actions

1. **Read `HANDOFF-2026-09-15-entropy-lap128.md`** (the lap doc) and `PENDING_WORK.md`'s ACTIVE
   section — they carry the module-by-module detail this file compresses.
2. This is an **altitude decision point**, not a grind resume.  The natural successor, sketched
   in the lap doc but deliberately NOT started: the **converse** `IsNormal (b^K) x → IsNormal b x`,
   completing the Maxfield equivalence.  Split `p = K·q + r`; an occurrence of `v` (length `ℓ`) at
   `K·q + r` is an occurrence at `q`, in `blockOf`, of one of the length-`L = ⌈(r+ℓ)/K⌉`
   base-`b^K` words whose flattening carries `v` at offset `r`; each has density `b^{−KL}` and
   there are `b^{KL−ℓ}` of them.  The one new ingredient is that count —
   `#{k < b^M : wordOf b M k has v at offset r} = b^{M−ℓ}`, a bijection with
   `range (b^r) ×ˢ range (b^{M−r−ℓ})`.  Everything else already exists in `PowerBaseBlock`.
3. Optional and cheap, still open from lap 127: the Aristotle line-by-line diff of job
   `48c7d703-d18e-4e47-ae9c-6734f6737047`.

## ⚠️ Gotchas

* A **grind lap may not edit `DIRECTION.md`**; altitude laps own it.  Its forbidden-drift list is
  still live: don't touch `fullRealW`/`fullPosW`/`fullDigW`/`G4EntropyWStatement`, any
  pre-expedition G4/G5 module, `Adder*`/`CF*`/`Mahler*`/`LnTwo*`/`Stoneham*`, and claim nothing
  about the normality of **`G₄` itself** (objective B is closed: NO, in kernel).
* The two remaining `src/` sorries are deliberately untouched, not oversights.
* Lean/mathlib friction hit this lap (also flushed to the memory staging buffer):
  `Finset.range_succ` doesn't exist (`Finset.range_add_one`); `card_le_card_of_injOn`'s
  hypotheses are on **coe** sets (`Finset.mem_coe` first); pointwise goals after `refine … ?_;
  intro n` are not beta-reduced (`dsimp only` before `gcongr`/`rw`); `omega` atomises `K*j` but
  can't relate it to `K*(j+1)` (feed it `by ring`); `Int.floor_toNat` + `Nat.floor_div_natCast`
  are the clean pair for nested-floor digit work.

## 📁 Key files

- `src/NormalNumbers/BlockRigidity.lean` — `Sys.eq_uniform`, the rigidity heart (lap 127).
- `src/NormalNumbers/PowerBaseCount.lean` — `resCount` and its three exact relations (lap 127).
- `src/NormalNumbers/PowerBaseLimit.lean` — **new**: the ultrafilter glue, `tendsto_resCount`.
- `src/NormalNumbers/PowerBaseBlock.lean` — **new**: `valOf`/`flat`/`blockOf`, `isNormalSequence_pow`.
- `src/NormalNumbers/PowerBaseReal.lean` — **new**: `blockOf_digitOf`, `isNormal_pow`, corollaries.
- `src/NormalNumbers/PowerBaseStatement.lean` — **new**: audit surface + kernel anchors.
- `ROUTE-ESCALATION-2026-09-15-base2k-step3.md` — why the directive's own route died.
- `HANDOFF-2026-09-15-entropy-lap128.md` — the detailed lap doc.

---
**→ Next session: this is your starting point.  Don't summarize this doc back to Trevor or wait
for instructions, and don't offer other projects from the KB — this doc IS the chosen thread.
The headline objective is already met and the treadmill stop is signalled, so start at Next
action #1 (read the lap doc + PENDING_WORK ACTIVE) and then make the altitude call in #2.**
