# HANDOFF 2026-08-24 22:00 — W5 Lemma-13 measure balance PROVED & UNCONDITIONAL

**Branch/HEAD**: master @ `48e4396`, clean tree, `lake build` green (8736 jobs).
Supersedes HANDOFF-2026-08-24-1942.md.

## Review-lap disposition (this was a FRESH-MIND review lap)

Diagnosed **input-gathering fixation**: the prior ~10 laps built every INPUT
to B–Y Lemma 13 but never touched the assembly. Reset the stale DIRECTION
CURRENT DIRECTIVE (was pointing at completed W4) → **W5 Lemma-13 assembly**,
naming the route-decisive test as *the measure balance*. Refreshed
STATUS.md + PENDING_WORK.md. Re-verified all 8 headlines axiom-clean
(trust triple). Then GROUND OUT THE CRUX (below).

## Done this session (7 green commits, all axiom-clean) — file `TBrick.lean`

The **entire measure side of B–Y Lemma 13 is now proved and unconditional.**
The route-decisive uncertainty ("does the balance close with the repo's
non-uniform-length Lemma-5 substitute?") is settled YES, in-kernel.

- `structure TBrick (t)` (Defs 10–11): genuine CF word `w` + per base
  `2 ≤ d ≤ t` an order-`m d` cell block `r d ∈ {1,2}` containing `I_w`,
  brick-ratio field `hratio : d^{-m d} ≤ 2d·|I_w|` (repo's Prop-12 `1/(2d)`
  route, NOT B–Y's `1/(16 e^{4c} d)`).
- **d-ary side**: `volume_aggregate_daryBadZoneWide_le`,
  `TBrick.volume_aggregate_bad_le` — aggregate wide d-ary bad zone
  ≤ (Σ_d 12d²ρ_d^kmin/(1−ρ_d))·|I_w|, ρ_d = e^{−dε²/6} = `daryBadRatio d ε`.
- **CF side**: `cfBadZone`, `gaussMeasure_aggregate_cfBadZone_le` (γ),
  then `volume_le_ofReal_mul_gaussMeasure` + `measurableSet_cfBadZone` +
  `volume_iUnion_cfBadZone_le` (α, Leb) + `volume_iUnion_cfBadZone_le_vol`
  (γ-CF) — CF bad ≤ (14·SL/(δ²n))·|I_w|, SL = Σ_{v∈F}(8|v|+80).
- **good mass**: `goodExtSet`, `volume_goodExtSet`,
  `exists_C_half_le_volume_goodExtSet` — good-length extensions ≥ ½|I_w|
  (the `if..else ∅` biUnion trick = half_mass tsum verbatim, no reindexing).
- **combine**: `exists_mem_notMem_of_measure_lt` (core: good≥M, bad≤a<M ⇒
  survivor), `exists_mem_notMem_union_of_bounds` (abstract p+q<½ packaging),
  **`exists_good_avoiding_bad`** (Lemma-13 measure CORE, conditional on the
  two coefficient thresholds), and threshold lemmas
  `exists_N_cfCoeff_lt` / `tendsto_daryCoeff` / `exists_kmin_daryCoeff_lt`
  → **`exists_good_avoiding_bad_of_large`** (UNCONDITIONAL: ∃ N kmin₀, ∀ n≥N
  ∀ kmin≥kmin₀, a good-length extension of I_w avoids BOTH bad zones).

## What remains for Lemma 13 (structural, NOT measure-theoretic)

Recorded as (β)/(δ) in PENDING_WORK. The measure fight is over; these are
digit-bookkeeping to turn the surviving POINT into a refinement BRICK:

- **(δ) Lemma 13 proper**: from the survivor `x ∈ goodExtSet B.w C n \ bads`:
  (1) x lies in a good-length cylinder `J = cfCylinder (B.w ++ u)`; extract
      `u` and show its CF block frequencies are good — connect x avoiding
      `cfBadZone` to `CFDiscLt` of `u` via `CFWordBridge`
      (`blockCount_sub_countOccurrences_bounds`).
  (2) x avoids the WIDE d-ary zone ⇒ (`badBlock_cell_far`) x's order-(m+k)
      d-ary cell is distance ≥ 2 from every bad cell ⇒ a further sub-cylinder
      of J sits in GOOD d-ary cells for every base d ≤ t.
  (3) package (1)+(2) into a new `TBrick t` (or `TBrick (t+1)` via Prop 12,
      new-base ratio 1/(2(t+1)) from `interval_subset_daryCell_two`) refining
      the input brick with all discrepancies < ε.
- **(β) kmin(n) link** (feeds δ's step 2): good extensions have
  |J| ≤ 2·φ^{−2(n−1)}·|I_w| (`volume_append_mul_fib_le` in CFDigitLaw), so the
  d-ary "new digit" block length for J is ≥ kmin(n) with kmin(n) → ∞; this is
  what lets δ apply the ⋃_{k≥kmin} zone. Likely just needs an explicit
  kmin(n) := ⌊c·n⌋ and a floor/log estimate.
- **`hpos : volume (cfCylinder B.w) ≠ 0`** is currently a hypothesis of
  `exists_good_avoiding_bad*`; discharge it once (genuine cylinders have
  positive measure — see `volume_cfCylinder`) or thread it through the brick.
- Then W5 §2.1–2.2: schedule t(s),ε(s),n(s); x = ∩ intervals; three
  correctness proofs (Lemma 7/9 chains); Pillai powers-equivalence for
  "absolutely normal". Then W6 Khinchin graft.

## Gotchas (this session)

- `volume` is NOT `IsFiniteMeasure` — `measure_ne_top volume _` fails; prove
  `volume (cfCylinder w) ≠ ⊤` by hand (⊆ Ioo 0 1, `Real.volume_Ioo`).
- `positivity` can't see through the `daryBadRatio` def (it's `Real.exp`);
  feed `(daryBadRatio_pos d ε).le` / `pow_nonneg` explicitly.
- `Σ` (U+03A3) is a reserved token — can't be an identifier (`set Sbd`, not
  `set Σreal`).
- ENNReal: `ENNReal.mul_lt_mul_left/right` are IMPLICATIONS not iffs; for the
  `a<M` step use the division route (`ENNReal.div_le_of_le_mul`,
  `ENNReal.div_eq_inv_mul`). `ofReal (2*log2)⁻¹` parses as
  `ofReal ((2*log2)⁻¹)` (inv inside).
- `tendsto_finset_sum` deprecated → `tendsto_finsetSum`.
- `ENNReal.ofReal_sum_of_nonneg` turns `∑ ofReal(x_d)` into `ofReal(∑ x_d)`.
